# 手法書 METHODS — BB(6)/x2 解明の明確な手続き (2026-07-23)

このプログラムで**実際に機能した手法**を、再現可能な手続きとして明記する。各手法に前例
（それで閉じた定理）と検証規律を付す。ラベルは厳格: [PROVEN]（Lean green, `[propext,
Quot.sound]`）/ [MEASURED]（計器検証済み実測）/ [OPEN]。**本書は手法の記録であり、
いかなる機械も決定せず、いかなるラベルも昇格しない。**

---

## 第I部 — 共通規律（全手法の土台）

### M0. 測定第一の原則 (Measure first, narrate after)
**手続き:** 構造についての主張（「X は Y という形をしている」）は、形式化・文書化の前に必ず
実軌道測定で検証する。測定と物語を区別してラベルする。
**実績:** 2日間の校正記録 — 測定は全て生存、測定を超えた推論は全て失敗
（⊕未配線説 / gap(11)+80の帰属 / comb オドメータ / 固定 descRung / 固定 descTrans —
いずれも誤形式化の**前に**測定が捕捉）。
**規則:** 主張の種類を必ず自問する — これは「測ったこと」か「対象が何であるかの物語」か。
物語は失敗率が高い（本記録で 0/5）。

### M1. 計器検証 (Instrument check)
**手続き:** シミュレータ/プローブは、結論を読む前に**記録済みアンカー**への厳密一致で検証する
（x2: M1(1)@188099/1^503, M1(2)@732733/1^1021, M1(3)@2852091/1^2039; o4: 8アンカー;
Antihydra: 偶密度・M₂ᵒᵈᵈ既録値）。アンカー不一致なら**結論を保留し計器を疑う**。
**実績:** T7偵察の初回実行はアンカー3連敗 → 自作遷移表のバグ（C:1LE の向き）を捕捉。
このガードなしなら自信満々の誤答だった。o17 再検証も同様（`bytearray` 負インデックス
リスクを dict-tape 再実装で排除）。

### M2. カット原則 (The cut-point principle)
**手続き:** 輸送法則は「**総和が一様になるカット点**」で宣言する。継ぎ目の煩雑さは機械の
性質ではなく**どこで切るかの性質**。内部分解を文に固定しない（内部はレベル毎に証明可能な
ブラックボックスとして扱う）。カットが悪ければ動かす。
**実績:** trailLaw は継ぎ目 `393+trailCost+7` を見つけて閉じた。ladderStep は
regenIn/cascadeReg カットで閉じた。DescLaw は descIn カットで総和 `9(2^{k-1}−1)` が
4レベル厳密一様（内部の接合は 6歩/12歩と変動しても法則は well-posed）。

### M3. タイル+fold+強帰納 (The tile-fold-induction toolkit)
**手続き:** 自己相似カスケード機械の輸送は3段で形式化する:
1. **タイル**: 最小周期単位（6歩等）を cell 単位でトレース → `#eval` で出力確定 →
   `rfl`+`cfgPos` で証明（内部位置式は実測の移動方向と一致させる）。
2. **fold**: タイルを `∀m` 長さ帰納で fold（`steps_add`+`someBind`、堆積は
   `pow01_add`/`pow10_add`/`ones_add` 系で整理）。
3. **強帰納**: レベル間を `carryExit_strong_frame` パターンで `∀k` に組む。
   位置は `∀q` 形（`regenLaw_pos` 型）で通す — `∃p` は連鎖しない。
**実績:** sweepEF/chewFold（既存）、regenLaw_closed（07-21 クロージャ）、
ladderStep/ladderFold/ladderToCascade、descTile/descFold/descTile2/descFold2/descTrans/
descChew/descRung（本セッション11定理、全て `[propext, Quot.sound]`）。

### M4. 反空虚性統制 (Anti-vacuity controls)
**手続き:** ∀定理は必ず (a) 独立証明済みの具体レベルへの特殊化一致、(b) 未接地レベルへの
到達、(c) 可能なら独立ルートとの二重導出、を確認する。組立の総ステップ数は実測総和を
再現しなければならない。
**実績:** regenLaw_closed↔regenLaw_4/7（+RegenLaw 8,100到達、assembly_agrees_7 の
独立ルート）; ladderFold 3 5 ↔ 実 g=2 梯子; doubPhase は 2119015/8.43M/33.66M 再現が要件。

### M5. 撤回の作法 (Retraction discipline)
**手続き:** 誤りは削除せず supersession バナーで上書きし、**何が・なぜ・どの測定で**
誤りと判明したかを残す。自分の当日の主張も対象。偽の証明はゼロを維持する。
**実績:** ⊕未配線説（同日撤回）、comb オドメータ（翌日撤回）、o17「REFUTED」過大主張
（[OBSERVED]に降格、≥298状態下界のみ[PROVEN]昇格）、champion 値矛盾（解決せずフラグ）。

---

## 第II部 — x2 完全証明の手法（実行段階）

**目標:** `h_doub ∀g` を閉じ、`x2_nonhalt` を発火する（`h_init`/`h_low` は済）。

### アーキテクチャ（確定）
```
M6(g) ─topEntry(g)→ descIn(K−1) ─DescFold→ descIn 5 ─regenEntry(≈615)→ regenIn 5
      ─ladderToCascade[PROVEN]→ cascadeReg(g+9) ─tail(g)→ M1(g+1)     (K = g+8)
```

### R1. DescLaw（本丸）— 手法 (A)+(B)+(C)
- **(A) 主:** `DescLaw k : steps (9·(2^{k-1}−1)) (descIn k …) = some (descIn (k−1) …)` を
  一様カットで宣言。内部証明は `descFold (2^{k-1}−2∓j) ∘ 接合ケース ∘ descFold2 (2^{k-2})`
  — 接合は2〜3形の**有限ケース分析**（±1スロット; k=8 は両フェーズ厳密、k=6 は+1）。
  fold は `DescFold`（強帰納、M3-3）。
- **(B) 統制:** k=5..8 を chunked `rfl` で接地（279/567/1143/2295歩）— M4。
- **(C) 予備:** 接合が抵抗したら phase-2-start にカット移動 — M2。

### R2. 固定エピソード2つ
`regenEntry`（descIn 5→regenIn 5, ≈615歩, レベル非依存）と `tail(g)`
（cascadeReg(g+9)→M1(g+1), 211/184/265歩）。**手法:** cell 単位抽出（M0）→
chunked `rfl` + フレーム補題（h_init/HInit.lean の実績パターン）。

### R3. topEntry(g)（唯一の真に g 依存な輸送）
M6(g)→descIn(K−1)。パリティ分裂（奇数g は h_low の奇装飾を運ぶ; 実測 1681/43884?/6319 級）。
**手法:** h_low_even/h_low_odd と同一 — パリティ別に `∀TAIL` コアの輸送鎖を抽出し、
`steps_add` で連結、レジスタ整合は List 恒等式（`lowTurnOdd` の前例）。

### R4. doubPhase 組立
`topEntry ∘ DescFold ∘ regenEntry ∘ ladderToCascade ∘ tail` を `steps_add` 連鎖で。
反空虚性: g=2/3/4 の実測総和を厳密再現（M4）。パリティ分裂は R3 由来のみ。

### R5. 義務H
§5am 族を `realizeM1_port`+`BlankNorm`+`steps_pos_shift` で realized 形に移送し、
3仮説を同一 `M1,M6 : Nat → Cfg` で成立させる。偵察は `probe_g01`（g=0,1）から。

### R6. 最終組立と監査
`exact x2_nonhalt …` 一発 → **cold full build + `#print axioms` 全監査 + red-team**
（ラベル昇格はこの後のみ）。

**工数:** h_low 弧と同規模。**リスク:** 旧未知は全て命名・測定済み（入れ子=パリティ頑健、
基底=空白、rung総和=一様）。

---

## 第III部 — carry-transparent 島の手法（post-x2）

**対象:** x2 と同じ整数乗算型の約5–8機械（`CARRY_DICHOTOMY_2026-07-11`）。
**手法:** 第II部の道具箱をそのまま転写 — milestone 特定（M0/M1）→ カット選定（M2）→
タイル+fold+強帰納（M3）→ 統制（M4）。x2 が閉じれば各機械が同型の決定候補。
**意義:** 1104 残差を削る新しい可決定領域。**(K) には触れない**（下記）。

---

## 第IV部 — BB(6)/(K) の手法（外部段階）

### 確定した不可能性（内部手法の墓標 — 再挑戦しない）
輸送型・構造型・スペクトル型・オートマトン型の証明書は (K) に到達しない
[PROVEN 群: No-Structure(C1-C3), decider-preemption, coisometry no-go, AIU/ENT 障害,
Coverage no-go]。D10/D11 [MEASURED]: quenched 指標和は dyadic も互いに素も
**√階層**（Antihydra と o4 の両方で確認）— CLT 典型、特定軌道での証明手段なし。
**第II部の道具箱は (K) に転写しない**（頻度文 ≠ 輸送文）。

### 生きたレバー（証明済み還元 — 外部ペイロード）
1. **定理E** [PROVEN 還元]: 任意の δ>0 で低導手 dyadic 指標の power-saving
   `|Σ_{i<N, c_i odd} ψ(c_i)| ≤ C·N^{1−δ}` ⇒ Antihydra 非停止。
2. **M₂ᵒᵈᵈ** [PROVEN 還元]: 奇制限2次モーメント `Σ 2^{-(k+1)/2}M₂ᵒᵈᵈ(k)^{1/2} ≤ 1/4`
   ⇒ 非停止（実測 0.0074、マージン34倍）。
3. **o4-first** [最鋭形, 2026-07-22]: o4 決定 ⟺ `freq{ρ=1} < 0.8`（1行スカラー;
   実測 0.3336、1519倍内側; 劣臨界証明書 `5≤81` [Lean]; 破滅総和可能 = BC-I 級）。
**手続き:** この3点を技術ペイロードとして AEV 著者/解析数論コミュニティへ
（送信はオーナーの明示的 per-send go-ahead 必須 — 外部方針）。
未着手の attach: Stewart 分布化 / FFY 包含 at 3/2（外部数学）。

### 非(K)の残り（独自新着想が必要）
- **o7 / SpaceNeedle**: thin-set 到達可能性。合同/オートマトン/Baker は反駁済み。
- **o17**: ゲートタイミング。有限状態性は [OBSERVED] 非有限（≥298状態下界は [PROVEN]
  として形式化可能 — E-tier の銀行候補）。
- **工学**: champion 停止検証（E2: GO, 3–6週, `Reaches` 層が先）; 列挙・1087残差
  （コミュニティ規模, GATED）。

---

## 一枚図

| 対象 | 手法 | 段階 |
|---|---|---|
| x2 h_doub | R1–R6（M0–M5 の下で） | **実行** — 材料全証明済み |
| carry島 ~5–8機械 | 第II部道具箱の転写 | post-x2 |
| (K) 14機械 | 定理E/M₂ᵒᵈᵈ/o4-first を外部へ | **gated** — go-ahead 待ち |
| o7/SN/o17 | 新着想待ち（既知手法は反駁済み） | 開 |
| BB6 工学 | E2 GO / 列挙・残差は凍結 | 部分 gated |

**機械は未決定。ラベルは未昇格。x2 は [OPEN]、BB(6) は (K) 壁の背後。**
