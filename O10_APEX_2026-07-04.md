# o10 apex deep-dive — a single-orbit hitting of a density-⅓ set, the sole annealed-HALT mirror of Antihydra (2026-07-04)

*Deepens the P1′ leverage map's apex (`P1PRIME_EFFEQ_LEVERAGE_2026-07-04.md` §9.1). Reduces o10's halting to a
clean **single-orbit hitting statement** and, in doing so, **corrects the "BC-II / 2nd-moment / harder-than-(K)"
framing** of brick 5 (that was one failed proof strategy, not the intrinsic object). SOUNDNESS: `[PROVEN
structural]` reduction + `[OBSERVED]` 0-mismatch verification vs the authoritative epoch model; **o10 undecided,
halting `[OPEN]`, no label upgraded.* Script: `scratchpad/o10_deepdive.py`, `/opt/homebrew/bin/python3.13`.

## 0. Headline
- **o10 halts ⟺ the deterministic doubly-exponential reseed orbit `B_e` ever lands in a FIXED set
  `S_halt = {C_t : m_t odd}`** — where `m_t` is the deterministic `m ↦ ⌈3m/2⌉` orbit started at `m=6` (same every
  epoch) and `C_t = Σ_{s≤t}(1+[m_s odd])` is the cumulative `b` consumed. **Verified 0 mismatches** (halt(B) via
  `B∈S_halt` vs the epoch simulation, `B=1..5000`, 1672 halts). `[PROVEN structural + OBSERVED]`.
- **`S_halt` has density → 1/3**, with a clean reason: odd-`m` fraction `≈ 1/2`, and `C_t` grows at average rate
  `1.5`/step, so `density(S_halt) = (1/2)/(1.5) = 1/3` `[OBSERVED: 0.328, 0.334, 0.332, 0.332 at N=10³…10⁵]`.
- So o10 is a **single-orbit hitting of a density-⅓ THICK target** — a **first-order** (single-orbit
  equidistribution) object, the **mirror image of Antihydra**, NOT a 2nd-moment / BC-II object. **This corrects
  brick 5's "BC-II pair-correlation, harder than (K)" — that framing was the *failed annealed-halt heuristic*, not
  the intrinsic hardness.**

## 1. The reduction `[PROVEN structural]`
Within any epoch, `m` starts at `6` and follows the deterministic orbit `m ↦ ⌈3m/2⌉`: `6,9,14,21,32,48,72,…`;
`b` decrements by `dec_t = 1+[m_t odd]` per step, and (H-CRITERION, `O10_HALTER.md`) the epoch **halts iff `b`
reaches exactly `0` at an odd-`m` step**. Since the `m`-orbit is epoch-independent, define once
`C_t = Σ_{s≤t} dec_s`. Then `b` hits `0` exactly at the step `t` with `C_t = B`, and that is an odd-`m` step iff
`B ∈ S_halt := {C_t : m_t odd}`. Hence **`epoch(B)` halts ⟺ `B ∈ S_halt`**, a fixed subset of ℕ; `B ∉ S_halt`
(either `B=C_t` at even `m`, or `B` skipped by a `dec=2` step) ⇒ refill. (Note: `⌈3m/2⌉` has 2-adic parity
`parity(m_{t+1}) = parity(⌊m_t/2⌋)`, a pure 2-adic shift — which is *why* the `m`-orbit and hence `S_halt` are
clean and deterministic.)

## 2. The density-⅓ target, explained `[OBSERVED]`
`|S_halt ∩ [1,N]| / N → 0.332…`. Reason: half the steps have odd `m` (`[OBSERVED] 0.499`), and each step advances
`C` by `1` (even `m`) or `2` (odd `m`), average `1.5`; the odd-`m` steps therefore populate `C`-values at density
`(1/2)/(1.5) = 1/3`. This is the exact origin of the `p_e ≈ 1/3` per-epoch halt-probability (brick 5's ensemble
number) — it is the **density of the fixed hitting set**, not a coin.

## 3. The corrected hardness picture — o10 is Antihydra's mirror `[the sharpening + soundness correction]`
Both are single-orbit statements on one deterministic orbit hitting/avoiding a set:

| | **Antihydra** (`c_n`, halt=boundary) | **o10** (`B_e`, halt=`S_halt`) |
|---|---|---|
| target | thin first-passage boundary (even-density < 1/3) | **thick** set, density **1/3** |
| annealed/generic verdict | **non-halt** (even-density → 1/2) | **HALT** (thick target, orbit "should" hit) |
| order | 1st (single-orbit equidistribution) | **1st** (single-orbit hitting) — *not* 2nd-moment |
| prove the generic side | control the specific orbit's density = `(K)` | exhibit a hitting epoch = **doubly-exp INFEASIBLE** (feasibility wall, decidable-in-principle if it halts) |
| prove the exotic side | halt = measure-0 exotic | **non-halt = anti-equidistribution** (avoid a positive-density set forever) — stronger than genericity |
| orbit scale | `c_n` linear-index | `B_e` **doubly-exponential** (only epochs 1–2 feasible, both refill) |

**SOUNDNESS CORRECTION (brick 5 / `P1PRIME §9.1`):** the claim "o10's wall is BC-II = a pair-correlation /
2nd-moment statement, strictly harder than (K)" **overstated the order.** The intrinsic object is a **first-order
single-orbit hitting** of a density-⅓ set. "BC-II needs independence" (`O10_HALTER.md` `[PROVEN negative]`) is
correct but it describes the **failed annealed-halt heuristic** (modelling `B_e` as fresh random draws), not the
intrinsic hardness. The correct apex statement is:

> **o10 is the sole annealed-HALT machine** (the density-⅓ target makes "generic ⇒ halt", mirror of Antihydra's
> "generic ⇒ non-halt"). Deciding it needs EITHER a hitting epoch (a finite but doubly-exp-infeasible computation —
> so *if o10 halts it is decidable in principle*) OR, for non-halt, a proof the reseed orbit **avoids a
> positive-density set forever** = anti-equidistribution, a statement stronger than the genericity (K) asks for.
> It is the apex not by moment-order but by being the **only machine whose generic verdict is HALT** and whose
> non-halt side demands anti-genericity.

## 3b. o10's thick target is itself MAHLER-3/2-defined — o10 = (K) + BC-II `[OBSERVED, 2026-07-04]`
A further structural fact: the density `⅓` of `S_halt` is **not a free coin — it is a single-orbit Mahler-3/2
equidistribution.** `S_halt = {C_t : m_t odd}` where `m` runs the `⌈3m/2⌉`-orbit from `6` — a `×(3/2)` (ceiling)
value orbit, exponentially growing (`m` reaches `58 499` bits by step `10⁵`). Its **odd-density** is
`0.49744 ≈ ½` `[OBSERVED]` — the *same kind* of single-orbit `×3/2` parity equidistribution as Antihydra's
even-density `0.50159 ≈ ½` (the `(K)` object). And `density(S_halt) = (odd-density)/(C-rate 1.5) = ½/1.5 = ⅓`. So:

> **o10's halt target is Mahler-3/2-defined.** Proving `density(S_halt)=⅓` is itself a single-orbit `×3/2`
> equidistribution — a `(K)`-**type** statement about the seed-`6` `⌈3m/2⌉`-orbit. Hence o10 carries `(K)`-hardness
> in **two** places: **(i)** the target density (`(K)`-type, the `m`-orbit parity), **and (ii)** the doubly-exp
> reseed orbit hitting/avoiding it (BC-II / pair-correlation). **o10 `= (K)` [in the target] `+` BC-II [the reseed
> layer]**, cleanly confirming `o10 ⊋ (K)` and — importantly — **connecting the sole "outside" residual back to the
> Mahler kernel.** Even o10 is Mahler-3/2 at its core; it merely wraps `(K)` in a reseed/BC-II layer that the
> thin-target majority does not have.

**SCOPE `[SOUNDNESS CORRECTION, same-day red-team]`.** This does **not** say "the whole frontier is Mahler-3/2."
o10 is a **Type-I (nested-Mahler) machine** — its inner map is literally `⌈3m/2⌉`, so of course its target is
`×3/2`-defined; the finding is that o10's target density is a `(K)`-type quantity, hence **`o10 = (K) + BC-II ⊋
(K)`**, placing the sole divergent Type-I machine cleanly above the kernel. The **non-Mahler** cryptids
(o3, o17 Type II; Space Needle Type III; H5 Type IV) are **floor-multiplier / generalized-Collatz**, *not* Mahler-`3/2`
(their `⌊(p/q)·⌋` is a `2`-adic-driven or `2/3` or carry map, not the `×3/2` value orbit). The **universal** object
is **"floor-multiplier / non-affine"** (`CRYPTID_NONAFFINE_UNIFICATION`), *not* "Mahler-3/2"; Mahler-`3/2` is the
Type-I subset (which includes o10). *(Correction: an earlier draft here over-claimed "entire frontier
Mahler-3/2-connected"; only Type-I is.)*

## 3c. o10 is the SOLE thick machine — o13 tested and found THIN `[OBSERVED, 2026-07-04]`
`NESTED_COLLATZ_THEOREM.md` left o13 `[OPEN — could be divergent like o10]`. Tested: **o13 is THIN, not thick.**
Over the blank orbit, o13's halt-approach (`D` reads `1`, then `E` reads the left cell; halt iff `0`) fires
`5518` times with the left neighbour **always `1`** (structurally avoided, like Antihydra's gate). Its value orbit
is **single-level `×3/2`** — value peaks `…,161,250,377,574,863,1303,1957,2938,4409` with clean ratio `1.50` — **not**
a nested doubly-exponential reseed like o10 (`5,57,2.1×10⁸`). **The discriminator is nesting:** o10 is the sole
*nested* machine whose per-epoch parity is a generic fair-coin (⇒ thick / generic-HALT); o13 (and the other Type-I
machines) are single-level `×3/2` orbits with a spontaneous-defect halt (⇒ thin / generic-non-halt). **So o10
remains the unique thick residual** — a test that could have found a second thick machine confirmed o10's uniqueness
instead.

**Extended to the whole Mahler family `[OBSERVED, 2026-07-04]`.** The `p=3` (`×8/3`) machines **o18 and o15** are
also **THIN**: over 25 M steps each enters its halt-state only **9–10 times** (a nested per-outer-epoch check, the
epochs doubly-exp spaced) with the halt config **never present** (`danger=0`, structurally avoided). So o18/o15 are
**nested-but-thin** (BC-I convergent existence events — o18's "frontier-bit", `PROBLEM_LIST.md`), *not* thick. Across
the **entire Mahler family** (Antihydra, o10, o13, o18, o15) **only o10 is thick**: it is the unique *nested* machine
whose per-epoch event is a **generic fair-coin parity** (density `⅓`, BC-II/divergent), while every other Mahler
machine — nested or not — has a **thin spontaneous-defect / existence** halt (BC-I/convergent). **o10's uniqueness as
the sole thick machine is now confirmed across the whole Mahler class.**

## 4. Consequence for the leverage map
o10 stays the sole out-of-BC-scope divergent machine, but the **reason is sharpened**: its target is thick
(density 1/3), flipping the annealed verdict to HALT, so EFF-EQ's BC-I (convergent, thin-target, non-halt) engine
**structurally cannot address it** — not because o10 needs a higher moment, but because it is on the **opposite
(divergent/anti-genericity) side of the same single-orbit axis**. Antihydra and o10 are the two poles of the
single-orbit-equidistribution axis; the whole spontaneous-defect frontier clusters at the Antihydra (thin,
generic-non-halt) pole, o10 alone at the thick pole.

## 5. Honest verdict
**(b) — a sharper characterization + a soundness correction; no machine decided.** o10 halting is `[PROVEN
structural]`-reduced to a single-orbit hitting of a fixed density-⅓ set `S_halt` (verified 0-mismatch); the `1/3`
is `(½)/(1.5)`; o10 is Antihydra's mirror (sole annealed-HALT), and brick 5's "2nd-moment/BC-II harder-than-(K)"
is corrected to "first-order, opposite pole, anti-genericity on the non-halt side, feasibility-blocked on the halt
side." **Halting `[OPEN]`. No machine decided. No label upgraded.**

## Reproduce
- `scratchpad/o10_deepdive.py` — builds `S_halt` from the `⌈3m/2⌉`-from-6 orbit, verifies `halt(B) ⟺ B∈S_halt`
  (0 mismatches, B≤5000), density → 0.332, reseed orbit `5,57,2.1×10⁸` membership. o10 TM
  `1RB1RA_0RC1RC_1LD0LF_0LE1LE_1RA0LB_---0LC`; model authoritative in `O10_HALTER.md` / `O10_REDUCTION.md`.
