# R5 — H upper bound, the arithmetic pinned (2026-07-24)

Goal: pin `j = 10` in `hlow_padded` (`∃ j ≤ 16`, and `hlow_j_ge` gives `10 ≤ j`), so the padded
low phase delivers exactly the `[false] ++ zeros 10 = zeros 11` left that `doubPhaseEven`
consumes. **No machine decided. No label upgraded.**

## The arithmetic is fully pinned by measurement

* **Head reach is `−6`, k-independent** (`#eval`, g=2 and g=4): over the whole `267+38(2k+2)`-step
  low run with left `zeros 16`, the head's minimum pos is `−6`. So cells at index `−7 … −16`
  (the lower 10) are **never touched** and survive verbatim.
* **OUT pos is `−5`, pad-independent** (`#eval`, pad = 0/4/8/16 → left length 1/1/3/11). So
  `j = m − 6` for `m ≥ 6`, i.e. `j = 10` at `m = 16`.
* **Full config equality at `m = 16` holds ∀ TAIL** (`#eval` at k=0,1): the padded OUT is
  exactly `⟨.E, −5, ⟨[false] ++ zeros 10, false, M6-right⟩⟩`.

## The Lean pin — two routes, one recommended

* **Lower bound (DONE, GREEN):** `hlow_j_ge` — `steps_left_mono` gives `16 ≤ 6 + j`, so `j ≥ 10`.
* **Upper bound (the residue):** `j ≤ 10`. This is NOT obtainable from `steps_left_mono` alone
  (it is a one-sided monotone). Two options:
  1. **A "boundary-inert" BlankNorm lemma** — if a run keeps `pos ≥ −d` throughout, then
     padding the left with `zeros k` beyond depth `d` survives entirely (`j = k − d₀` for the
     fixed base). Needs the measured head-reach `−6` as a Lean invariant (`steps_pos_ge`,
     provable by induction on `steps` tracking `min pos`). **This is the cleaner route** — one
     general lemma, reusable for the odd branch and obligation H.
  2. `++ LL` ports of the five low-phase lemmas (`lowEntry`/`lowMiddle`/`lowTurn`/`lowReturn`/
     `lowExit`), each left-padded (`ent_c0` already recompiles with `zeros 3 ++ LL`; the whole
     chain follows). Mechanical (p1tLL-style) but 5–6 theorems.

**Recommendation:** route 1 (`steps_pos_ge` + a boundary-survival lemma).  It pins `j` for BOTH
parities at once and is the same tool obligation H (init connection) will need. Route 2 is the
fallback if the invariant proof is awkward.

## Status

R5's mathematics is fully MEASURED and the lower half is GREEN. The remaining Lean object is one
monotone-position invariant (`steps_pos_ge`) plus a one-line boundary-survival corollary — a
BlankNorm-scale lemma, not a low-phase re-derivation.
