
### The odd climb IS the standard ladder — every span is a library constant

`#eval` against the measured spans:

| span (measured) | library constant | |
|---|---|---|
| → cReg 8 | `exitSteps 8 = 9 282` | ✓ |
| cReg 8 → regenIn 9 | `topGrindSteps 8 = 64 775` | ✓ |
| regenIn 9 → cReg 9 | `exitSteps 9 = 35 202` | ✓ |
| cReg 9 → regenIn 10 | `topGrindSteps 9 = 260 615` | ✓ |
| regenIn 10 → regenIn 11 | `exitSteps 10 + topGrindSteps 10 = 1 181 961` (`ladderStep 10`) | ✓ |
| regenIn 11 → cReg 11 | `exitSteps 11 = 536 066` | ✓ |

So from `cReg 8` upward the odd branch runs the **standard alternating ladder**
(`exit → topgrind → exit → topgrind → …`), with `Lc = 1` at every internal rung — identical to
the even branch. **Nothing odd-specific happens in the climb at all.**

(Correction: the earlier "descIn 9 → cReg 8 = 38 502" was wrong — the `+9 282` line was relative
to an intermediate entry the truncated output hid, and 9 282 is `exitSteps 8`. The descent leg's
own span still needs its own measurement.)

**Where the odd-specific content actually lives:** only in the DESCENT from `descIn (g+6)` down to
the bottom of the ladder, where `ones 20` and the `pow10 N` comb are absorbed — and in the TOP
rung, where `Lc` becomes 6 (already handled: `oddTopRung`, `oddTopRungToMilestone`, both green and
on-orbit). The middle is the even machinery verbatim.

**No machine decided. No label upgraded.**
