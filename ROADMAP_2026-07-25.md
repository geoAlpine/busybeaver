# BB(6) 完全証明 — ロードマップ（2026-07-25）

本日までの全知見を統合した実行計画。**到達可能なものと到達不可能なものを分けて書く** — 
これが本ロードマップの要点で、混ぜた瞬間に計画は嘘になる。
**No machine decided. No label upgraded.**

---

## 0. 一枚の地図

```
                        BB(6) 完全証明 = 17 機すべて決定
                                    │
                ┌───────────────────┴───────────────────┐
                │                                        │
      template クラス（~3〜8 機）              (K) クラス（~14 機）
      carry 透明 / 乗数 = 基数                  Mahler 核 ＋ o17 核
                │                                        │
      有限の厳密輸送 + ∀g 帰納                  単一軌道の桁正規性
      → Lean で無条件に閉じられる               → 外部の未解決数学
                │                                        │
        ★ 到達可能（工学）                      ★ 内部到達不能（証明済み）
```

**この分割は今日、機械的に確定した。** 判別基準は carry 透明性＝乗数が基数の冪か
（`carry_transparency.py`、`ATTACK_TRANSPARENCY_2026-07-25.md`）。

---

## 1. 到達可能な部分 — 実行計画

### フェーズ A：x2 を無条件に閉じる（現在地）

| 段 | 状態 | 残作業 |
|---|---|---|
| 偶 doubling phase | ✅ GREEN（`doubPhaseEven`） | — |
| 奇 topEntry | ✅ GREEN（`topEntryOddFull`）＋**軌道照合済み** | — |
| 奇 exit REGEN ∀コム | ✅ GREEN（`regenLawGenC_ge7/_closed`） | — |
| 奇 top rung | ✅ GREEN（`oddTopRung`、無条件）＋marker/右側とも軌道照合済み | — |
| 奇 rung→milestone | ✅ GREEN（`oddTopRungToMilestone`） | — |
| **奇 spine** | ⚠ 緑だが**族が軌道と不一致** | `descIn 9 → cReg 11` の内訳分解 → 述べ直し |
| 奇 doubling phase | ⬜ | `topEntryOddFull ∘ oddSpine` の M8 組立 |
| H 上界（R5） | ⬜ | `j = 10` の確定（算術は済、`steps_pos` 不変量 or ++LL 移植） |
| 義務 H 組立 | ⬜ | `hlow16 ∘ doubPhase{Even,Odd}` 両親等 |
| F（最終） | ⬜ | `exact x2_nonhalt …`、cold build、`#print axioms` 監査、red-team |

**律速は今 1 点：** `descIn 9 → cReg 11`（g=3 で 2 117 201 歩）の内訳。
実測スパンが「梯子 1 段」説を否定済み（差 1 581 657 vs 裸 1 段 1 181 961、緑 `decide`）。
分解が出れば `oddSpine` の正しい形が決まり、以降は機械的。

**見積り：** 内訳分解 1 セッション → 奇 spine/phase 1〜2 セッション → H/組立/監査 1〜2 セッション。

### フェーズ B：template 島を掃く（~5〜8 機）

x2 で実証された道具立てを他の carry 透明機に適用：
1. **透明性判定**：各 cryptid の乗数 μ を確定し `μ = 2^a` を選別（`carry_transparency.py` の基準）
2. **テンプレート移植**：x2 の `topEntry / spine / topRung / tail` の骨格を機ごとに再インスタンス化
3. 各機ごとに M0〜M9 規律で実行（測定 → タイル → 折り畳み → 組立 → 反空虚性）

**注意：** これは「x2 の証明を書き直す」のではなく、**同じ形の証明を機ごとに埋める**作業。
x2 で得た ∀Lc 一般化・位相アンカー・長さアンカーはすべて再利用可能。

### フェーズ C：包囲（Tier 0）— 不可領域を 2 公理に圧縮

```
axiom K_Mahler : <単一軌道 ⌊μⁿ⌋ mod p 等分布, μ = 2^a/3^b>
axiom K_o17    : <o17 の marker が偶にならない>
theorem BB6 : … := by  -- 14 機を上の 2 公理から、残りを無条件に
```
- 14 機の (K)-還元（現在 python/紙）を Lean の定理にする
- **効果**：外部で AEV 型定理が出た日に BB(6) が即 discharge される状態を作る
- **副次**：#32（AI 大規模証明探索）の前提となる形式化がここで完成

---

## 2. 到達不可能な部分 — 正直な記述

### 2.1 なぜ不可能か（証明済みの層）

| 層 | 内容 | 出典 |
|---|---|---|
| 1 | seed 一様上界は不存在（φ=1 が −14 で実現） | #30 反証 |
| 2 | 解析的頻度 bound 全種が死ぬ（単一膨張写像の完全マルチフラクタル） | O4_FREQUENCY_RIGIDITY |
| 3 | 第二の乗法独立写像は 3 供給源すべて閉（2-adic 従属因子 / 自明自己同型群 / 完全シフト） | O4_KWALL_CLOSURE |
| 4 | 構造のみ証明書（sub-action・LP・magnitude・adelic） | BB6_NO_STRUCTURE_THEOREM |
| 5 | スペクトル／作用素ノルム縮小 | ENDOGENOUS_UE, EUE_COISOMETRY |
| 6 | 既存 rigidity/equidist フレームワーク全滅 | RANK1_AMENABLE_EQUIDIST |
| 7 | 有限例外集合ハイブリッド（`E(D*)` 密度 `2^{−D*}>0`） | route18 |
| 8 | 有界窓／透明-carry 輸送（o17 の C(3j) witness） | route_o17 |
| 9 | 基数変換による透明化（2・3 同時透明化不可＝Furstenberg） | ATTACK_TRANSPARENCY |
| 10 | 外層/入れ子層の分離（o10/o15） | o10o15/o15_transport_probe |
| 11 | 解析的/付値/Parikh 証明書言語 | route20 |

**核は 2 つで最終確定**（相互還元 (d) 独立、`route22`）。

### 2.2 それでも残っている攻撃面（期待値順）

| 手段 | コスト | 期待値 | 備考 |
|---|---|---|---|
| **Tier 0 包囲** | 中 | 確実（≠突破） | 外部事象の即時換金装置 |
| **#18 前提補集合マイニング** | 小（1 発火） | 低いが生成器 | 層 1〜11 の前提を条項単位で破る攻撃を機械生成。**未発火の最後の生成器** |
| #32 AI 証明探索 | 中（Tier 0 後） | 低 | 形式化核への網羅探索 |
| Tier 2 AIU/ENT 建設 | 世代規模 | ~0/セッション | 仕様は書けている（7 制約フィルタ） |
| Tier 4 文献監視 | 小・継続 | 外部依存 | 読み取りのみ（方針適合） |

---

## 3. 実行順序（推奨）

```
1. [今]  descIn 9 → cReg 11 の内訳分解        ← 律速、スキャン実行中
2.       oddSpine 述べ直し → doubPhaseOdd      ← 機械的
3.       R5（H 上界）→ 義務 H 組立 → F 監査     ← x2 完成
4.       #18 を 1 回発火                        ← 最後の未試行生成器、安価
5.       Tier 0(a)：14 機の (K)-還元 Lean 化    ← 包囲の本体
6.       フェーズ B：template 島を機ごとに      ← 反復作業
7.       #32 ＋ Tier 2 ＋ 文献監視              ← 長期
```

**4 を 3 の後に置く理由：** #18 は層の前提を破る攻撃を生成するが、層は今日 11 に増えた。
生成空間が広がった状態で 1 回だけ撃つのが情報効率最大。

---

## 4. 規律（全フェーズ共通）

- **発火前に前提を測定**（#30 の教訓：マージン ≠ 証明可能性）
- **測定 → タイル → 折り畳み → 組立 → 反空虚性**（M0〜M9）
- **緑の定理 ≠ 軌道上の定理** — 合成前に必ず M4 で族を照合（今日 `oddSpine` で作動）
- **フルログ grep**（`sorryAx` の静かな混入を今日 2 回捕捉）
- ゼロ偽証明。ラベルは監査なしに昇格しない
- 新案は再突入禁止表（§2.1）と照合してから発火

---

## 5. 正直な見通し

**達成できるもの：** x2 の無条件証明、template 島（~5〜8 機）、そして
**「BB(6) = f(公理K1, 公理K2)」という Lean の定理**。これは実在の、検証可能な成果物である。

**達成できないもの：** 無条件の BB(6)。(K) は外部の未解決数学であり、
内部の全手法クラスに proven no-go が立っている（§2.1）。

**この 2 つを混ぜないことが、本プログラムの唯一の誠実さである。**

**No machine decided. No label upgraded. x2 remains [OPEN].**
