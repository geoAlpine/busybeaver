# o17 gate law — the protection gets its coordinate system: exact milestone transducer F, the wall-and-erosion gate-time law (tower sparsity), the 5-branch determinant mapped (rigid islands + all finite reductions refuted) (2026-07-07)

*Task: give o17's protection conjecture a precise formulation — gate-time law, 5-branch determinant, protection
statement, species. o17 = `1RB1LD_1RC0LE_1LA1RE_0LF1LA_1RB0RB_---0LB` (halt = F reads 0). Basis:
`O17_HALT_FLAVOR_2026-07-06.md` (verdict "sparse-gate carry-timing"), `O17_CORE_TRANSDUCER.md` (milestone language
𝓛, marker automaton, width identity). SOUNDNESS: `[PROVEN]`/`[OBSERVED]`/`[OPEN]`; every F-value below is an exact
finite TM run; nothing about halting decided. Interpreter `/Users/aokiyousuke/quantum-ecc/.venv/bin/python`; scripts
`o17_gate_map_2026-07-07.py`, `o17_gate_ticks_2026-07-07.py`, `o17_gate_regular_2026-07-07.py`,
`o17_gate_branch_2026-07-07.py`, `o17_gate_substitution_2026-07-07.py`, `o17_gate_census.c`, `o17_gate_front.c`.
Not committed.*

---

## 1. The coordinate system: the milestone transducer F `[PROVEN parameterization, machine-validated]`

A **gate** = true-frontier arrival (head in state A or D reads 0 with all-0 tape to its left); D-arrival = HALT
(`[PROVEN]`, prior). Every A-gate configuration is a word of 𝓛 (`[OBSERVED, 0 exc]`, prior) and hence **fully
parameterized by the pair (marker μ ∈ {3,5}, digit vector d⃗)** — so the gate-to-gate map

>   **F(μ, d⃗) = (μ′, d⃗′)**, with excursion tick count **T** and step count **Δt**,

is the EXACT dynamics (deterministic TM; a synthetic (μ,d⃗) start is byte-identical to the real gate config).
**Validation:** iterating F from (3,[]) at t=5 reproduces the blank orbit's gate steps
`5, 22, 44, 101, 314, 724, 2005, 1072566` exactly, digit vectors included; the independent C census
(`o17_gate_census.c`, leftmost-1 tracked exactly) reproduces the same list (`o17_gate_map_2026-07-07.py` §1).
All coordinates below are components of F.

## 2. The gate-time law: WALL-AND-EROSION, tower sparsity

**(a) The step clock is exactly quadratic in ticks `[OBSERVED, tight; forced by the proven width identity]`.**
With n = odometer ticks (right-end (E,0) reversals): t ≈ 3.97·n² (measured at n=403,640: t=6.5·10¹¹, ratio 3.99;
width = 3n+O(1)). So gate times in steps are the tick sequence squared; the law lives in tick time.

**(b) Gate ticks `[OBSERVED, C census]`:** blank: n = 1, 5, 11, 21, 533, then **none to n > 403,000 (t > 6.5·10¹¹)**;
j=3: n = 1, 4, 10, 18, 66, none to 223,667 (t > 2·10¹¹); j=6: n = 1, 6, 12, 52, 76, none to 223,677 (t > 2·10¹¹).
**Geometric law t_{k+1} ≈ λ·t_k REFUTED**: blank step-ratios run 4.4, 2.0, 2.3, 3.1, 2.3, 2.8, **535, >6.0·10⁵** —
not a constant λ, an accelerating cascade. Also refuted `[OBSERVED]`: n_{k+1} ≈ n_k² (284,089 passed gateless).

**(c) The mechanism `[OBSERVED, snapshots + front tracker]`.** What sets T_k = n_{k+1} − n_k:

1. **Wall erection.** At gate k the era's free-running LSB counter (value ≈ T_{k-1}) **shatters into ≈ T_{k-1}
   zero-blocks**: blank gate 7→8 has T=512 and the arrival vector jumps m: 8 → 19 → (within 500 ticks) ≈ 519,
   its prefix = the old digits + a wall of ~500 zero-blocks. (Same at gate 6→7: T=10, arrival `[0,2,0,0,0,0,0,16]`
   with a 5-zero wall.)
2. **Freeze.** During the void the string is a **frozen prefix + active tail**: snapshots at ticks 1000/2000/4000/8000
   share a 490–499-block common prefix (of ~515); all growth is the new LSB (+1 per tick, exactly: 465, 1465, 3465,
   7465); the carry cascade's leftmost penetration ("front") stays within ~25 blocks of the right end.
3. **Erosion.** The front's per-window minimum grinds left: ≈ 650 ticks per wall block at n ≈ 5·10³, ≈ 13,000
   ticks/block at n ≈ 6·10⁴ (`o17_gate_front.c` to n=123,001) — erosion cost per block grows ∝ n (≈ a·n,
   a ≈ 0.1–0.2). A gate fires only when the front reaches block 0.

**(d) The law `[OBSERVED + extrapolation — NOT proven]`.** Integrating (c): log(n_{k+1}/n_k) ≈ a·m_k with wall height
m_k ≈ T_{k-1} ≈ n_k, i.e. **log n_{k+1} ≈ a·n_k: gate ticks grow as an ITERATED EXPONENTIAL (tower)** — each survived
gate converts its whole era into the wall that delays the next gate exponentially. Extrapolated next blank gate:
front ≈ 481 remaining wall blocks at n ≈ 1.2·10⁵ ⟹ n₉ ~ 10^{30±10} ticks, **t₉ ~ 10^{60±20} steps** — beyond any
simulation, consistent with (and explaining) every observed void. Whether the front reaches 0 at all (gate
recurrence) is `[OPEN]` — it is the carry-history question itself.

## 3. The 5-branch determinant: b = F_μ, its islands and its irreducibility

The halt bit at gate k is **b_k := F_μ(μ_k, d⃗_k)** — exactly defined, computable, deterministic. What decides it:

- **Rigid islands `[OBSERVED, value-robust]`:** F_μ(5, []) = 8; **F_μ(5, [d]) = 3 for ALL d ≤ 1000** (safe);
  **F_μ(5, [d₁,d₂]) = 8 for ALL tested pairs** incl. (1000,0), (0,1000), (200,200) (fatal); F_μ(3, [d]) = 5 ∀d ≤ 1000.
  At a 5-gate with m ≤ 2 digits the branch is decided by m ALONE: m ∈ {0,2} dies, m = 1 survives. (The blank orbit's
  first 5-exposure, t=44, was the m=1 island; its second, t=724, was deep (m=5) and survived by the full cascade.)
- **All finite reductions REFUTED for m ≥ 3 `[OBSERVED, 4374-config ensembles]`** (`o17_gate_regular_2026-07-07.py`,
  `o17_gate_branch_2026-07-07.py`): b is NOT a function of — the residue word (d_i mod q) for q = 2, 3, 6 (36
  ambiguous classes at q=6; q=12 "decides" only vacuously, one sample per class); any prefix/suffix window of d⃗ up
  to length 5; digit values under +6 shifts (branch flips: e.g. (3,[6,3,0,1]) coord 1 gives μ′ = 5,5,3,3 with T =
  12,56,156,338 — wildly non-affine); the final-tick (breakthrough) left window K ≤ 4; (m, S, parities, N mod small,
  v₂/v₃ recodings). The carry front is non-monotone (bounces: 5,7,8,6,8,2,13,…), so no moving-front locality either.
- **Exact definition (deep but pinned):** b = whether the breakthrough carry RESETS (5→3) or INCREMENTS (5→8) the
  top digit; it is a total, computable function of the full digit vector, with the m ≤ 2 islands as its only known
  finite part. The minimal known description of b **is F itself** — and §2's law says future gates have
  m_k ≈ n_k → ∞, far outside every island.

## 4. The formulation — o17's protection statement, now precise

> **PROTECTION(o17, seed):** for every gate index k with μ_k = 5:  **F_μ(5, d⃗_k) = 3.**
>
> **o17 halts ⟺ PROTECTION fails** `[PROVEN reduction: gate lemma + 𝓛-parameterization + F exactness]`,
> where the gate sequence (μ_k, d⃗_k, n_k) is the F-orbit of the seed, t_k ≈ 3.97·n_k², and
> log n_{k+1} ≈ a·n_k (tower sparsity, §2d `[OBSERVED]`).

**Species call: genuinely timing-based — NEW.** Not o15-style pattern-avoidance: the forbidden event is not a
regular/bounded-window pattern (§3 refutations). Not o4-style return-frequency: o4's branch driver is a scalar
residue with affine branch maps and exact v₃ fixed-point run laws (`O4_RUN_STRUCTURE_2026-07-07.md`); o17's marker
transitions have **no fixed-point structure** — no scalar exists (prior `[PROVEN]`), v₂/v₃ recodings fail, and the
branch driver is the unbounded string. o17 is **tower-sparse regenerative-wall carry-timing**: each survived gate
erects a wall of height ≈ its own era length, making protection SELF-REINFORCING in time (exponentially rarer
exposure) while remaining permanently 1 step from death in value (marker 5 → 8). The protection conjecture is a
sparse-sequence avoidance statement over an infinite-state transducer — the first B2 cryptid whose non-halt
statement cannot be compressed below the full transducer.

## 5. The √step lens (substitution test) — partial self-similarity

The string IS self-similar in a weak, history-stack sense `[OBSERVED]`: s(2N) = s(N) on a ≥96% prefix (the frozen
wall = the previous arrival string), plus a free-running LSB and an O(25)-block active tail. But the wall CONTENTS
are the irregular arrival vectors (not generated by any fixed substitution — shape census of the halt-flavor note),
so no automatic-sequence computability of the gate law is claimed; the self-similarity explains the √step growth
(width 3n, content n) and the freeze, not the branch.

## 6. Honest scope
- Nothing decided: blank/j=1 non-halt stays `[OPEN]`; all census bounds (6.5·10¹¹ blank, 2·10¹¹ runners) prove nothing.
- §2d's tower law is a two-era + erosion-curve extrapolation `[OBSERVED]`, not a theorem; the erosion exponent
  (∝ n vs ∝ n^{1.1}) is not pinned; gate recurrence itself is `[OPEN]`.
- §3's island laws are value-robust over tested ranges only (d ≤ 1000 single, mixed pairs); no induction proved.
- F-exactness rests on the 𝓛-parameterization, which is `[OBSERVED, 0 exceptions]` (prior note), plus the
  byte-identical synthetic construction (checked against the real orbit, §1).

## Reproduce
- `o17_gate_map_2026-07-07.py` — F tabulation (1312 configs), blank-orbit validation (exact gate-step match),
  branch projections, structured T families, d ≤ 25 island scan.
- `o17_gate_ticks_2026-07-07.py` — tick-by-tick decoded strings (the carry rewrites, zeros⇄values), product-law tests.
- `o17_gate_regular_2026-07-07.py` — residue-word (mod 2/3/6/12) and +6-shift refutations (4374 configs).
- `o17_gate_branch_2026-07-07.py` — final-tick window refutation, carry-front bounce statistics, m growth.
- `o17_gate_substitution_2026-07-07.py` — big-digit island stress test, front histogram, geometric snapshots.
- `o17_gate_census.c` — exact long census (blank 0/9/18 seeds; validated against Python gate lists).
- `o17_gate_front.c` — per-window carry-front minima to n=123,001 (the erosion curve).

**No machine decided. No label upgraded.**
