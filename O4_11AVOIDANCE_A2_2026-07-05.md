# Proving o4's 11-avoidance (path A′) — the cleanest reduction: "F never reads 1", with the mechanism (2026-07-05)

*Path A′: prove the base-4/3 odometer never emits `11` in `B`'s path (⟺ o4 non-halting). This turn **reduces the
theorem to its sharpest form and pins the mechanism**: o4 halts iff **state `F` ever reads `1`**, and `F` is entered
**only** by `B:1→1RF`, so `F` reads the cell right of a `B`-read-`1`. Verified: `F` reads `0` **2,488,446 / 2,488,446**
times; the mechanism is that `B` is **created** on its read-cell and always lands on the **right** `1` of any `11`
(never reads a left `1`). The remaining core — proving this invariant holds for all `G` — is the non-regular
unbounded-computation theorem (turn 4). SOUNDNESS: `[OBSERVED/VERIFIED]`; o4 `[OPEN]` — not decided. No machine decided.*

## The sharpest reduction `[PROVEN from transitions + VERIFIED]`
o4's only halt is `F:1→---`. `F` is entered **only** via `B:1→1RF` (`B` reads a `1`, writes `1`, moves R, →`F`), so
`F` reads the cell **immediately right** of a `B`-read-`1`. Hence:
> **o4 does not halt `⟺` `F` never reads `1` `⟺` every `1` that `B` reads has right-neighbour `0`.**
Verified over 10⁷ steps: `F` reads `1` **zero** times (of 2,488,446 `F`-reads); every `F`-read is head-`0` with
left-`1` (context `1 0 1` bulk, `1 0 0` at boundaries).

## The mechanism `[OBSERVED, the why]`
`B` does **not** sweep as `B`: `B:1→F` and `B:0→C`, so `B` reads exactly one cell then leaves. `B` is **created** at its
read-position `p` by `A:0→1RB` or `F:0→0RB` (from `p−1`, moving R). The `B`-read-`1` windows `(L,head,+1,+2,+3)` are
`(0,1,0,1,0)` (bulk sweep), `(1,1,0,1,0)` (`B` on the **right** `1` of a `11`), `(0,1,0,0,1)` (near the `1001` cap) —
**all with `+1 = 0`**. So when a `11` exists, `B` sits on its **right** cell (`A` filled the gap to `B`'s left); `B`
**never reads a left `1`** (that would send `F` into the second `1` = halt). The `11`-avoidance is therefore:
> **whenever `B` is created at `p` and reads `1`, `tape[p+1]=0`** — `B` is never created on the left `1` of a `11`.

## What remains — the invariance is the non-regular core `[honest]`
The reduction is maximally clean and the mechanism is understood, but **proving the invariant holds for all reachable
configs** is exactly the non-regular statement turn 4 showed has **no `m`-gram certificate** (`m≤26`): its invariance
requires tracking the base-4/3 odometer (big-gap counter). So the theorem "`F` never reads `1`" is a genuine
**unbounded-computation** result — its truth is a property of the base-4/3 carry cascade for all `G`. This turn gives
the **statement a proof must establish** in its tightest form (a local one-line invariant on `B`'s creation), and the
verified mechanism; the invariance proof itself (by induction over the odometer / a counter-automaton certificate)
remains the substantial work.

## Verdict
**(b) — the 11-avoidance theorem reduced to its sharpest form + mechanism, verified.** o4 non-halt `⟺` `F` never reads
`1` `⟺` `B` is never created on the left `1` of a `11`; verified `0/2.49M` `F`-reads-`1`, mechanism pinned (`B` always
on the right of any `11`). The **invariance for all `G`** is the non-regular unbounded-computation core (turn 4). Real
progress toward the proof; **o4 not decided**. **Halting `[OPEN]`. No machine decided. No label upgraded.**

## Reproduce
- `/opt/homebrew/bin/python3.13`: `F` reads `1` `0/2488446`; `B`-read-`1` windows all `+1=0`. Basis:
  `O4_VERIFIER_BUILD_A1_2026-07-05` (unbounded generation), `O4_VERIFIER_BUILD_T4_2026-07-05` (non-regular),
  `O4_HALT.md` (halt = `F:1`).
