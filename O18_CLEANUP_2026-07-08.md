# o18 cleanup — the 5 SPLIT adjudications resolved + the T_ext re-run of the adversarial analyses (2026-07-08)

*Closes the two queued items of `O18_R1_PINNING_2026-07-08.md` §4: (1) the 5 unresolved
SPLIT adjudications from the base-vs-T_ext equivalence fuzz; (2) the T_ext re-run of the
BAD-attractor and adversarial-reachability analyses of `O18_INVARIANT_SYNTHESIS_2026-07-07.md`.
Scripts: `o18_clean_splits.py`, `o18_clean_attractor.py`, `o18_clean_reach.py`,
`o18_clean_reach2.py`. Interpreter: `/Users/aokiyousuke/quantum-ecc/.venv/bin/python`.
No machine decided.*

## 1. The 5 SPLIT adjudications — RESOLVED, all five [PROVEN concrete, per-pass isolated]

Reproduction: `o18_clean_splits.py` replays the exact PRNG stream of
`o18_r1_fuzz.mode_eq(seed=1, n=200000)`. Total base-vs-ext disagreements: **392**; the
original run adjudicated the first 60 (`max_adjudicate=60`) — re-adjudication reproduces
**55 clean T_ext-right + the same 5 SPLITs** (dumped to scratchpad
`o18_clean_splits.pkl`). All five are cells of the corrected family (base `T` says HALT
via the unsound j=1 rule — three `r=1` direct, two `r=0` by delegation; `T_ext` says
MOVE via the j=1 dec3 law); the SPLIT verdicts of the original
adjudicator were caused by `probe_cell`'s **run-wide `unsafe` flag under `maxF=24`
free-running** (multi-pass attribution — the continuation, not the pass under test).

Method for the re-adjudication (`probe_isolated`): the concrete run is stopped at the
**first anchored F-entry** (iterating `run_cfg`'s `maxF` upward, skipping interior
entries), so both the outcome and the `unsafe` flag are attributed to the single pass
under test. Each cell probed at the **small-m scan m ∈ {r-adjusted 4..91 step 3}
plus 8 large magnitudes m ≈ 13, 40, 100, 301, 601, 1000, 3001, 10^4** (all ≡ r mod 3).

| # | r | cell w | base T | T_ext | isolated single-pass outcome (all ~34 magnitudes) | verdict |
|---|---|---|---|---|---|---|
| 0 | 1 | `1.2.2.0\|4.2.2.2.1` | HALT | MOVE −23 | = T_ext exactly, unsafe=0 | **T_ext RIGHT** |
| 1 | 0 | `1.2.1.2.2.1.0\|6.2.1.2.2` | HALT | MOVE −3 | = T_ext exactly, unsafe=0 | **T_ext RIGHT** |
| 2 | 1 | `1.2.1.2.2.1.0\|6.2.1.2.2` | HALT | MOVE −23 | = T_ext exactly, unsafe=0 | **T_ext RIGHT** |
| 3 | 0 | `1.2.1.2.2.0\|14.2.2` | HALT | MOVE −3 | = T_ext exactly, unsafe=0 | **T_ext RIGHT** |
| 4 | 1 | `1.2.1.2.2.0\|14.2.2` | HALT | MOVE −23 | = T_ext exactly, unsafe=0 | **T_ext RIGHT** |

(Cases 1/3 are r=0 delegation cells whose inner branch lands in the same corrected j=1
region; their −3 = 6j+8v−2+(−23) is the delegated constant, consistent.)

**No CRITICAL flag: (m mod 3, w)-determinism does NOT break.** Every cell's isolated
single-pass outcome (kind, constant c, exact rewrite w′) is IDENTICAL at every probed
magnitude — small-m scans (every m ≡ r from 4 or 6 up to 91) show **0 deviations**, so
there is not even a small-m template threshold for these five shapes; and none of the
suspected "genuinely magnitude-dependent" behavior exists. The per-pass isolated
`unsafe` flag is 0 at every magnitude: the original `UNSAFE-NONHALT` entries were pure
continuation attribution (the free-running probe's LATER passes halting), exactly the
previously root-caused artifact class.

The adjudication score is therefore **60/60 T_ext right, base wrong** (was 55/60 + 5
unresolved), and the pinning note's suspected root cause (multi-pass attribution) is
CONFIRMED at root-cause level for all five.

## 2. BAD-attractor under T_ext [PROVEN on bounded universe]

`o18_clean_attractor.py`, same 800,712-word universe (len ≤ 5, b ≤ 9, s ≤ 3), same
least-fixpoint computation, transducer parameterized:

- **BAD under T_ext: 65,669 — the set is EXACTLY the base set** (symmetric difference
  0/0, base re-run in-process cross-check also 65,669). Not merely "fewer or equal":
  identical.
- Why removing the false-HALT family changes nothing here: every in-universe word of
  the corrected family (`1 (1,2)(1,2) deep-tail`, base false-HALT at r=1) is still
  genuinely BAD by another route (set-equality makes this exact; mechanism, checked on
  examples: r=2 strips the lead unit to expose the j=0 adjacent-2 word, whose r=1 pass
  is a GENUINE halt — the fatal family — while the corrected r=1 rewrite itself escapes
  the small universe and counts SAFE, under-approx). Conversely T_ext's newly-defined
  cells open no new in-universe forcing path.
- Committed feature statements re-verified on the T_ext set: **0** BAD words without a
  value-2 block; every tail-2-free BAD word has first non-unit block `(3,2)`
  (0 exceptions); exit cone and D-family **0 BAD**.
- Erratum (documentation-level, base analysis): the synthesis note says "the 55 without
  a tail 2"; recomputing with `o18_inv_conemap.py`'s own predicate on its own cached
  status gives **37**, all first-block `(3,2)`. The qualitative claim (and everything
  built on it) is unaffected; the numeral 55 in the note appears to be a transcription
  slip. No conclusion used the numeral.

## 3. Adversarial reachability under T_ext + witness survival

**(a) The 97-pass witness SURVIVES [PROVEN].** (`o18_clean_reach.py` part (i)) All 96
interior edges: `T_ext(r_i, w_i)` == base `T(r_i, w_i)` == the recorded path edge
(96/96, zero differences); the final fatal cell `(r=1, 1¹(1,2)(1,2))` HALTs under BOTH
transducers. The witness never enters the corrected region — consistent with its
edge-by-edge concrete verification (485 probes), which was transducer-independent to
begin with. The invariant-exclusion meta-theorem stands unchanged.

**(b) Reachability census.** Fresh exact closure from the exit cone under T_ext (same
caps len ≤ 14, b ≤ 60, s ≤ 6, budget 3M states): **362 reachable HALT cells**
(base run: 491) and **0 reachable Unknown cells** (base run: 517 — T_ext defines every
cell the adversarial closure meets, as the pinning claimed). Because both closures are
budget-capped and explore different frontiers, the raw 362-vs-491 is only [OBSERVED];
the apples-to-apples census (`o18_clean_reach2.py`, both transducers evaluated on every
one of the ~9.0M visited (r,w) cells of the T_ext closure) is exact:

| (base, ext) on the ext-visited set | cells |
|---|---|
| MOVE, MOVE (identical rewrite) | 8,786,275 |
| LAND, LAND | 204,806 |
| HALT, HALT (genuine fatal) | 357 |
| **HALT, MOVE (base false-HALT)** | **134** |
| UNK, MOVE (newly defined) | 512 |
| UNK, HALT (newly defined, fatal) | 5 |
| any other disagreement | **0** |

All 134 false-HALT cells trace into the corrected j=1 dec3 region and nothing else:
59 direct `r=1` cells `1¹(1,2)(1,2)+deep tail`, 62 `r=0` delegation cells whose
delegated suffix is that family, 13 `r=1` cells whose R1 inner recursion descends into
it (verified exhaustively on the census dump). Genuine fatal cells remain adversarially
reachable (357 + 5 = 362 ≥ 1), so §3 of the synthesis note is intact.

## 4. Does any conclusion of O18_INVARIANT_SYNTHESIS change? — NO (qualitatively none)

| conclusion | status after T_ext re-run |
|---|---|
| 97-pass witness + invariant-exclusion meta-theorem | UNCHANGED (witness path outside the corrected region; concrete anyway) |
| BAD attractor 65,669 + "every BAD word has a 2" + cone/D 0-BAD | UNCHANGED (set-identical; the "CONSERVATIVE/upper bound" caveat of the pinning audit can be retired: the bound is exact) |
| fatal cells adversarially reachable | UNCHANGED (362 cells under T_ext; the count is now also free of false-HALTs) |
| "no residue-oblivious invariant of any class" | UNCHANGED |
| 517 reachable Unknown cells must be pinned | DISCHARGED: 0 Unknown cells reachable under T_ext |
| the 55 numeral (§2 of the synthesis note) | erratum: 37 by the script's own predicate; qualitative claim intact |

## 5. Soundness ledger
- The 5 SPLIT resolutions rest on per-pass-isolated fresh concrete simulation at ≥30
  magnitudes per cell (small-m scan + up to m ≈ 10⁴), not on either transducer.
- (m mod 3, w)-determinism: INTACT — the five cells were the only candidate violations
  on record, and each is now deterministic at every probed magnitude (4 ≤ m ≤ ~10⁴,
  ~34 values/cell). Status of the law is unchanged: [PROVEN on grid] where fired,
  [OBSERVED/exact-fit] beyond.
- NOT claimed: anything about the o18/o15 orbits' actual halting; the attractor/reach
  results are bounded-universe/budget-capped statements as before, labels unchanged.
- The base transducer `o18_md_rules.py` remains unsound at the j=1 deep-tail family
  (false-HALT direction only) — use `o18_md_rules_ext.py` for anything touching that
  region; base file intentionally untouched.

o15/o18 stay `[OPEN]`. **No machine decided. No label upgraded.**

## Reproduce
`o18_clean_splits.py` (SPLIT reproduction + isolated deep probes; dumps
`o18_clean_splits.pkl`, `o18_clean_split_verdicts.pkl`) · `o18_clean_attractor.py`
(T_ext BAD attractor + base cross-check + diff) · `o18_clean_reach.py` (witness edge
check + T_ext closure) · `o18_clean_reach2.py` (single-visited-set base-vs-ext census;
dumps `o18_clean_reach2_census.pkl`). Basis: `O18_R1_PINNING_2026-07-08.md`,
`O18_INVARIANT_SYNTHESIS_2026-07-07.md`.

**No machine decided. No label upgraded.**
