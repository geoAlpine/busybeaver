# ×3/2-family census cleanup: o2's complete ceiling chain (the second Antihydra, link by link) + o7's v₂ deepening (2026-07-08)

*Two cleanup items from `X32_FAMILY_REDUCTIONS_2026-07-07.md`. Model: Antihydra's 5-link chain
(`BB6_FRAMEWORK_PACKAGE.md` §2) and the criticality framework (`ANTIHYDRA_LEDGER_UNIFICATION_2026-07-07.md`).
SOUNDNESS: every claim labeled `[PROVEN]` / `[PROVEN given the automaton]` / `[OBSERVED]` / `[OPEN]`; all
numerics exact big-int (`/Users/aokiyousuke/quantum-ecc/.venv/bin/python`); scripts `x32c_o2_chain.py`,
`x32c_o7_v2.py`. **No machine decided.***

## 0. Headline

| item | result |
|---|---|
| **o2 chain** | complete, Antihydra-parallel: Link 0 `[OBSERVED, 0-mismatch: 17 raw transitions + 320 seeded configs]` → Link 1 conjugacy `[PROVEN given automaton]`, **manifold `3\|a+4` now shown universal (reached in one step from any milestone)** → Link 2 halt/escape `[PROVEN given automaton; seeded raw incl. a=2405]` → Link 3: **exact ceiling gap lemma** `T̂(o)=3^{G−1}(3o+1)/2^G, G=v₂(3o+1)` — the literal mirror of Antihydra's — and the run form **halt ⟺ ∃i: s_i ≥ B_i+2** (Antihydra: ≥ B_i+1). Needed statement is **strictly weaker than ceiling-(K)**; the (K)-shaped link stays `[OPEN]` |
| **o2 criticality** | ratio = log₂(3/2)/(1/2) = **1.169925 — Antihydra's number exactly**; fixed points x=−r (0,−1), run laws e=v₂(y), s=v₂(y+1) `[PROVEN given automaton + exhaustive]`; η = 1/φ. **o2 is a second critical rung**, ε more protected (hatch = +1 run unit and a measure-½ filter per zero-touch, not a slope change) |
| **o7 census** | 10⁶ milestones / 250,007 cascade entries: v₂(a+3) geometric `2^{−d}` through d=22; **max depth 22** (needs ~201,232 at the frontier); **odd part never shrinks toward 1** — min oddpart(a+3)=7 (at n=2, never again); per-decade min bitlen(oddpart): 3, 10, 109, 822, 8136, 80671 (linear growth ≈ 0.2012 bits/milestone) |
| **o7 v₂ dynamics** | inside cascades: **deterministic countdown, closed form run = v₂(a+3)** (fixed point −3, ratio 1/2 — the family fixed-point law); across cascades: **no autonomous law** — d′ = v₂(3^e x₁/2^e − 1) depends on the full value (odd part + ledger), not on d `[PROVEN given automaton; 0 violations over 250,006 cascades]`; statistically P(d′\|d) ≈ geometric, independent of d `[OBSERVED]` |

---

## 1. TASK A — o2's complete ceiling chain

`o2 = 1RB1RC_1LC1LE_1RA1RD_0RF0RE_1LA0LB_---1RA` (halt `F,0`; unique edge into F: `D,0→0RF`).

### 1.0 Link 0 — machine ⟺ milestone automaton `[OBSERVED, 0-mismatch]`

**Milestone (precise machine-level configuration).** State **A**, head on a `0` cell, **every cell strictly
left of the head blank**, and the tape from the head rightward reads
> `0 · 11 · (01)^a · 0 · 11 · (01)^b · 0^∞`   (head on the leading 0) — the config `D(a,b)`.

**Automaton** (as in `CRYPTID_SLOWWIDTH_2026-07-04.md` §1):
```
a even:        D(a,b) -> D((3a+4)/2, b+2)
a odd, b>=1:   D(a,b) -> D((3a+7)/2, b-1)
a odd, b=0:    -> S(N), N=(3a+11)/2;  S(N) -> HALT if N odd, else D((3N-2)/2, 1)
               (composed: D(a,0) -> HALT if a≡1 mod 4, else D((9a+29)/4, 1))
```
**Verification (re-done and extended here).** Blank tape, **10⁸ raw steps**: 18 parsed A-milestones
(`(2,1)@44 → (5,3)@124 → (11,2) → (20,1) → … → (7187,11)@62,095,432`), 2 unparsed A-frontier events (startup
transients before step 44); **17 consecutive raw transitions, 0 mismatches** (extends the banked 15). Seeded
grid: **320 one-step raw-TM configs** (a=1..80 × b=0..3, all four branches), **0 mismatches** (extends the
banked 103). *Honest label:* this is 0-mismatch finite verification, **not a certified tape-shape induction**
— o2's Link 0 stays `[OBSERVED]` where Antihydra's is `[PROVEN, machine-verified]`. This is the single
foundational gap of the whole chain; everything below is `[PROVEN given the automaton]`.

### 1.1 Link 1 — D(a,b) ⟺ (y,b), y=(a+4)/3 `[PROVEN given the automaton]`

Exact algebra (grid-confirmed to a < 3·10⁵, 0 violations of any clause):
- **Universal manifold (new).** `3 | a+4` is reached from **any** milestone in **one** step:
  `a'+4 = 3(a+4)/2` (even branch), `3(a+5)/2` (odd branch), `9(a+5)/4` (escape) — each divisible by 3.
  So the conjugacy is not a blank-orbit accident; every seeded orbit lands on the manifold immediately.
- On the manifold `y := (a+4)/3` obeys **`y' = ⌈3y/2⌉` exactly on both D-branches**; parity `a even ⟺ y even`;
  blank seed `D(2,1) ⟹ y₀ = 2` (ladder `2,3,5,8,12,18,27,41,62,…`).
- Escape branch = **exactly two ceiling steps through an even midpoint**: `y ≡ 1 (mod 4) ⟹ (3y+1)/2 even ⟹
  y'' = (9y+3)/4`, machine-level `a' = (9a+29)/4`.
- Halt-case identities: `N=(3a+11)/2` odd `⟺ a ≡ 1 (mod 4) ⟺ y ≡ 3 (mod 4)`.

### 1.2 Link 2 — halt ⟺ b=0 at y ≡ 3 (mod 4); the escape hatch `[PROVEN given the automaton; seeded-verified incl. LARGE y]`

> **o2 HALTS ⟺ some milestone has `b = 0` with `y` odd and `y ≡ 3 (mod 4)`** (`⟺ a ≡ 1 mod 4`).
> At `b = 0, y ≡ 1 (mod 4)` the machine **escapes** to `D((9a+29)/4, 1)` — ledger reseeded at 1.

Seeded raw-TM verification (this session): **all odd a in 1..201** with b=0 (101 seeds) **plus large seeds
a = 401, 403, 801, 803, 1199, 1205, 2399, 2405** (up to 28.3M raw steps each): halt exactly for `a ≡ 1 (mod 4)`
(56/56 incl. a=2405), and every escape lands **exactly** at `D((9a+29)/4, 1)` (53/53 incl.
`a=2399 → D(5405,1)`). 0 mismatches. The escape mechanism is not a small-y artifact.

### 1.3 Link 3 — the exact Birkhoff form, and how much weaker than ceiling-(K) it is

**Ledger identity `[PROVEN given the automaton]`:** `Δb = +2` (y even) / `−1` (y odd) ⟹ between escapes
> `b_n = 1 + 3E_n − n` — with `β_n := 3E_n − n` this is **Antihydra's balance functional verbatim, at the
> same underflow level** (fatal touch is `β = −1`; Antihydra halts at `B = −1`). o2 adds the mod-4 filter.

**Ceiling GAP LEMMA (new, the mirror of Antihydra Link 1) `[PROVEN given the automaton]`:** for odd `o` on the
ceiling orbit, the next odd value is
> **`T̂(o) = 3^{G−1}(3o+1)/2^G`, `G = G(o) = v₂(3o+1)`, reached in exactly G steps; `G ≥ 2 ⟺ o ≡ 1 (mod 4)`**
— literally Antihydra's `T(o) = 3^{D−1}(3o−1)/2^D, D = v₂(3o−1)` with the sign mirrored. Seed `o₀ = 3`
(from `2→3`). Verified 10⁵ induced returns, 0 mismatches (formula, step count, and the mod-4 equivalence).

**Kac/renewal (mirror of Link 2–3) `[PROVEN, elementary]`:** even-density ≥ 1/3 ⟺ `mean G ≥ 3/2`; truncating
at level 2: `mean G ≥ 1 + freq(o ≡ 1 mod 4)`, so **`liminf freq(o ≡ 1 mod 4) ≥ 1/2` is sufficient** — the
exact one-sided cylinder form of Antihydra's `(K)`, on the ceiling side, seed 3. Orbit values: `mean G =
2.00250`, `freq(o≡1 mod 4) = 0.50201` at 10⁵ returns (Haar: 2 and 1/2) `[OBSERVED]`.

**Run form with the mod-4 refinement `[PROVEN given the automaton]`.** With run laws `e_i = v₂(y)` /
`s_i = v₂(y+1)` at entries (§1.4) and `B_i = 1 + 2Σ_{j≤i}e_j − Σ_{j<i}s_j` the ledger at odd-run entry
(verified: **0 violations over 249,810 odd runs** to n=10⁶):
> **halt ⟺ ∃i: `s_i ≥ B_i + 2`; escape ⟺ `s_i = B_i + 1` exactly.**
Reason: inside an odd run the b=0 touch at position `B_i` has `v₂(y+1) = s_i − B_i`; the fatal condition
`y ≡ 3 (mod 4)` means `v₂(y+1) ≥ 2`, while `s_i = B_i + 1` puts the touch on the run's **last** odd milestone
(`y ≡ 1 mod 4`) — the hatch. **Antihydra's form is `s_i ≥ B_i + 1`: o2's needed avoidance is exactly one unit
weaker per run**, and a boundary hit additionally reseeds the epoch (`b = 1`).

**Is the needed statement weaker than full ceiling-(K)? YES, strictly, in two senses:**
1. **Logical:** ceiling-(K) (`liminf E_n/n ≥ 1/3` for the seed-2 orbit) + the finite check ⟹ non-halt, but
   **not conversely**: o2's halt event is a strict sub-event of ledger underflow (a touch at `y ≡ 1 mod 4`
   survives and resets), so **non-halt is NOT equivalent to any Birkhoff-average statement** — Antihydra's
   two-sided Link 0 (`non-halt ⟺ density ≥ 1/3`, sharp) has **no o2 analogue**. o2's exact criterion is the
   epoch/hatch statement above.
2. **Quantitative (the hatch's value):** on the **real orbit** the hatch is *unexercised* — **0 zero-touches
   in 10⁶ ceiling steps** (min b at odd-y milestones = **2** at n=2; global min b = 1 ⟺ worst prefix
   `3E_n − n = 0` at n=3, an even-y milestone). On a **seeded ensemble** (a odd 1..1999 × b∈{0,1,2}: 3,000
   orbits, 2,484 zero-touches): **escape fraction 0.5004 ≈ 1/2** `[OBSERVED]` — each zero-touch is fatal only
   half the time, and every escape restarts the ledger at 1.

**Chain summary:** non-halt ⟸ `3E_n − n ≥ 0 ∀n` ⟸ ceiling-(K) seed 2 + finite check (verified n ≤ 10⁶,
even-density 0.500767, b = 502,302 at 10⁶) — **the one unproven link is exactly the (K)-shaped one `[OPEN]`.**

### 1.4 Criticality row `[criterion of ANTIHYDRA_LEDGER_UNIFICATION §2]`

**Branch fixed points (ceiling instance of the family fixed-point theorem).** Branches `y ↦ (3y+r)/2`,
`r = y mod 2`; fixed points solve `2x = 3x + r` ⟹ **`x = −r`: x_even = 0, x_odd = −1** — the literal mirror
of the floor case's `x = +r` (0, 1). On each branch `y' − x = (3/2)(y − x)` exactly, hence
> **even-run = v₂(y), odd-run = v₂(y+1), exactly** `[PROVEN given the automaton]` — verified exhaustively
> y = 1..2·10⁵ (0 mismatches) and on all 499,621 completed runs of the real orbit to n = 10⁶ (0 mismatches).

**Magnitude cap `[PROVEN given the automaton]`:** `2(3/2)ⁿ ≤ y_n ≤ 3(3/2)ⁿ − 1` (2-line induction; checked
exactly to n = 2000), hence drain-run cap `s = v₂(y_n+1) ≤ n·log₂(3/2) + log₂ 3`.

| machine | depth process | run-cap slope ρ | budget slope β (memory class) | **ρ/β** | verdict |
|---|---|---|---|---|---|
| Antihydra | v₂ under ⌊3c/2⌋, seed 8 | log₂(3/2)=0.58496 `[PROVEN]` | 1/2 cumulative `[OBS=annealed]` | **1.1699** | > 1: not excluded — critical |
| **o2** | v₂ under ⌈3y/2⌉, seed 2 | log₂(3/2)=0.58496 `[PROVEN\|automaton]` | 1/2 cumulative `[OBS=annealed]` (0.5023 at 10⁶) | **1.1699** | **> 1: not excluded — a SECOND critical rung** |

Same increments `{+2, −1}` ⟹ same ruin base `η = (√5−1)/2 = 1/φ` (η³−2η+1 = (η−1)(η²+η−1)). **The mod-4
hatch does not move the slopes**: it raises the single-run fatality bar by exactly one unit (`B+2` vs `B+1`)
and filters each zero-touch by measure ½ — an O(1)/probabilistic refinement at the same critical point 1.1699.
Orbit margins `[OBSERVED, n ≤ 10⁶]`: max even run = max odd run = **17** (≈ log₂ n; a fatal run needs
≈ 0.5n); worst single-run drain `s/B(entry) = 0.667` at n=1 (fatality needs > 1); runs 249,811/249,810 with
means 2.0046/1.9984 (Haar 2). The depth-side truth beats the needed bound by the same ~10³–10⁴ factor as
Antihydra; the proven-vs-needed gap is the bare 1.17 on both.

### 1.5 What remains OPEN for o2, honestly, vs Antihydra

- **Weaker foundation (o2 worse):** Link 0 is `[OBSERVED]` (17 raw transitions + 320 seeded configs,
  0-mismatch), not a certified induction. Antihydra's Link 0 is `[PROVEN, machine-verified]`. Every o2
  theorem above carries the "given the automaton" caveat; closing this needs a tape-level induction on the
  four branch trajectories (routine-looking but NOT done).
- **Weaker needed target (o2 better):** the open kernel is **at most** ceiling-(K) seed 2 — per-run
  threshold one unit slacker, boundary touches survivable with ledger reset (½ of touches on the ensemble),
  and the real orbit never touched in 10⁶ steps. Any proof of ceiling-(K) decides o2 (with the finite check);
  something strictly weaker could too.
- **Lost equivalence (o2 murkier):** there is **no** density statement equivalent to o2's non-halting; the
  exact criterion is the epoch/hatch statement. o2's halting problem is thus not literally "a (K)-instance"
  — it is *protected by* one.
- **Same criticality, thinner early margin:** ratio 1.1699 on both; both axes (depth and frequency) open,
  like Antihydra and unlike o4/o3. o2's worst prefix is **0** (b touched 1 at n=3) vs Antihydra's min balance
  +2 — the ceiling orbit starts even closer to the boundary, then drifts at the same +1/2.
- **Meta-theorems NOT ported:** the No-Structure-Only-Selection / shared-free-structure theorems were proven
  for the floor induced map `T`. The ceiling mirror `T̂` has the analogous violating atom at the odd-branch
  fixed point `o = −1 ∈ ℤ₂` (`G(−1) = 1`, all-drain), so the port looks mechanical, but it is **not verified
  here** — o2's (K)-hardness is inherited morally, `[OPEN]` formally.

---

## 2. TASK B — o7's thin-set census deepened: `a+3 = 2^k`

`o7 = 1RB0RB_1LC1RE_1LF0LD_1RA1LD_1RC1RB_---1LC`; automaton and halt criterion
(`HALT ⟺ some milestone has oddpart(a+3) = 1`) as verified in `x32_o7_reduction.py`.

### 2.1 v₂ / odd-part census, 10⁶ milestones `[OBSERVED]` (`x32c_o7_v2.py`)

Blank orbit from `(2,2)`: **250,007 cascade entries** (matches the 500k-milestone bank exactly at half-time:
124,914 entries, 100,649 bits). Value growth **0.2012 bits/milestone** (linear).
- **Depth distribution:** `v₂(a+3)` at entries matches geometric `2^{−d}` over the whole observed range —
  freq 0.50067 / 0.24999 / 0.12506 / 0.06237 / … through d = 16, tail counts consistent to d = 22.
- **Max depth = 22** at n = 425,280 (where `u = a+3` had 85,649 bits). Fatality needs `d = bitlen(u) − 1`
  ≈ **201,232 at the frontier** — the observed record is ~log₂ n; the requirement grows linearly.
- **The odd part never shrinks toward 1:** min `oddpart(a+3)` at entries = **7, at n = 2, never again**;
  min `bitlen(oddpart)` per entry-decade: **3, 10, 109, 822, 8,136, 80,671** — monotone linear growth
  (`bitlen(w) = bitlen(u) − d` with d = O(log n)). The `a=3` branch (oddpart = 3): **0 hits** in 10⁶.

### 2.2 The exact dynamics ON v₂ `[PROVEN given the automaton; 0 violations on 250,006 cascades]`

With `u = a+3`, `d = v₂(u)`, `w = oddpart(u)`, `B` = b at entry:
- **(D1) Inside a cascade — closed-form countdown.** The odd branch `a ↦ (a−3)/2` has fixed point
  `x = −3` (`2x = x − 3`), contraction **1/2**: `u' = u/2` exactly, v₂ counts down 1 per milestone.
  **Run length = v₂(a+3)** — the family fixed-point run law (o4's Theorem-2 mechanism at ratio 1/2).
  Verified exhaustively (odd a < 2·10⁵) and on the orbit.
- **(D2) Exit ledger:** the cascade exits after d milestones at `a_exit = w − 3` with
  **`b_exit = B + (u − w) + d`** (odd step k adds `u/2^{k+1} + 1`). 0 violations, 250,006 cascades.
- **(D3) Re-entry seed:** first even step gives `a₁ = 3(w−3)/2 + 1 + b_exit`, i.e. `u₁ = (3w−1)/2 + b_exit`.
  0 violations.
- **(D4) Even chains** (b = 1 after every even step): fixed point `x = −4` (`2x = 3x + 4`), ratio **3/2**:
  `a+4 ↦ 3(a+4)/2` exactly, chain length = `v₂(a+4)`. Verified exhaustively.
- **(D5) The entry-to-entry v₂ map:** `d' = v₂(3^e·x₁/2^e − 1)` with `x₁ = u₁ + 1`, `e = v₂(x₁)` —
  **a function of the full value `(w, b_exit)`, NOT of d**. So v₂ is a deterministic countdown *inside* runs
  (closed form) but has **no autonomous entry-to-entry law** — the same counter-dependence that blocked a
  finite shortcut for o4's odometer. It is Markov-ish only statistically: `P(d'=1|d) = 0.500 ± 0.01` and
  `P(d'=2|d) = 0.25 ± 0.02` for every d = 1..8+ `[OBSERVED]`, i.e. entry depths look i.i.d.-geometric.
- **What fatality requires:** `u = 2^k ⟺ w = 1 ⟺ d = bitlen(u) − 1` — the odd part must collapse to 1
  **simultaneously** with a full-depth 2-adic return. These are one condition, not two: a single geometric-tail
  event of depth ≈ 0.2n bits at milestone n (observed ceiling: 22). There is no gradual approach: w is
  regenerated at every entry from (D3) and its bit-length tracks `bitlen(u) − O(log n)`.

### 2.3 o7's protection, stated in family terms + classification

> **SPECIES: thin-set hitting (B2 reachability).** The fatal set `{u = 2^k}` is **one point per dyadic
> scale** — relative measure `2^{−bitlen(u)} → 0`, a measure-zero target in the 2-adic limit — hit by
> reachability, not by an accumulated density. **MARGIN: the odd-part growth rate.** `bitlen(oddpart(a+3))`
> grows linearly (≈ 0.2012 bits/milestone, decade minima 3 → 80,671) while observed depths stay O(log n);
> annealed heuristic `Σ_n 2^{−0.2n} < ∞` (Borel–Cantelli: the annealed model survives outright — contrast
> Antihydra/o2, whose annealed ruin is a delicate `η^B`). **LEDGER-MEMORY: NO scalar ledger** — b is never
> tested by any branch; the implicit string object (the 2-adic tail of `a+3`) is **recomputed from the value
> at every entry — RESET-per-entry**, kin to o15's cylinder species, not to Antihydra/o2's cumulative
> balance. The criticality criterion (run-cap/budget) is **inapplicable**: there is no scalar budget to
> exhaust. o7 is not on the Antihydra ladder at all; its wall is generalized-Collatz reachability `[OPEN]`.

---

## 3. Soundness ledger

- Labels: milestone automata `[OBSERVED, 0-mismatch]` (o2: 17 raw + 320 seeded; o7: banked 29 raw + 124
  seeded, reused); all algebra downstream `[PROVEN given the automaton]` with 0 numeric violations on every
  identity (conjugacy grid 3·10⁵; run laws exhaustive 2·10⁵ + 499,621 orbit runs; gap lemma 10⁵ returns;
  o7 formulas (D1)–(D4) exhaustive 2·10⁵ + 250,006 cascades); densities, margins, escape fraction, depth
  histograms `[OBSERVED]`; ruin/Borel–Cantelli numbers heuristic only.
- Nothing here strengthens any non-halting claim: o2's chain terminates in the `[OPEN]` ceiling-(K) link;
  o7's terminates in the `[OPEN]` avoidance wall. No banked note contradicted; the 500k o7 numbers and the
  o2 ladder/margins reproduce the 2026-07-07 bank exactly.
- Nothing committed.

## Reproduce (interpreter `/Users/aokiyousuke/quantum-ecc/.venv/bin/python`)

- `x32c_o2_chain.py [raw=10⁸] [orbit=10⁶]` — Link 0 blank (10⁸ steps, 17 transitions) + seeded 320; Link 1
  grid; Link 2 seeded b=0 incl. large a=2399/2405; Link 3 orbit census (10⁶ ceiling steps: runs, ledger
  identity, drains, zero-touches) + induced odd map (10⁵ returns) + ensemble escape fraction (3,000 orbits);
  run laws exhaustive; magnitude bounds; criticality row. (~4 min)
- `x32c_o7_v2.py [milestones=10⁶]` — (D1)/(D4) exhaustive; 10⁶-milestone census with (D2)/(D3) verified per
  cascade, depth histogram vs 2^{−d}, per-decade odd-part minima, entry-to-entry transition matrix. (~3 min)

**Halting stays `[OPEN]` for o2 and o7. No machine decided. No label upgraded.**
