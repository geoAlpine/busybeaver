# RF-4 — turn 相に閉形式があった(2026-07-27)

> **本文(§0〜§6)は右向き turn を閉じた時点の記録。末尾の追記(§7〜§10)で左向きも閉じ、
> epoch の 13 部分中 10 個・99.959% が証明済み `∀` 法則で覆われた。数字は追記が正。
> §10 に「これが証明していないこと」を明記してある — 読み違えないこと。**

`RESUME_2026-07-26.md` §3 の残り1項目、D のクリティカルパス上の **RF-4** に着手した結果。

`lake build` EXIT=0(コーパス 83 jobs)、公理は `[propext, Quot.sound]` のみ、`sorryAx` 0、
`Classical.choice` 0。

**D は依然 `[OPEN]`。本文書は機械を決着させず、ラベルを昇格させない。**

---

## §0 訂正 — RF-4 の従来の読みは悲観的すぎた

`D_SPEC_2026-07-26.md` §5 は turn 相を「`≈2 steps/cell`、加法定数は `k` だけの閉形式ではない」、
§8 項目3は「各々が別個の小補題。**閉形式なし**」と記録していた。

**実測はそうではなかった。** 右向きの turn 相は **rung タイルそのもの**で、復路掃引が着地パッドに
届く前に `(1 0)^w` の櫛を横切るだけである。定数は自由パラメータではなく **`6(u+m)+15`** —
すなわち**局所的な rung 形状の関数**であり、だからこそ `k` の関数には見えなかった。

---

## §1 どう見つけたか(rung タイルと同じ手順)

`d_rf4_turns.py`:epoch を **rung タイルの発火で分節**し、その間の状態列を全部ダンプする。
`d_rung_atoms.py` が rung に対してやったことを turn 相に対してやっただけである。

epoch `M1(4) → M1(5)`(905,244 歩)は 6 ladder + 7 turn に分解され、右向き turn の状態列は:

```
(ABED)^{u+1} · A · (ABED)^{m} · A · (BC) · (BE)^{m} · (BC)^{u+2} · (BE)^{w} · C · D
                                                                   ^^^^^^^^^
                                                    rung との差はこの1本だけ
```

原子は rung とまったく同じ。テープを見れば理由も明らかで、rung は右文脈 `1 0^g`(`g ≥ 3`)を
要求するが、turn は**ギャップが枯れた瞬間**(`g = 0`)に起き、復路掃引がパッドではなく櫛に
出会う。櫛を `w` 個の `swap10` 原子で渡り、その先のパッドで turn する。

## §2 法則(`IN` の literal-`m` 規約)

```
IN2(u,m,c,w,g) := ⟨A, p, ⟨(0 1)^u 1 1 (1 0)^m 0 0 1^c TAIL | 0 | 1 (1 0)^w 0^g REST⟩⟩

steps (6(u+m)+15 + 2w) IN2(u,m,c,w,g) = IN(0, w−1, 1, g−3)   at pos + 3 + 2w
```

**出力自身が `IN` 族の要素**(`u'=0, m'=w−1, c'=1`)なので、turn 相はそのまま次の ladder に
繋がる — epoch がやっているのは正にこれである。

**`w = 0` は rung タイルそのもの。** つまり2つの法則ではなく **1つの法則**である。

## §3 検証

| 検査 | 結果 |
|---|---|
| 実軌道 spot check `t=291698`(`u=9, m=2, w=66`) | 予測 `6·11+15+2·66 = 213` / **実測 213** |
| 実軌道 spot check `t=310271`(`u=72, m=29, w=309`) | 予測 `6·101+15+2·309 = 1239` / **実測 1239** |
| 次 ladder の開始形 | 両方 `IN(0, w−1, 1, ·)` — 予測どおり |
| 網羅掃引(`d_rf4_law.py §2`) | `u≤4 × 1≤m≤4 × 0≤c≤3 × 1≤w≤5 × 3≤g≤6 × 9 TAIL × 8 REST` = **115200/115200** |
| `w=0` が rung タイルに一致(§3) | **144/144** |
| 出力が `IN(0,w−1,1,g−3)`(§4) | **540/540** |
| negative control(`g=2`, span±1) | いずれも要求どおり失敗 |

## §4 Lean

`lean/RungCalc.lean`:

* `rung_prefix` — 相 1〜7 を切り出した(右文脈を一度も読まないので `Rt` は完全に任意)。
  これが rung タイルと turn 相で**共有**される部分であり、9相合成の重複は無い。
* `rung_core2` / `IN2` / `tile2` — turn 相、`Atoms` を満たす**任意の機械**に対して `[PROVEN]`。
* `turn_out_word` — 出力語を `IN` 語として読み直す語恒等式(`pow10_shift` 経由)。
* `markerFold` — `marker` の fold(`(A)^n` = 左向きに `1`-run を渡って消す)。**左向き turn の
  主要原語**。fold は証明済みだが、左向き turn 相の組立ては未了(§6)。

`lean/DMachine.lean`:

* `turnPhase` — D の turn 相、`∀ u m c w g p TAIL REST`。
* `turn_291698_via_law` と `turn_291698_kernel` — **同一命題を2通りに証明**(法則からの特化と、
  kernel が `dT` を 213 歩実行)。
* `turn_310271_via_law` — 1239 歩の方。
* `tile_is_turn_at_w_zero` — `IN2 … 0 … = IN …`(定義的等号)。

`lean/HMachine.lean` は `hAtoms` から同じものを**無料で**継承(`turnPhase` + kernel grounded)。

## §5 正直な会計 — epoch のどれだけが `∀` 法則で覆われたか

epoch `M1(4) → M1(5)` = 905,244 歩:

| 部分 | 歩数 | 割合 | 状況 |
|---|---:|---:|---|
| ladder 6 本(rung タイル) | 895,545 | 98.93% | **[PROVEN]** |
| turn 7 本 | 9,699 | 1.07% | |
| ├ 右向き 2 本(`tile2`) | 1,452 | 0.16% | **[PROVEN]** |
| └ 残り 5 本 | 8,247 | 0.91% | **[OPEN]** |
| **合計 覆われた** | **896,997** | **99.09%** | |

## §6 残っているもの(正直に)

**覆われていない 5 本の turn 相:**

| t | 歩数 | 状態列の特徴 | 診断 |
|---|---:|---|---|
| 291168 | 26 | `(AB)(CB)(C)(D)(ABED)²…` | entry 直後の短い混合相 |
| 291254 | 180 | **`(DF)^8` を含む** — 状態 `F` に到達 | `F` は他のどの相にも現れない。別原語 |
| 1168982 | 6504 | `(ABED)^617 (A)(AB)(CB)^619(C)(D) (ABED)^620 (A)^{~900}` | **左向き**。`markerFold` + rung(`m=0`)の合成 |
| 1194806 | 1371 | 同型、`(A)^66` | **左向き**、同上 |
| 1196246 | 166 | 2つの部分 turn が連続 | 混合 |

左向き turn の**原語は同定でき `markerFold` として証明済み**だが、その**組立ては未了**である。
状態列は `(ABED)^p A (AB)(CB)^{p+1} (C)(D)` +『`(ABED)^q` `(A)^r` `(ABED)`』の形で、前半は
`m=0` の rung、後半は crawl + markerFold + crawl に見える。ただし **[未検証]** — 測定して
から書くのが本プロジェクトの規律であり、まだ測っていない。

**RF-4 は半分閉じた。** 「`k+1` 個の定数を列挙する」問題ではなくなり、「右向きは1法則、
左向きは原語同定済み・組立て未了」になった。`(DF)^8` の相は独立した第3の原語を要する。

**D を閉じるまでに残るもの**(本文書で縮んだのは 1 だけ):

1. ~~RF-4 の turn 相~~ → 右向き `[PROVEN]`、左向き組立て `[OPEN]`、`F` 相 `[OPEN]`
2. **RF-5** 偶 `k` の shifted `S1` セグメント 3 本 — 第2タイル、内部歩数則未導出
3. cascade level の内部帰納(epoch は `k+1` 相)
4. entry セグメント blank → `M1(4)` = 291,168 歩
5. `TapeCalc.nonhalt_of_invariant` による組立て
6. milestone 族 `k=4..9` のみ pin、`k ≥ 10` 未実測

---

*計器(repo root, committed): `d_rf4_turns.py`(epoch 分節と turn 相の状態列)、
`d_rf4_law.py`(法則の網羅検証・`w=0` 特殊化・出力の `IN` 性・negative control)。
Lean: `lean/RungCalc.lean`、`lean/DMachine.lean`、`lean/HMachine.lean`。*

**No machine decided by this document. No label upgraded. `D` remains `[OPEN]`.**

---

# 追記 — 左向き turn も2法則に分解できた(同日)

## §7 左向き turn = `rung0` + `descend`

`d_rf4_left.py` が未カバーの5相をダンプ。左向きの大きい2本は、head 軌跡が分割を裏付ける
(前半は `+3` 進む = rung、後半は純粋な下降):

```
t=1194806 (1371歩): (ABED)^128 A A (BC)^130 D  ++  (ABED)^131 (A)^66 (ABED)
                    \___ rung0, u=127, 777歩 __/    \_ descend, q=130, r=65, 594歩 _/
t=1168982 (6504歩): 同型、u=616 (3711歩) ++ q=619, r=308 (2793歩)
```

**`rung0` = 櫛が枯れた rung(`m_literal = 0`)。** `marker` と `turnaround` の間の crawl run が
空になる場合で、これは `IN` 族が除外している境界(`IN` は `m ≥ 1` を要求)そのものである。
span `cr(u+1) + mk + ta + s01(u+3) + tu` = `6u+15`、head `+3`(タイルと同じ)。
**すべての ladder セグメントはここで終わる。**

**`descend` = `crawlFold q ; crawl ; markerFold r ; crawl`。** `(1 0)^q` の櫛を左へ渡り、`1`-run
に乗って `markerFold` で消し、もう1回 crawl する。span `cr(q+2) + mk(r+1)`、
head `−2(q+1) − (r+1) − 2`。

**新しい原子は1つも要らなかった** — 4つの fold の組み合わせだけである。

## §8 検証

| 検査 | 結果 |
|---|---|
| `rung0` 網羅(`u≤5 × c≤3 × 3≤g≤6 × 8 TAIL × 8 REST`) | **6144/6144** |
| `descend` 網羅(`q≤5 × r≤5 × 8 TAIL × 8 REST`) | **2304/2304** |
| 実軌道 budget `t=1194806` | `(6·127+15) + (4·132+66) = 777+594 = 1371` ✓ 実測 1371 |
| 実軌道 budget `t=1168982` | `(6·616+15) + (4·621+309) = 3711+2793 = 6504` ✓ 実測 6504 |

Lean: `RungCalc.rung0` / `RungCalc.descend`(`Atoms` を満たす任意の機械)、
`DMachine.combExhausted` / `descent`、それぞれ kernel `rfl` の grounded インスタンス付き
(`descent_grounded` は rfl と法則の二重証明)、および上の budget 恒等式2本を `rfl` で。

## §9 epoch のカバレッジ(更新)

epoch `M1(4) → M1(5)` = 905,244 歩、13 個の部分:

| 部分 | 個数 | 歩数 | 割合 | 法則 |
|---|---:|---:|---:|---|
| ladder | 6 | 895,545 | 98.929% | `tile` |
| 右向き turn | 2 | 1,452 | 0.160% | `tile2` |
| 左向き turn | 2 | 7,875 | 0.870% | `rung0` + `descend` |
| **計** | **10 / 13** | **904,872** | **99.959%** | **すべて `[PROVEN]`** |
| 残り(短相3本) | 3 | 372 | 0.041% | `[OPEN]` |

残る3本:`t=291168`(26歩)、`t=291254`(180歩、**`(DF)^8` で状態 `F` に到達** — 他のどの相にも
現れない第3の原語)、`t=1196246`(166歩、2つの部分 turn の連続)。

## §10 これが**証明していない**こと(重要)

上の 99.959% は「**epoch のこれだけの歩数が、証明済み `∀` 法則のインスタンスである**」という
測定であって、**epoch 全体が証明されたという意味では断じてない**。

具体的に不足しているもの:

1. **継ぎ目の構成。** 各法則の出力配置が次の法則の入力配置と*実際に一致する*ことは、
   軌道上のテープ(= D の cascade 語)を知らないと言えない。budget 恒等式が示すのは
   **歩数の辻褄**だけである。これが「cascade level の内部帰納」項目であり、未着手。
2. 残る3相(うち1本は未知の第3原語を要する)。
3. **RF-5**、偶 `k` の shifted `S1` セグメント3本。
4. entry セグメント(291,168 歩)、milestone 族の `k ≥ 10`、最終組立て。

**RF-4 自体は実質的に閉じた**(短相3本を除く)。しかし D を閉じるまでの道は、上の 1 が示すとおり
まだ実質的に残っている。**D は `[OPEN]`。**

---

# 追記2 — epoch を跨いだ検証と、法則を**連鎖**させた場合のカバレッジ(同日)

## §11 3つの残余は epoch あたり**ちょうど3個**、構造的である

`d_rf4_epochs.py`:epoch `k=4` と `k=5` を分節し、各部分を**実際に法則を適用して**分類する
(テープからパラメータを読み、法則の span を走らせ、セル単位で照合。形状ヒューリスティクスは
使わない)。

| | k=4 | k=5 |
|---|---|---|
| epoch 歩数 | 905,244 | 3,650,250 |
| 部分の個数 | **13** | **13** |
| 被覆(1部分1法則) | 10 (99.9589%) | 10 (99.9596%) |
| 残余 | **3** | **3** |
| 残余の位置 | 最初 / 2番目 / 最後の turn | 同じ |

**構造は k=4 と k=5 で完全に同一。** 残余は epoch あたり3個で固定であり、`k` とともに
**個数は増えない**(サイズは epoch スケールで増える)。

## §12 だが「1部分1法則」の分類は厳しすぎた — 連鎖させると 99.98%

残余の状態列を見ると、それらは**同じ法則の連鎖**である。例:k=4 の最後の turn は
`rung0(u=10) ; descend(q=13,r=7) ; rung0(u=0) ; <epoch 境界で切断>`。
分節器は `IN` 形の配置でしか切らないので、連鎖を1部分と数えていた。

法則を貪欲に**連鎖適用**した場合(`d_rf4_epochs.py` の greedy cover):

| | k=4 | k=5 |
|---|---|---|
| `tile` 適用 | 379 | 782 |
| `rung0` 適用 | 4 | 5 |
| `descend` 適用 | 2 | 4 |
| `tile2` 適用 | 2 | 2 |
| **被覆** | **904,986 / 905,244 = 99.9715%** | **3,649,593 / 3,650,250 = 99.9820%** |
| 詰まった区間 | 3(計 258 歩) | 5(計 657 歩) |

**カバレッジは `k` とともに上がる。**

## §13 唯一の新しい原子 — 状態 `D` に錨を置いた `swap10`

詰まった区間のうち epoch 先頭のものは `(DF)^n` を含む。これは
`D1→0RF`, `F0→1RD` = 2歩、head `+2`、`1 0 ↦ 0 1` — **`swap10` 原子そのもの**だが、
復路状態 `B` ではなく**状態 `D` に錨を置いている**。

`D^R` の各状態を調べると(`/tmp` の走査ではなく本文書の主張として再掲):
`B` は `swap10`+`swap01` の両方、`C` と `D` は `swap10` のみ、`E` と `F` は `swap01` のみ。
つまり `D` では `swap01` が成り立たない(`D0 → 1LA` は左に動く)ので、これは**部分的な**原子集合
である。

そこで `RungCalc.sweep10At` を追加した:**fold は `swap10` 原子1本だけを要求し、`sB` である
必要も `Atoms` 全体である必要もない**。既存の `sweep10` はその特殊化として derive し直した
(呼び出し側は無変更)。`DMachine.swapD` / `sweepD` が `D` 錨の実例で、kernel `rfl` の
grounded インスタンス付き。

## §14 残っているもの(更新)

* 詰まった区間 3〜5 本(epoch の 0.02〜0.03%)。うち1本は `(DF)^n` を含み、`sweep10At` で
  fold できるが**周囲の組立ては未了**。他は epoch の継ぎ目付近の短い合成で、第5の法則か、
  既存法則の入口条件の緩和を要する。
* **継ぎ目**(追記1 §10)。これが依然として本当のギャップである。99.98% は
  「これだけの歩数が証明済み法則のインスタンスである」という測定であって、**epoch が
  証明されたという意味ではない**。連鎖には各法則の出力配置が次の入力配置と実テープ上で
  一致することが要り、それには D の cascade 語が要る。
* RF-5、entry(291,168 歩)、`k ≥ 10`、最終組立て。

**D は `[OPEN]`。**

---

# 追記3 — ladder セグメントを1つの法則にした(2026-07-27)

## §11 なぜ必要か

ladder セグメントは rung タイルの `n` 回連射であり、`n` は大きい:`k=4` の epoch だけでも
2, 4, 36, **308**, 28, 1 本、`k=6` の main セグメントは **1246** 本に達する。
**セグメント全体が1つの補題適用にならない限り、epoch 規模の組立ては始まらない。**

span は一定倍ではない — rung ごとに `u` が +2、`m` が −1 なので**各 rung が前より6歩長い**。
よって `ladderSpan` は iterate と同じ再帰で定義する。

## §12 法則(`RungCalc`)

```lean
def ladderSpan (cr mk ta s10 s01 tu u m : Nat) : Nat → Nat
  | 0     => 0
  | n + 1 => span cr mk ta s10 s01 tu u (m + n) + ladderSpan cr mk ta s10 s01 tu (u + 2) m n

theorem tileIter (h : Atoms …) : ∀ n u m c g p TAIL REST,
    steps T (ladderSpan … u m n) (IN sA u (m + n) c (g + 3*n) p TAIL REST)
      = some (IN sA (u + 2*n) m (c + n) g (p + 3*n) TAIL REST)
```

`n` についての帰納で、既証明の `tile` から出る。`Atoms` を満たす**任意の機械**に対して成立。

## §13 検証

`k=4` epoch の**実測 6 セグメント**すべてを閉形式が厳密に再現(`/tmp` ではなく本文書の表):

| rungs | IN₀ の (u, m_lit) | 予測 | 実測 |
|---:|---|---:|---:|
| 2 | (0, 2) | 60 | 60 |
| 4 | (1, 6) | 264 | 264 |
| 36 | (0, 65) | 18,360 | 18,360 |
| **308** | (0, 308) | **857,472** | **857,472** |
| 28 | (71, 28) | 19,320 | 19,320 |
| 1 | (8, 1) | 69 | 69 |

Lean 側では `DMachine.ladderSpans_measured` が6本すべてを `rfl` で(公理フリー)、
`DMachine.ladder_main` が main セグメントを1適用として述べる:
`IN 0 308 1 (g+924) → IN 616 0 309 g`、**857,472 歩、head +924**。

コーパス `lake build` EXIT=0(93 jobs)、公理は `[propext, Quot.sound]` のみ。

**D は `[OPEN]`。**
