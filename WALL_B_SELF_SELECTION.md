# Wall (B): does the self-referential definition of the orbit's point select it into the good set? (2026-06-28)

Attack on **wall (B)** of the BB6/Antihydra core (the "named-orbit selection" wall isolated in
`SESSION_2026-06-28_TWISTED_RPF.md`: the core splits into **(A) gap = Mahler** and **(B) selection of
the specific orbit `c0=8` into the equidistributing set**). The angle here, and the only thing tested:

> **Does the SELF-REFERENTIAL definition of the orbit's "point" force it into the good (equidistributing)
> set, excluding the exceptional / clustered / Liouville-like behaviour that the a.e.-theorem permits on a
> measure-zero set?**

Every claim labelled **[PROVEN] / [CONDITIONAL] / [OPEN] / [OBSERVED] / [REFUTED]**. Numerics:
`wall_b_self_select.py`, `/Users/aokiyousuke/quantum-ecc/.venv/bin/python` only. **Zero false proofs. NOT committed.**
AUDIT lesson heeded: self-reference arguments tend to be circular — the central deliverable below is the
*exact line* at which this one closes vs. becomes circular.

---

## 1. The self-referential fixed point, formalized precisely

**[PROVEN — exact dynamical statement].** The Antihydra orbit is **not** a member of a parametric family
`{x·(3/2)^n}` with `x` ranging over an interval. It is the single forward trajectory of one deterministic
map from one fixed initial condition:

```
   system  =  ( ℤ₂ , T , c0 = 8 ),     T(c) = (3c − (c mod 2))/2  =  "×(3/2)" on the 2-adics
   itinerary  e_n = parity(T^n c0) ∈ {0,1}   under the partition  ℤ₂ = {even} ⊔ {odd}.
```

(On the integers this is exactly `c_{n+1} = ⌊3c_n/2⌋`, `e_n = c_n mod 2`.) The **carry phase** is
`ψ_n = T_n/2^{n+1} mod 1` with the closed recursion (derived, `exp_sum.py` / `ATTACK_BOOTSTRAP.md` §1)

```
   ψ_{n+1} = { (3/2) ψ_n + e_n/4 },        e_n = 1[ψ_n in the "odd" half ]   (partition rule).
```

So **the phase is generated by the past itinerary (causal), and the itinerary is the partition-coding of the
phase**: `ψ → e → ψ` is a *closed loop*. The "point" of the orbit is **`c0 = 8` itself** — a fixed initial
condition, not a number drawn from a continuum. Equidistribution of `(e_n)` = the single orbit being
**generic for Haar measure** (the `T`-invariant / SRB measure on ℤ₂).

**The contrast with Koksma made exact.** Koksma's theorem: for `θ>1`, `{x θⁿ}` equidistributes for **a.e.**
`x` in any interval; the exceptional set `E` (extreme-Diophantine / Liouville-like `x` with phase-clustering)
has Lebesgue measure 0. Here there is **no interval and no `x`**: a single deterministic orbit. "Almost
every" is *vacuous* on a one-point set — the orbit is always a candidate member of the measure-zero `E`.
**This is precisely the a.e.→specific wall.** The self-reference question is whether the *closed loop*
`ψ↔e` supplies a **non-measure-theoretic** reason to exclude `E`-behaviour for this one orbit.

---

## 2. Does self-consistency exclude clustering? — the sub-question, answered

The proposed lever: *a clustered orbit would have biased parities, which feed back through the
`(3/2)^j`-weighted carry sum and (by the map) change the very point; clustering is therefore not
self-consistent, so the fixed point cannot be a clustering (exceptional) point.* Two findings kill the
global form and locate the circularity in the local form.

### 2a. Globally FALSE: the most-clustered configurations ARE self-consistent. **[REFUTED]**
The atomic fixed points `δ₀` (`c0=0`, all-even, even-density 1) and `δ₁` (`c0=1`, all-odd, even-density 0)
are **genuine fixed points of `T`** (`T(0)=0`, `T(1)=1`) and are **maximally clustered** (constant parity).
They are exactly the self-consistent atoms re-found in `AUDIT_CONTRACTION.md` §1b. Numerics §[1]:
constant parity, verified. **So self-consistency does NOT exclude clustering** — the most extreme clustered
states are self-consistent. Self-reference cannot be a global selector. (Consistent with: the McKean–Vlasov
closure `ν=L_ν ν` admits `δ₀,δ₁` besides Haar — `AUDIT_CONTRACTION.md`.) The "clustering is not
self-consistent" claim is therefore at best a **basin/local** statement about `c0=8`, never a global one.

### 2b. The genuine (non-circular) gain — and exactly how weak it is. **[PROVEN, but weak]**
For **integer** `c0 ≥ 2`, `T(c) = ⌊3c/2⌋ ≥ ⌊3·2/2⌋ = 3 > c` grows strictly and unboundedly (numerics §[2]:
`c0=8` reaches 39 bits in 60 steps; same for `2,100,2²⁰`). Hence the integer orbit **can never return to the
clustered atoms `0,1`** — it is *not* in their basin. **This is a real, unconditional, non-circular
exclusion: the orbit is not attracted to an atomic (constant-parity) fixed point.** But this excludes only
the **grossest** failure mode. Equidistribution failure does **not** require convergence to an atom: an
orbit can have density `≠1/2`, or recurrent/transient clustering with no limit, while still growing. Growth
rules out the atoms; it says nothing about Diophantine phase-clustering. **The selection gain from
self-reference stops here**, far short of equidistribution.

### 2c. The local form is CIRCULAR — exact location. **[the circularity, pinned]**
The remaining (local) form: *biased weights `e` ⇒ the `(3/2)^j`-weighted carry sum `Σ_j e_{n-1-j}(3/2)^j`
is non-clustered ⇒ parities re-balance ⇒ a clustered state is not self-consistent.* The implication
"**biased weights ⇒ the `(3/2)^j`-weighted sum equidistributes mod 1**" **IS the Mahler/Korobov
equidistribution statement** (`exp_sum.py`: annealed `|E[e(ψ_n)]| = Π_j |cos π{(3/2)^j/4}| → 0` *iff*
`{(3/2)^j}` equidistributes = Mahler). So the step that would break a cluster *assumes* the very
equidistribution of the `(3/2)^j` phases that is the open core. **The local self-consistency argument is
circular at exactly this implication.** This matches the `ATTACK_BOOTSTRAP.md` finding from the dual side:
the feedback `gain ≈ 0` (input bias has no leverage on output balance), so output balance comes **entirely**
from the base term `A(0,N)=(1/N)Σ(-1)^{bit_n(T_n)}` = Mahler, and the bias hypothesis is *inert*.

---

## 3. Honest verdict — genuine selection gain, or "equidistributes because it equidistributes"?

| Step | Status |
|---|---|
| Orbit is a single deterministic `T`-trajectory of fixed `c0=8`; "a.e." is vacuous | **[PROVEN]** |
| Self-consistency excludes clustering **globally** ("only good points are self-consistent") | **[REFUTED]** — `δ₀,δ₁` are clustered *and* self-consistent fixed points |
| Integer `c0≥2` grows ⇒ orbit not attracted to the clustered atoms `0,1` | **[PROVEN]** (non-circular, but only excludes atomic failure) |
| "Biased parities feed back to non-clustering" (the cluster-breaking step) | **[CIRCULAR]** — equals the `{(3/2)^j}` equidistribution = open Mahler |
| Self-reference selects `c0=8` into the equidistributing set | **[OPEN]** = a.e.→specific, untouched |

**Verdict.** Self-reference yields **one genuine, non-circular exclusion** — the orbit cannot collapse to a
constant-parity atom (it grows). That is a real structural fact, but it is **strictly weaker** than
equidistribution and does not touch the Diophantine exceptional set. Beyond it, the self-consistency
argument is **operatively circular**: the only mechanism by which a clustered state could be ruled
"non-self-consistent" is the equidistribution of the `(3/2)^j` phases, i.e. it **reduces to "the orbit
equidistributes because it equidistributes."** Self-reference **relocates** the kernel (into "is `c0=8` in
the good basin?") but does **not** remove it — the same pattern as the measure-contraction
(`AUDIT_CONTRACTION.md`) and bootstrap (`ATTACK_BOOTSTRAP.md`) routes.

---

## 4. Numerical self-correction test (`wall_b_self_select.py`) — is there an active restoring force?

A *genuine* self-selection would show the map **actively pulling** clustered configurations back to balance
(restoring force, early cluster-breaking). It does not. All numerics exact (big-int parities; carry phases
floated from exact top bits).

**(3) Forced-cluster test.** `c0 = 2^k` forces **exactly `k` consecutive even** parities (`run0 = k`, verified
`k=10,20,40,80`). The cluster is **not broken early**; it persists its full forced length, then balance
**resumes the very next segment**:

| `c0=2^k` | forced run0 | even-dens [0:k] | even-dens [k:2k] | even-dens [2k:10k] |
|---|---|---|---|---|
| `2^10` | 10 | 1.000 | 0.400 | 0.525 |
| `2^20` | 20 | 1.000 | 0.450 | 0.494 |
| `2^40` | 40 | 1.000 | 0.500 | 0.503 |
| `2^80` | 80 | 1.000 | 0.425 | 0.470 |

**[OBSERVED] The cluster is *diluted/outgrown*, not actively corrected** — balance resumes after the forced
segment with no overshoot/restoration; the cumulative density drifts to ~1/2 only because subsequent bits are
balanced.

**(4) Restoring force.** `corr(next sign, trailing-window even-bias) = −0.005, −0.004, −0.004, +0.001`
(`W=8,32,128,512`, real orbit `N=2·10⁵`). **[OBSERVED] ≈ 0 — no active pull** (re-confirms `ATTACK_BOOTSTRAP.md`).

**(5) Spontaneous re-clustering.** Max parity run over `N=2·10⁵`: orbit **20**, fair coin **18**, `log₂N ≈ 17.6`.
**[OBSERVED] No super-clustering beyond fair-coin statistics** — i.e. the only "balance" present is the
statistical one (= Mahler), not a dynamical restoring mechanism.

**(6) Phase clustering, clustered vs balanced start.** L∞ phase-discrepancy (20 bins): clustered starts have
huge **early** discrepancy that relaxes to the **same late value** as the real orbit:

| start | early [:200] | late [400:] | even-density |
|---|---|---|---|
| `c0=8` (orbit) | 0.055 | 0.008 | 0.506 |
| `c0=2^40` | 0.200 | 0.008 | 0.512 |
| `c0=2^200` | **0.950** | 0.008 | 0.534 |
| random `~2^300` | 0.045 | 0.008 | 0.487 |

**[OBSERVED] The limiting phase law is start-independent.** Even a `c0=2^200` start (near-degenerate early
phases, discrepancy 0.95) relaxes to discrepancy 0.008 — identical to `c0=8`. The cluster is **diluted by a
start-independent limiting law**, not corrected by feedback. (That start-independent limiting law is exactly
the equidistribution one would have to prove = Mahler.)

---

## 5. The precise point where the argument CLOSES vs. FAILS

- **CLOSES (non-circular):** "the orbit is not attracted to a constant-parity atom." Proof: integer `c0≥2`
  grows under `T`, never returns to `{0,1}`. **[PROVEN]** — but excludes only atomic failure, not
  equidistribution-failure.
- **FAILS (becomes circular) at:** the implication **"biased parities ⇒ the `(3/2)^j`-weighted carry sum is
  non-clustered (re-balances)."** This is `Σ_j e_{n-1-j}(3/2)^j` cancellation = `{(3/2)^j}` equidistribution
  = the **open Mahler core**. The numerics confirm the mechanism is *dilution by a start-independent limiting
  law*, not a restoring force — and that limiting law is the thing to be proved.

**Net.** Wall (B) is **not** breached by self-reference. The self-referential / fixed-point structure gives a
real but weak exclusion (no atomic collapse) and **transparently reduces** the rest to wall (A) = Mahler.
There is **no hidden logical circularity** and **no false proof**; there is **no genuine selection gain**
toward equidistribution. Selecting `c0=8` into the good set remains **[OPEN] = a.e.→specific = Mahler/AEV**,
exactly the kernel `(5)` of `PROOF_STATUS.md`.

### New (non-circular) micro-asset banked
`[PROVEN]` *Integer Antihydra orbits with `c0≥2` grow unboundedly and so lie outside the basin of every
constant-parity (atomic) self-consistent fixed point of `T`* — the orbit's failure mode, if any, must be a
**non-atomic Diophantine clustering**, never a collapse to constant parity. This narrows the exceptional set
the proof must exclude (drop the atoms) without assuming equidistribution. Live next angle: characterize the
**non-atomic** exceptional configurations on ℤ₂ (recurrent-but-unbalanced 2-adic orbits) and ask whether the
*growth + carry structure* excludes those too — the only remaining sub-question of (B) that is not already
identical to (A).
