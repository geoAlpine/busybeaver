# AIU-3adic via Weyl / Erdős–Turán on the neutral group ℤ₃ˣ (2026-06-30)

*WEAPONS_AUDIT style. Attacks the **3-adic part of AIU** (`AIU_JOININGS` §2: AIU ⟺ the A-stable F₃-leaf
conditionals μ_x³ are ℤ₃ˣ-rotation-invariant = spherically Haar) by harmonic analysis on the compact
neutral group ℤ₃ˣ: form the multiplicative Weyl/character sums of the orbit's 3-adic **unit part** and ask
whether they → 0 (the 3-adic equidistribution AIU's spherical-Haar would require). SOUNDNESS PARAMOUNT:
every claim labelled; **no claim to prove (K)**; no label upgraded. Numerics
`/Users/aokiyousuke/quantum-ecc/.venv/bin/python`, exact big-int, seed o₀=27, N=10⁵ induced steps. NOT committed.*

---

## 0. ONE-LINE VERDICT

**The 3-adic Weyl sums are NOT a new, easier equidistribution problem and they are NOT a separate problem:
via the exact coupling `v₃(o')=D−1` together with `3o−1 ≡ −1 (mod 3)`, the orbit's 3-adic unit part is
*deterministically slaved to D*, and the leading character sum is EXACTLY the D-parity imbalance
`freq(D odd) − freq(D even)` — a D-statistic, hence (K)-flavoured, NOT independent.** But it reduces to the
**WRONG (non-vanishing) value**: `[OBSERVED, stable ≈0.33 with no decay over N≤10⁵; PROVEN-conditional =1/3
if D is geometric]`. So **orbit-level "AIU-3adic" (raw unit part Haar on ℤ₃ˣ) is FALSE / DISPROVEN-at-orbit-
level**, in sharp contrast to the 2-adic side whose cylinder discrepancy *does* decay to ~0 (the open (K)
target). The genuine AIU spherical-Haar of `AIU_JOININGS` is therefore exposed as a strictly **ambient,
post-lattice (measure-level) phenomenon, invisible to — indeed contradicted by — the pointwise orbit angle**:
the (T2) tautology barrier (pointwise lock ≠ measure invariance) made **quantitative**. No partial AIU-3adic
is proven; the 3-adic side does not unlock (K) and is not a softer target. No machine decided. No label upgraded.

---

## 1. The ℤ₃ˣ character-sum formulation of AIU-3adic `[DEFINITION + PROVEN equivalence]`

The dual of the compact group ℤ₃ˣ is the group of multiplicative characters χ of `(ℤ/3ᵐ)ˣ`,
`m=1,2,…`. Since 2 is a primitive root mod 3ᵐ for all m (`ord₂(3ᵐ)=φ(3ᵐ)`, `AIU_JOININGS` §2 `[PROVEN]`),
`(ℤ/3ᵐ)ˣ` is cyclic of order `φ(3ᵐ)=2·3^{m−1}`, generated by 2; characters are
`χ_t(g)=exp(2πi·t·ind₂(g)/φ)`, `t=1,…,φ−1` (nontrivial), `ind₂` the base-2 discrete log.

Write each orbit term as `o = 3^{v₃(o)}·u`, `u∈ℤ₃ˣ` its **3-adic unit part**; reduce `u mod 3ᵐ ∈ (ℤ/3ᵐ)ˣ`.
Define the **3-adic multiplicative Weyl sums**
> `S_χ(N) := (1/N) Σ_{j<N} χ(unit part of o_j)`.

> **Orbit-AIU-3adic ⟺ `S_χ(N) → 0` for every nontrivial χ mod 3ᵐ, all m** (Weyl/Erdős–Turán criterion for
> equidistribution of the unit part w.r.t. Haar on ℤ₃ˣ). `[PROVEN equivalence — standard]`

This is the **multiplicative / 3-adic** analogue of the 2-adic cylinder equidistribution (K). It is the
correct orbit-level shadow of the `AIU_JOININGS` §2 statement "μ_x³ spherically Haar," whose angular marginal,
if it held, would force these sums to vanish. (Caveat made precise in §4: orbit-marginal ≠ ambient conditional.)

---

## 2. The decisive algebraic fact — the unit part is D-slaved `[PROVEN]`

The induced map is `o' = 3^{D−1}m`, `D=v₂(3o−1)`, `m=oddpart(3o−1)`, and `3o−1 ≡ −1 (mod 3)` ⟹ `3∤m`
(`ADELIC_COUPLING` §1a). Hence `v₃(o')=D−1` `[PROVEN]` and the **unit part of `o'` is `m`**. Reducing mod 3:
> `u(o') ≡ m = (3o−1)·2^{−D} ≡ (−1)·2^{−D} ≡ (−1)·(−1)^{−D} = (−1)^{D+1} (mod 3)`,  since `2 ≡ −1 (mod 3)`.

> **Lemma (mod-3 unit part = D-parity).** `unit(o_{j+1}) ≡ (−1)^{D_j+1} (mod 3)`: `≡1` iff `D_j` odd, `≡2` iff
> `D_j` even. `[PROVEN]` (Verified: 0 exceptions /10⁵; coupling `v₃(o_{j+1})=D_j−1` 0 exceptions /10⁵.)

The unique nontrivial character mod 3 is `χ(1)=1, χ(2)=−1`, so
> **`S_χ(N)|_{m=1} = freq(D odd) − freq(D even)` exactly.** `[PROVEN identity]`

For general m, `u(o_{j+1}) mod 3ᵐ = (3o_j−1)·2^{−D_j} mod 3ᵐ` depends on `D_j mod 2·3^{m−1}` **and** on
`o_j mod 3^{m−1}` — i.e. on D and the orbit's own lower 3-adic digits. **Every 3-adic character sum is a
function of the D-sequence (and self-referential low-order orbit data); none is an independent channel.**
This is exactly (T2)/`v₃=D−1`: the 3-adic place carries the same information as the 2-adic depth, now read
on the *angular* (unit) coordinate.

---

## 3. NUMERICS `[OBSERVED, exact big-int, N=10⁵, o₀=27]`

`threeadic_fourier.py`. Coupling `v₃(o_{j+1})=D_j−1`: 0 exceptions. `meanD=2.00069`, `freq(D=1)=0.49948`,
`freq(D odd)=0.66575`, `freq(D even)=0.33425`.

**m=1** (one nontrivial χ): `|S_χ|=0.3315` = `freq(D odd)−freq(D even)`. **Far from 0.** Unit residue freqs
`{1:0.666, 2:0.334}` (Haar would be `{0.5,0.5}`).

**m=2** (φ=6, five nontrivial χ): `|S_χ| =` 0.287, 0.187, **0.332**, 0.187, 0.287. (The 0.332 is `t=3`, the
pullback of the mod-3 char.) Unit freqs range `0.040–0.315` vs uniform `0.167`. **None vanish.**

**m=3** (φ=18): `|S_χ|` up to **0.349** (primitive chars `t=1,17`), median ≈0.19, min 0.032; unit freqs
`0.001–0.16` vs uniform `0.056`. **Grossly non-uniform.**

**N-sweep, leading 3-adic sum vs 2-adic cylinder discrepancy (the (K) target):**

| N | `|S_χ| (m=1)` 3-adic angular | 2-adic `max_r |freq(o≡r mod 8)−¼|` |
|---|---|---|
| 1 000 | 0.350 | 0.017 |
| 20 000 | 0.334 | 0.0015 |
| 100 000 | 0.3315 | 0.0018 |

> **The 3-adic angular Weyl sum is PINNED at ≈⅓ with no decay; the 2-adic cylinder discrepancy DECAYS toward
> 0.** Opposite behaviours: the 2-adic orbit empirically equidistributes (the open (K)/(A) target), the
> 3-adic unit part provably does **not** (it is D-slaved). `[OBSERVED]` If `D` is geometric `P(D=k)=2^{−k}`
> (the conjectured/(K)-regime law), `freq(D odd)=⅔`, `freq(D even)=⅓`, so `S_χ|_{m=1}=⅓` exactly
> `[PROVEN-conditional]` — orbit-Haar on ℤ₃ˣ is **incompatible** with the conjectured D-law.

---

## 4. Does `v₃=D−1` make the 3-adic side tractable, reduce it to (K), or open a new problem? `[verdict]`

| question | answer | label |
|---|---|---|
| Are the 3-adic Weyl sums **easier** than the 2-adic kernel (K)? | **No.** They are not a softer equidistribution: the leading one is an *equality* with a D-statistic, and it is **non-vanishing**, so it certifies **non**-equidistribution rather than buying any partial. | `[PROVEN — the identity; OBSERVED — non-vanishing]` |
| Are they a **genuinely different / independent** open problem? | **No.** By `v₃=D−1` and `3o−1≡−1 (mod 3)` every χ-sum is a deterministic function of the D-sequence (and low-order self-data); there is **no independent 3-adic channel** (this is (T2) on the angular coordinate). | `[PROVEN]` |
| Do they **reduce to (K)** via the coupling? | **Reduce to D-statistics, yes; to (K) itself, no — to a *different* functional of the D-law.** (K) is `freq(D=1)≤½` (a tail/cylinder fact); the 3-adic sums are the **parity / mod-`2·3^{m−1}` imbalances** of D. Same single-orbit genericity wall, different target, and the target is provably/empirically **nonzero**. | `[PROVEN]` |
| Is **orbit-level AIU-3adic** (raw unit part Haar on ℤ₃ˣ) true? | **No — DISPROVEN at the orbit/empirical level**: `S_χ|_{m=1}=freq(D odd)−freq(D even)≈⅓≠0`, no decay, all higher-m sums large. | `[OBSERVED; PROVEN-conditional on geometric D]` |
| Then what becomes of `AIU_JOININGS` §2 (μ_x³ spherically Haar)? | **Consistent, and sharpened.** That is an **ambient measure-level** statement on the solenoid `X=(ℝ×ℚ₂×ℚ₃)/ℤ[1/6]`; the F₃-leaf conditionals are NOT the raw orbit angular marginal (the orbit sits on the Haar-null diagonal ℤ[1/6], dense in ℚ₃, killed by the lattice reduction + the A-dilation 1/3 + renewal cocycle). The pointwise 3-adic angle is **D-locked and non-Haar**; AIU's spherical-Haar can only come *after* lattice reduction, at the ambient level — exactly the (T1)–(T2) "pointwise lock ≠ measure invariance" barrier, now **quantitative**. | `[PROVEN — consistency; the barrier is real]` |

**Honest classification of the three asks (a)/(b)/(c):**
- **(a) AIU-3adic partial PROVEN?** **No** — the opposite: orbit-level 3-adic Haar is *disproven* (non-vanishing
  Weyl sums). No partial toward AIU is obtained.
- **(b) New characterization?** **Yes, a sharp negative one:** the orbit's 3-adic angular law is the explicit
  pushforward of the D-law under `u(o') ≡ (−1)^{D+1} (mod 3)` (and its mod-3ᵐ refinements); in particular the
  3-adic unit part is **never** Haar in the orbit-empirical sense. This also re-derives, on the angular axis,
  that `freq(D=1)=density{3∤o}` etc. (`ADELIC_COUPLING` §1a) and adds the **parity** functional.
- **(c) = (K) via the coupling?** **Reduces to D-statistics (so it is (K)-class, not independent and not
  easier), but to a different, non-vanishing functional of the D-law — it does NOT solve or weaken (K).**

**Why this is the expected outcome, not a paradox.** `AIU_JOININGS` §3 already proved the AIU direction is the
**neutral** ℚ₃-rotation (`|2|₃=1`, zero entropy). Harmonic analysis on the neutral group ℤ₃ˣ is the right
language, but the orbit's coordinate on that neutral group is *not free* — it is rigidly tied to the 2-adic
depth D by the product-formula coupling. So the neutral direction, though a compact-abelian rotation where
Weyl/Erdős–Turán *live*, carries **no independent equidistribution to harvest at the orbit level**: its Weyl
sums are D-statistics, and the leading one is forced **away** from 0. The harmonic-analysis route therefore
**closes** as an orbit-level attack and **relocates** AIU entirely to the ambient measure, confirming (not
breaching) the wall.

---

## Sources
- Repo: `AIU_JOININGS.md` (§2 spherical-Haar reformulation; §3 neutral-direction obstruction),
  `ADELIC_COUPLING.md` (§1a `v₃(o')=D−1`, `3o−1≡−1 mod 3`), `NEWMATH_ADELIC_RIGIDITY.md` (AIU/AIU-min, (T1)–(T2)),
  `INTRATERM_ADELIC_MINING.md` (T1/T2 pointwise lock). `[PROVEN / framework]`
- Weyl / Erdős–Turán equidistribution criterion on compact abelian groups; Pontryagin duality for ℤ₃ˣ
  (characters = 3-power roots of unity). `[PROVEN-in-lit]`
- 2 is a primitive root mod 3ᵐ ⟹ `(ℤ/3ᵐ)ˣ` cyclic, `\overline{⟨2⟩}=ℤ₃ˣ`. `[PROVEN-in-lit / verified m≤12]`
- Furstenberg ×2,×3 / Mahler 3/2 (the 2-adic kernel (K) analogue). `[OPEN]`
- Numerics: `threeadic_fourier.py` (this session, scratchpad), N=10⁵, exact. `[OBSERVED]`

No machine decided. No label upgraded.
