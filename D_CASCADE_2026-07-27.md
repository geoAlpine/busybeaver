# `Dcascade` — 継ぎ目帰納の前提を Lean に置いた(2026-07-27)

`RESUME_2026-07-26.md` §3 の項目1(継ぎ目 = 本当のボトルネック)への着手。

`lake build` EXIT=0(コーパス **85 jobs**)、公理は `[propext, Quot.sound]` のみ、`sorryAx` 0、
`Classical.choice` 0。**本文書の Lean 定理はすべて公理フリー(`rfl`)。**

**D は `[OPEN]`。本文書は機械を決着させず、ラベルを昇格させない。**

---

## §0 なぜこれが必要か

`RungCalc` の4法則(`tile`, `tile2`, `rung0`, `descend`)は epoch の 99.98% を覆う。
欠けているのは epoch そのもの — 法則を連鎖させるには**各法則の出力配置が次の法則の入力配置と
実テープ上で一致する**ことが要り、それは D の cascade 語についての言明である。
**したがって語族が Lean に無いと継ぎ目帰納は始められない。** 本文書はその前提だけを置く。

## §1 まず doc を信用せず再測定した

`D_SPEC_2026-07-26.md` §2 は語族を閉形式で与え `[MEASURED, k = 4..9]` としている。
本プロジェクトの規律は「主張の上に建てる前に再導出」なので、`d_cascade_measure.py` が
**生軌道から** `M1(k)` を測り直した(k=1..9、313M 歩、34 秒)。確認できたこと(k=4..9):

* milestone 時刻(`291168, 1196412, 4846662, 19488198, 78148404, 312959448`)、位置 `−8k`、
  状態 `A`、head セル `0`、**左側が完全に空白**;
* §2 表の右語(明示語)ぜんぶ;
* 閉形式 `a(k) = 39·2^{k−1} − 4`、`G(k) = 57·2^{k−3} − 3k + 9`、`w(k) = 117·2^{k−1} + 3k − 55`;
* ブロック構成(`jmax` からのループ)が k=4..9 の**すべての milestone を再現**。

## §2 `D_SPEC` §2 への訂正 — 追加されるブロックは **1個**、2個ではない

§2 の prefix-stability の文は `M1(k) → M1(k+2)` が「**二つ**の新ブロックを追加する」と述べている。
実測は**一つ**である:最後以外の全ブロックを保持、最後の `(G(k), a(k))` を
`(33·2^{k−3}+12, 66·2^{k−2})` に書き換え、そして `(G(k+2), a(k+2))` を**1個**追加する。

```
k=4 -> 6: [(33,66),(111,308)] -> [(33,66),(78,264),(447,1244)]     追加 1 個
k=5 -> 7: [...,(222,620)]     -> [...,(144,528),(900,2492)]        追加 1 個
k=6 -> 8, k=7 -> 9 も同様(すべて 1 個)
```

`d_cascade_measure.py §4` で k=4,5,6,7 について測定。

## §3 Lean(`lean/DCascade.lean`)

* `block g a := zeros g ++ pow10 a`;
* `aLast j` / `gLast j` — 閉形式(`k = j + 4` で添字づけ、切り捨て減算を指数から排除);
* `blockE e` — 中間ブロック(`e=0`, `e=1` が2つの底、`e ≥ 2` は一様);
* `midBlocks p n` — パリティ `p` の中間ブロック `n` 個(`e = p, p+2, …`、テープ順);
* **`Dcascade j`** — `M1(k)`(`k = j+4`)の head 右側の語全体;
* `M1 j : Cfg St := ⟨.A, -8*(j+4), ⟨[], false, Dcascade j⟩⟩` — **左フィールドは文字通り `[]`**。

**定義の pin(すべて閉じた `rfl`、公理フリー):**

| 定理 | 内容 |
|---|---|
| `cascade4` … `cascade9` | `Dcascade j` が k=4..9 の**実測ブロック分解に一致** |
| `width4` … `width9` | 語長が `893, 1832, 3707, 7454, 14945, 29924` |
| `lasts` | `(aLast j, gLast j)` が6水準で実測値に一致 |
| `prefix46`, `prefix57` | **訂正後の** prefix stability(追加1個)を語の等式として |

定義の再帰がずれれば、これら 15 本が落ちる。

## §4 明示的に**証明していない**もの — epoch 法則

```lean
def EpochLaw : Prop := ∀ j, steps dT (epochSpan j) (M1 j) = some (M1 (j + 1))
```

これが**継ぎ目帰納**であり、D の正直な残ギャップである。4法則が epoch の 99.98% の歩数を
説明しても、`M1(j) → M1(j+1)` に連鎖させるには各法則の出力配置が次の入力配置と**この語の上で**
一致する必要があり、そこが未証明である。

本プロジェクトの `[DESIGN]` 流儀に従い **`def … : Prop` として述べてあり、`sorry` ではない** —
公理監査に漏れ出るものは何もない。`epochSpan` の値が実測差分であることは
`epochSpan_measured` が `rfl` で押さえている。

## §5 残っているもの

1. **`EpochLaw` の証明**(継ぎ目帰納)— 最大の残作業。本文書はその**前提**を置いただけ。
2. 貪欲連鎖で詰まる 3〜5 本の短区間(epoch の 0.02〜0.03%)。1本は `(DF)^n` を含み
   `sweep10At` で fold できるが周囲の組立ては未了。
3. RF-5、entry(blank → `M1(4)` = 291,168 歩)、`k ≥ 10` 未実測、最終組立て。
4. milestone 族は `k = 4..9` のみ pin。`k = 1,2,3` は族外(実測で再確認済み:
   `a(k)` は k=2,3 でも成り立つが `G(k)` は k≥4 のみ)。

---

*計器(repo root, committed): `d_cascade_measure.py`(生軌道からの milestone 再測定・
閉形式検査・ブロック再帰・構成の再導出)。Lean: `lean/DCascade.lean`。*

**No machine decided by this document. No label upgraded. `D` remains `[OPEN]`.**

---

# 追記 — entry を kernel で閉じ、D の非停止を**1つの義務**に還元した(同日)

## §6 entry セグメント `[PROVEN]`

`D_SPEC` §7 の「blank → `M1(4)` = 291,168 歩、kernel-`rfl` 3〜4 チャンク」を実行した。

```lean
theorem entry : steps dT 291168 init = some (M1 0)
```

`lean/DEntry.lean`。291,168 歩を1本の `rfl` にすると kernel がスタックオーバーフローするので、
生軌道から生成した(手書き転記ではない)2つの中間配置で3チャンクに切った
(`110000 + 120000 + 61168`)。両カット点はたまたま `zeros`/`pow10`/`ones` 語彙で
10 項・14 項に書ける — チャンクが安価なのはそのおかげである。

**適合は厳密で、パディング不要:** `t = 291168` で head は `pos = −32` にあり、しかも**そこが
それまでの最左点**なので `Tape.left` は文字通り `[]`;そして**それまでに訪れた最右セルが `+861`
で、それが最後の `1` の位置でもある**ので `Tape.right` は末尾 `false` を持たず、ちょうど
`Dcascade 0`(893 セル)になる。どちらも自明ではなく(片側1セルずれれば `zeros` パディングと
`steps_runpad_zeros` が要る)、したがってこの anchor は `Dcascade` の**実質的な検査**である。

**これが `M1`/`Dcascade` を機械に結びつける最初の Lean の事実でもある。** それ以前は
`DCascade.lean` は語を測定値に対して pin していただけで、`dT` との接続は無かった。

`entry_is_record`(1歩前の head は `−31`)が「左フロンティア記録である」ことも押さえている。

ビルドコストは約 1 分 40 秒。専用ファイルに隔離してある(他ファイルのビルドには影響しない)。

## §7 訂正 — 前版の `EpochLaw` は**偽**だった

前版は

```lean
def EpochLaw : Prop := ∀ j, steps dT (epochSpan j) (M1 j) = some (M1 (j + 1))
```

と書いていたが、`epochSpan` は**実測5点の表**であり `j ≥ 5` で `0` を返す。つまりこの命題は
`M1 j = M1 (j+1)` を主張しており、**未証明ではなく偽**だった。存在形に修正した:

```lean
def EpochLaw : Prop := ∀ j, ∃ n, 1 ≤ n ∧ steps dT n (M1 j) = some (M1 (j + 1))
```

これは `k ≥ 9` の span の閉形式を測っていない以上、正直な形であり、しかも
`TapeCalc.nonhalt_of_invariant` がそのまま消費できる形でもある。

## §8 D の非停止を1つの義務に還元 — `lean/DReduce.lean`

```lean
theorem nonhalt_of_EpochLaw (h : EpochLaw) : ∀ N : Nat, steps dT N init ≠ none
```

`[PROVEN]`、公理は `[propext, Quot.sound]`。材料は2つともすでに証明済み:
`DEntry.entry`(291,168 歩、kernel 検査済み)と
`TapeCalc.nonhalt_of_invariant`(機械独立の組立て補題)。

これは `lean/Completion.lean` が BB(6) 全体に対してやっている **M5「公理隔離」**を D に適用した
ものである。**D の非停止は今や `EpochLaw` ただ1つの未証明命題に懸かっている。**

## §9 現在の D の状況(正直に)

| 項目 | 状況 |
|---|---|
| rung タイル / turn 相 / rung0 / descend | **[PROVEN]**、機械独立(`RungCalc`) |
| epoch の 99.98% が上記法則のインスタンス | **[MEASURED]** k=4,5 |
| milestone 語族 `Dcascade` | **[PROVEN]** 定義 + 15本の公理フリー `rfl` で pin |
| entry(blank → `M1(4)`) | **[PROVEN]**、kernel 291,168 歩 |
| `EpochLaw ⟹ D 非停止` | **[PROVEN]** |
| **`EpochLaw`(継ぎ目帰納)** | **[OPEN] — これだけ** |

`EpochLaw` の中身は依然として「各法則の出力配置が、`Dcascade` の上で、次の法則の入力配置に
なっている」ことであり、これは実測されている(99.98%)が証明されていない。加えて貪欲連鎖で
詰まる 3〜5 本の短区間、RF-5、`k ≥ 10` の未実測も `EpochLaw` の証明には要る。

**D は `[OPEN]`。**
