# T7 recon — the `⊕` IS wirable, and the doubling phase is a RegenLaw ladder (2026-07-22)

**This document REFUTES my own entry of earlier today.** On 2026-07-22 I recorded in
`ROADMAP_2026-07-19.md` (Tier 4 / T7) and in `BB6_COMPLETE_ROADMAP_2026-07-22.md` that the `⊕` in
`h_doub ∀g = RegenLaw ∀k ⊕ (Θ(2^{2K}) doubling assembly)` was **NOT WIRED**, on the grounds that
§5g/§5h contain zero references to `regenIn`/`cascadeReg`/`RegenLaw`/`exitSteps`. That source-level
measurement was correct **and the inference drawn from it was wrong.** The Lean development does not
mention those objects; **the real orbit is full of them.**

Method: `x2t7_recon.py` / `x2t7_coverage.py` (this commit) — a from-scratch simulator of
`x2 = 1RB0RE_1RC---_0LD1LE_0RE1LD_1RF0LC_0RA1RE` plus a structural detector for the `regenIn k` /
`cascadeReg k` shapes, run over the real blank-tape orbit through the g=2 doubling phase.

## Instrument check (this repo's discipline; it earned its keep)

Anchors: `M1(1)` @ 188 099 carrying `1^503`; `M1(2)` @ 732 733 carrying `1^1021`; `M1(3)` @
2 852 091 carrying `1^2039`; state `E` at all three; no halt. **First run FAILED all three** — the
guard caught a real bug in my own transition table (state `C` on read 1 is `1LE`, write 1 move
**LEFT**; I had coded it moving right). After the fix: **all three anchors match exactly**, and
`M1(1)` @188 099 agrees with the Lean-proven `h_init_reached : steps 188099 init = some c189`.
Without the anchor guard this recon would have produced a confident, wrong answer.

## Finding 1 — the transports run ON-ORBIT inside the doubling phase `[MEASURED]`

Detector hits in the g=2 doubling phase M6(2)→M1(3) (steps 733 076 → 2 852 091, 2 119 015 raw
steps): **254** `regenIn`/`cascadeReg` configs. Their level multiplicities are exactly geometric
(`regenIn k`: 2^{11−k} hits; `cascadeReg k`: 2^{10−k}).

Decisive test — does `regenIn k` land on `cascadeReg k` after exactly `exitSteps k` steps (i.e. is
this RegenLaw's transport, on the real orbit)? Using the closed form
`exitSteps k = 2^{2k−3} + k·2^{k−1} + 2^{k−2} + 2` (`lean/X2.lean:4316`):

| k | exitSteps k | measured min gap | recorded grounding |
|---|---|---|---|
| 4 | 70 | — | 70 ✓ |
| 5 | 218 | 218 | 218 ✓ |
| 6 | 722 | 722 | 722 ✓ |
| 7 | 2 530 | 2 530 | 2 530 ✓ |
| 8 | 9 282 | 9 282 | 9 282 ✓ |
| 9 | 35 202 | 35 202 | (closed form) |
| 10 | 136 450 | 136 450 | (closed form) |
| 11 | 536 066 | 536 066 | (closed form) |

**Eight independent on-orbit confirmations of `exitSteps`**, five of them against the recorded
`exitSteps_grounds` values. `RegenLaw ∀k` — closed 2026-07-21 — therefore applies **inside** the
doubling phase. The `⊕` is real.

## Finding 2 — the doubling phase is an ASCENDING LADDER of RegenLaw transports `[MEASURED]`

After merging, the verified transports are **7 pairwise-disjoint intervals, exactly one per level
k = 5…11, in ascending order**:

| k | start | length (= exitSteps k) |
|---|---|---|
| 5 | 739 656 | 218 |
| 6 | 740 809 | 722 |
| 7 | 745 442 | 2 530 |
| 8 | 763 979 | 9 282 |
| 9 | 838 036 | 35 202 |
| 10 | 1 133 853 | 136 450 |
| 11 | 2 315 814 | 536 066 |

**Coverage: 720 470 / 2 119 015 = 34.00 %** of the doubling phase is discharged by transports
`RegenLaw ∀k` already proves. The k=11 transport alone is 25.30 %, and it **ends 211 steps before
`M1(3)`** — the phase terminates essentially at the top of the ladder.

Caveat on the numbers: the detector's RLE window (4000 cells) truncates at k=11, so the automatic
scan reported 11.79 %; the k=11 pair was then verified by hand with a 20 000-cell window (at step
2 851 880: state `E`, head 0, right = `0³ 1^2045 0² 1021 509 253 125 61 29 13 5 1` = exactly
`cascadeReg 11`, since `2^11−3 = 2045` and `descCascade 8 = descCascade(11−3)`). 34.00 % is the
figure including it. Deeper levels at larger g are not covered by this run and would only add.

Also honest: only about **half** of the `regenIn k` occurrences start an `exitSteps k` transport
(62/127, 31/63, 15/31, 7/15, 3/7 at k=5…9). The other half are `regenIn`-shaped configs occurring
inside the descent rather than at a transport IN. Coverage is computed from verified pairs only.

## Finding 3 — the UNCOVERED 66 % has a clean 4× recursion `[MEASURED]` — the actionable clue

The gaps between consecutive transports:

```
M6(2) → REGEN(5):        6 580
REGEN(5) → REGEN(6):       935
REGEN(6) → REGEN(7):     3 911
REGEN(7) → REGEN(8):    16 007
REGEN(8) → REGEN(9):    64 775
REGEN(9) → REGEN(10):  260 615
REGEN(10) → REGEN(11): 1 045 511
REGEN(11) → M1(3):         211
```

Successive ratios **4.18, 4.09, 4.05, 4.02, 4.01 → 4**. Residuals against an exact 4× law:
`g_{j+1} − 4·g_j` = 171, 363, 747, 1515, 3051 — whose own differences are 192, 384, 768, 1536
= `192·2^j`. So the gap sequence satisfies a clean two-term linear recursion with a `2^j`
correction, i.e. **the outer odometer has a closed form of exactly the kind `leadRec_closed` /
`trailSteps_closed` have.**

This is the precondition the roadmap named as decisive. §5bh and §5bj closed because the run split
at **k-independent seams** (`trailSteps k = 393 + trailCost(…) + 7`). The measurement above says the
doubling phase very likely has them too: it is not one monolithic Θ(2^{2K}) braid but an
**alternating ladder** `gap(5), REGEN(5), gap(6), REGEN(6), …, gap(11), REGEN(11), tail(211)` with
both families following k-indexed closed forms.

## What this changes

- **T7 is not "untouched" and not "the largest single object" in the sense recorded.** A third of
  the g=2 phase is already a theorem; the rest has measured structure, not fog.
- **§3′'s prediction is partially vindicated after all** — in the *wrong place*. It guessed the
  crux was "the interior ascend TO LEVEL k". That was wrong about `RegenLaw ∀k` (which closed by
  lead+trail+assembly). But the **doubling phase** genuinely IS an ascend to level k, one REGEN per
  level. The intuition was right; it was filed under the wrong obligation.
- **The calibration lesson repeats.** Of my own two claims today, the *measured* one (§5g/§5h
  contain no references) held; the *narrative* one inferred from it (therefore the ⊕ is unwired)
  was refuted within hours by measurement. Measure first, narrate after — again.

## Next (proposed, in order)

1. **Extract the gap law.** Fit and then *derive* the closed form for `gap(k)` from the machine
   (the way `x2lead_rec.py` / `x2trail_rec.py` derived `leadRec_closed` / `trailSteps_closed`),
   and identify what the gap episodes ARE structurally (this is the repack/outer-sweep of §5h).
2. **Confirm the ladder at g=3.** Re-run this recon on M6(3)→M1(4) to check the ladder is `∀g`
   (predicted: one REGEN per level k=5…13, coverage similar, gaps 4×).
3. **Then state the assembly**: `h_doub` for one generation = `∏_k [gap(k) ∘ REGEN(k)]` with
   `REGEN(k)` supplied by `regenLaw_closed`. If the gap episodes admit a `∀k` transport, T7 closes
   the same way `RegenLaw ∀k` did.

**No machine is decided. No label is upgraded.** Everything above is `[MEASURED]` on one generation
(g=2) of one machine; the `∀g`/`∀k` statements are conjectural until built in Lean.
