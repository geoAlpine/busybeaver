# o18 deep-R1 pinning — the unifying delegation law, a SOUNDNESS CORRECTION to the base transducer, and the blast-radius audit (2026-07-08)

*Harvest of the R1-pinning track (the executing agent died on an API error after producing all scripts; the main
loop completed adjudication, verification, and this note). Outcome: the 517 adversarially-reachable Unknown cells
are pinned by ONE recursive law; in the process the base transducer `o18_md_rules.py` was found UNSOUND on a deep
cell family — **erring exclusively toward false HALT** — which leaves every committed conclusion intact (audit
below) while correcting the tool. o15/o18 stay `[OPEN]`. No machine decided.*

## 1. The unifying law `[PROVEN on grid + concrete adjudication]`
The whole deep-R1 ladder (`j`-united `(1,2)`-nested words) is ONE recursive delegation: for `word = 1^j (1,2) X`
with `X = 1^p (1,2) Z`, let `inner = R1(p, Z)`; then the outer pass is determined by `inner`'s type
(`HALT` propagates; `MOVE −17` prepends `(1, 2j+2)`; `MOVE −23` splits by `j`), with the **dec3 law** at `j=1`:
HALT iff the tail's first non-unit block is separator-led, small (≤2), or absent; otherwise
`MOVE −23` with `(1,4)+1^7+dec3(tail)`. Implemented in `o18_md_rules_ext.py` (base file untouched).

## 2. SOUNDNESS CORRECTION to `o18_md_rules.py` `[PROVEN concrete, main-loop re-verified]`
The base rule "inner `c=−23` ⇒ HALT for `j≤1`" is **FALSE at `j=1` with deep tails**: e.g.
`w = (1,1)(1,2)(1,2)(1,1)(1,1)` gives `MOVE` (not halt) at m = 40, 100, 301, 1000 (re-verified independently).
The base grid never fired these cells (its `j≤1` HALT was pinned on `q≤1` tails only). Equivalence fuzz
(`o18_r1_fuzz.py`): **60 base-vs-ext disagreements adjudicated concretely — T_ext right and base wrong on 55**;
5 adjudications remain unresolved (`SPLIT` outcomes mixing `UNSAFE-NONHALT` flags at small magnitudes — suspected
probe boundary/attribution artifacts, same family as the previously root-caused multi-pass effect) `[OPEN]`.
**Direction of the base error: exclusively false-HALT** (never false-non-halt) — decisive for the audit below.

## 3. Blast-radius audit `[each item verified]`
| committed conclusion | affected? | reason |
|---|---|---|
| 97-pass fatal witness + invariant-exclusion meta-theorem | **NO** | verified edge-by-edge by fresh CONCRETE simulation (485 probes), independent of T |
| fatal cells (m≡1, [2,2]) | **NO** | concrete halts, re-verified at 5 magnitudes |
| orbit continuation (122k generations) + epoch/Lean-class test | **NO** | the unsound family needs 2-train ≥ 2; the orbit's max 2-train = 1 EVER (re-verified fresh 50k tower-steps) — never visits |
| depth-56 "no halting itinerary" exhaustive | **NO (a fortiori)** | base errs only toward EXTRA halts; none found even so; with T_ext (fewer halts) the claim is strictly stronger |
| annealed p* upper bounds | **NO** | false HALTs inflate measured halt mass ⇒ computed upper bounds remain valid upper bounds |
| BAD-attractor counts (65,669 words) & fatal-density figures | **CONSERVATIVE** | some BAD classifications may be false-fatal; counts are upper bounds; re-run with T_ext queued |

## 4. Status of the 517 cells
Pinned by the delegation law via `T_ext` (grid + 55/60 adjudications); the residual `[OPEN]` items are the 5
unresolved adjudications and a full T_ext re-run of the adversarial reachability/BAD-attractor analyses
(queued, not blocking: no committed conclusion depends on them in the unsafe direction).
**BOTH RESIDUALS CLOSED (same day, `O18_CLEANUP_2026-07-08.md`):** (i) all 5 SPLITs re-adjudicated with per-pass
isolation at ~34 magnitudes each — **T_ext right 60/60**, single-pass outcomes magnitude-independent, unsafe=0,
(m mod 3, w)-determinism INTACT (the `UNSAFE-NONHALT` entries were multi-pass continuation attribution, as
suspected); (ii) T_ext re-runs: BAD attractor **exactly identical** (65,669, symmetric difference 0 — the
"conservative upper bound" caveat retires as EXACT), the 97-pass witness survives (96/96 edges), T_ext closure has
**0 Unknown cells** (all 517 discharged; 362 reachable HALT cells; 134 base false-HALT cells all traced into the
corrected j=1 region). One documentation erratum: the synthesis note's "55 tail-2-free BAD words" is 37 (first-block
(3,2); qualitative claim intact).

## Verdict
**(b) — the Unknown frontier is pinned by one law; the pinning exposed and corrected a base-tool unsoundness whose
error direction (false-HALT only) leaves every committed conclusion standing; 5 adjudications + the T_ext re-run
remain.** This is the ~45th self-caught correction; the harvest-after-agent-death protocol worked. o15/o18
`[OPEN]`. **No machine decided. No label upgraded.**

## Reproduce
- `o18_md_rules_ext.py` (T_ext; header documents the law + the correction), `o18_r1_enum.py`, `o18_r1_probe.py`,
  `o18_r1_grid.py`, `o18_r1_reach2.py`, `o18_r1_fuzz.py` (adjudication: 55/60 + 5 SPLIT).
- Main-loop re-verifications: the j=1 counterexample (4 magnitudes), the orbit 2-train census (fresh 50k).
- Basis: `O18_MULTIDEFECT_2026-07-07.md`, `O18_INVARIANT_SYNTHESIS_2026-07-07.md`.
