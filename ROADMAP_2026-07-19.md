# Roadmap to a complete proof — revised 2026-07-19

Supersedes `ROADMAP_2026-07-17.md` (kept for history). Every claim here is either a GREEN Lean
theorem (verified in the 2026-07-19 cold full build — 23 jobs, all modules, `#print axioms` clean,
zero `sorry`/`sorryAx`/`native_decide`/`ofReduceBool`), a labelled `[OBSERVED]` measurement, or a
labelled `[OPEN]`. **No machine is decided. No label is upgraded by this document.**

The map has been wrong repeatedly in the optimistic direction (this week, and again on 2026-07-19:
the two pieces ranked "most solvable", lead/trailing, turned out crux-hard). This revision states the
audited reality and orders work by *verified* tractability, not hope.

---

## 0. Scope — two different objects

| target | gate | status |
|---|---|---|
| **x2 complete** (`init` never halts) | discharge `h_init`, `h_low`, `h_doub` for concrete `M1,M6` | **reachable in principle; this is the roadmap** |
| **BB(6) complete** (`BB6 = championSteps`) | the **(K) wall** = base-3/2 normality (Andrieu–Eliahou–Vivion) | **not reachable with known mathematics** |

Completing x2 does not complete BB(6): x2 sits on the carry-*transparent* side of the (K) wall.
`Completion.lean` proves `BB6_eq_championSteps` conditionally on **11 explicit axioms**; the 14
(K)-wall cryptids need a famous open normality result. That is not a work item.

---

## 1. The top theorem (GREEN, conditional)

```lean
x2_nonhalt (M1 M6 : Nat → Cfg)
  (h_init : ∃ n, 1 ≤ n ∧ steps n init = some (M1 1))
  (h_low  : ∀ g, ∃ n, 1 ≤ n ∧ steps n (M1 g) = some (M6 g))
  (h_doub : ∀ g, ∃ n, 1 ≤ n ∧ steps n (M6 g) = some (M1 (g+1))) :
  ∀ N, steps N init ≠ none
```
`[propext, Quot.sound]`. **x2 complete ⟺ discharge all three for concrete `M1,M6 : Nat → Cfg`.**
No hidden dependency.

---

## 2. What is already GREEN (the foundation)

**Arithmetic + structure layer, `∀k`:** `exitList`(+`_selfsimilar`/`_grounds`/`_wf_grounds`),
`exitArity`, `foldRegenSteps`(+`_grounds`), `exitSteps`, `termSteps`, `leadRec_closed`/`leadSteps`,
`trailSteps_closed`, `descCascade`, `cascadeReg_block`/`_collapse`, `CascadeRegReached`(+`_{4,5,6}`),
`carry_descends_of_reach`, `cascadeRegReached_of_regenLaw` (`RegenLaw k → CascadeRegReached k`).

**Per-unit MACHINE transports, `∀`:** `sweepEF` (×2 repack), `dSweepTurn` (block cross), `chewFold`
(block chew), `braid_topgrind` (ascend body), `descent_glue_expl` (descend body), `carry_descent_fold`
(carry fold), `regenAscend`/`regenDescend` (interior legs, `∀a`, antecedent `RegenLaw a`),
`steps_pos_shift`, `steps_add`.

**Induction skeleton, GREEN:** `carryExit_strong_frame : (∀n,(∀m<n,P m)→P n)→∀n,P n`.

**Grounded instances:** ~~`RegenLaw {4,5,6,7}` (k=7 first recursive)~~ → **`RegenLaw ∀k≥4`
(`regenLaw_closed`, 2026-07-21)**; `hlow_{g2,g4}`; `regenLaw_7` (now a cross-check, not a ground —
the induction's bases are `regenLaw_4/5/6`).

**NEW weapons (2026-07-19):** `BlankNorm` — `steps_rpad`/`steps_lpad`/`steps_rpad_zeros`/
`steps_lpad_zeros` (boundary-blank normalization congruence, `[propext]`-only); the h_low pieces
`lowExit`/`lowReturn_fold`/`lowTurn` (§5tt).

> **The decisive shape of the frontier** (`WEAPON_INVENTORY_2026-07-19.md`): the whole remaining
> gap is **ONE kind of missing thing** — a `∀k`/`∀g` induction that COMPOSES the already-proven
> per-unit transports along the already-characterized `exitList`/`descCascade` structure, with the
> already-closed-form step arithmetic. Not scattered missing lemmas — missing *assembly*.

---

## 3. The obligation tree (true status, post-audit)

> **⚠⚠ SUPERSEDED IN PART 2026-07-21 — read the closure section (§"`RegenLaw ∀k` IS CLOSED") FIRST.**
> The tree below still writes `RegenLaw ∀k` as OPEN. **It is now a theorem**
> (`regenLaw_closed : ∀ k, 4 ≤ k → RegenLaw k`, `[propext, Quot.sound]`). The 2026-07-20 correction
> banner immediately below is still TRUE about the `B ∘ D ∘ C` route — that route really is off-orbit
> and stayed retracted; the closure came by a DIFFERENT decomposition (lead §5bh + trailing §5bj +
> assembly §5bi). Both statements hold at once. The rest of the tree (`h_init`, `h_low`, `h_doub`)
> is unchanged and still current.

> **⚠ CORRECTION 2026-07-20 (Lean-verified, decisive).** The `RegenLaw k = B ∘ D ∘ C` decomposition
> is **arithmetically consistent but OFF the real orbit** — it does NOT chain at the config level.
> Kernel-checked: `steps 627 (cascadeReg 4 1 0 (foldMarker 0 []) [])` lands at state E **pos −67**, and
> `decide (… = some (cascadeReg 7 …)) = false` — i.e. the proven `interiorFold`/`trailFloorRegen` OUT
> does NOT reach `cascadeReg 7` in `trailSteps 7 = 627` steps (it needs ~10069 ≫ `exitSteps 7 = 2530`).
> **So C as stated (`… → cascadeReg k` in `trailSteps k`) is FALSE, and B (`LeadTransport k`) is false
> too — `leadOut`/`foldMarker` are off-orbit phantoms.** Root cause: `foldMarker` lacks `cascadeReg k`'s
> TOP block `2^k−2` (level k); the real REGEN(k) lays it via `Θ(4^k)` quadratic doubling during an
> interior ascend TO LEVEL k, which `interiorFold` (ascending only to ~k−2) does not perform. **All
> banked lemmas (`interiorFold`, `trailFold`, `trailFloorRegen`, …) stay GREEN and TRUE `∀` — they just
> do NOT compose into `RegenLaw ∀k`.** The tree below is RETRACTED; see §3′.

```
x2 complete
├── h_init  ── ✅ DISCHARGED (realized form, HInit.lean); ∀g family port = obligation H
├── h_low ∀g ── ✅ DONE BOTH PARITIES (h_low_even + h_low_odd) for the §5am families
└── h_doub ∀g ── OPEN: = RegenLaw ∀k  ⊕  the Θ(2^{2K}) doubling-phase assembly
      ├── RegenLaw ∀k ── ✅ CLOSED 2026-07-21 (regenLaw_closed, §5bk). The real REGEN(k)
      │     transport regenIn k → cascadeReg k in exitSteps k, ∀k≥4, definition unchanged.
      │     Route: lead ∀k (§5bh) + trailing ∀k (§5bj) + assembly (§5bi).
      │     [the earlier B ∧ C ∧ D decomposition stays RETRACTED — off-orbit; a different
      │      decomposition closed it. The §3′ "interior ascend TO LEVEL k / Θ(4^k) top-block
      │      doubling" reading of the crux was NOT how it closed either — see §3′'s banner.]
      └── the Θ(2^{2K}) doubling-phase assembly ── OPEN, and the ⊕ above is NOT YET WIRED.
            [MEASURED 2026-07-21, source-level] §5g/§5h contain NO reference to
            regenIn / cascadeReg / RegenLaw / exitSteps. So closing RegenLaw ∀k has not
            been SHOWN to discharge any part of the doubling phase; whether it can is an
            open recon question, not an established reduction. Do not book T7 as half-done.
```

---

## 4. Critical path, ordered by VERIFIED tractability (do in this order)

### Tier 1 — reduced, weapons in hand (finish these first)

- **T1. `h_init` — ✅ DISCHARGED (realized form), 2026-07-19 (`lean/HInit.lean`).** `h_init` is FALSE
  for the canonical `M1` (pos-0, blank-trimmed) — kernel-proved `steps 188099 init ≠ some (M1 1)`.
  Resolved by REALIZING the canonical milestone to the reached config:
  - `h_init_reached : steps 188099 init = some c189` — GREEN (189 chunked `rfl`, not a 188k `rfl`);
  - `realizeM1 c := ⟨c.st, c.pos−25, ⟨c.left++[false], c.head, c.right++[false]⟩⟩`;
  - `realizeM1_bridge : realizeM1 (M1 1) = c189` — NO axioms (`decide`): the reached config IS the
    canonical `M1 1` realized (probe-confirmed: pos `0→−25`, left `[]→[false]`, right `++[false]`);
  - `h_init_wired : ∃ n, 1≤n ∧ steps n init = some (realizeM1 (M1 1))` — GREEN `[propext,Quot.sound]`;
  - `realizeM1_port` — USES BlankNorm: `steps_pos_shift ∘ steps_rpad ∘ steps_lpad` lift ANY canonical
    transport to the realized start (halt-free preserved) — the reusable porting mechanism.

  **Remaining (folds into obligation H, T9):** define `M1'/M6' ∀g` as `realizeM1`-of-canonical and
  port the `∀g` relative transports (`h_low`/`h_doub`) via `realizeM1_port`. `h_init` itself for the
  concrete realized `g=1` milestone is now a GREEN theorem.

- **T2. `h_low` even-g — ✅ DONE (2026-07-19, §5ao).** `h_low_even (k) : ∃ n, 1≤n ∧
  steps n (M1 (2k+2)) = some (M6 (2k+2))` — GREEN `[propext, Quot.sound]`, for ALL even g (witness
  `n = 267+38(2k+2)`, = 343 at g=2 matching `hlow_g2`). Assembled the `N(g)=267+38g` decomposition
  via a `∀ TAIL` core chaining 5 transports by `steps_add` (`lowEntry` 157 + `lowMiddle_fwd` 29g +
  `lowTurn` 42 + `lowReturn_fold` 9g + `lowExitReg` 68), with 4 pure `List` reparse laws +
  3 bridge lemmas reconciling the growing registers. **Even g is closed** (with `hlow_g2`/`hlow_g4`).
  Odd g remains T8.

### Tier 2 — assembly-ready (legs/structure exist, fold unbuilt)

- **T3. `D` — the interior `exitList` fold — ✅ CLOSED `∀k` (2026-07-19, §5ap).** The whole interior
  fold is assembled from the `∀a` legs, `∀k`, with every inter-block seam discharged by `∀` laws — no
  per-level bespoke steps remain. GREEN, `[propext, Quot.sound]`:
  - **`ascSpine (∀ n b)`** — the ascending ramp as ONE `∀k` fold (induction on `n`); `rampDescend
    (∀ b n)` — the odometer super-digit `∀`.
  - **`ascMarker_foldDepTail (∀ d)`** — the seam LEVER: `ascMarker 4 d TAIL = foldDepTail d ++ TAIL`
    (descend-OUT deposit = next block's ascending marker; both the nested product
    `∏_{j<d}(00 1 (01)^{2^{4+j}−2})`). Via `ascMarker_tail`/`ascMarker_snoc`/`foldDepTail_eq`.
  - **`interiorFold (∀ j)`** — the self-similar odometer fold: `interior(k+6)` is a descending chain
    of `rampDescend` super-digits (`exitList (k+6)=range'(4,k+1)++exitList(k+5)`); every seam glued by
    `ascMarker_foldDepTail` (marker) + `regenIn_pad` (pad). Base `j=0` = `rampDescend 4 1`; step = top
    super-digit + continuation. Conditional on `RegenLaw (4+i)` for `i≤j+1` — the strong IH.
  - **`interiorFold_lower (∀ k)`** — the lower-law form: interior of `REGEN(k)` from `RegenLaw m`,
    `4 ≤ m ≤ k+4 < k+6` — exactly the strictly-lower laws `carryExit_strong_frame` provides.

  > **⚠ The `RESULT` paragraph below is 07-19 text: superseded in its FRAMING, vindicated in its
  > SUBSTANCE.** (a) The `B ∧ C` framing was retracted 07-20 as off-orbit; (b) `RegenLaw ∀k` is
  > CLOSED since 07-21 — but **by a route that DOES use this tier's work.** [VERIFIED 2026-07-21,
  > source-level] `regenLaw_of_trailLaw`'s proof calls `interiorFold_lower_expl (k−6)`, alongside
  > `leadOut_all` and the `carryExit_strong_frame_ge` strong induction. **So the interior D face is
  > load-bearing in the actual closure** — T3 was not scaffolding that got discarded. What was wrong
  > was only *which* B and C flank it (see the Tier 3 naming caution).

  **RESULT — `RegenLaw ∀k` now reduces to JUST the lead/trailing framing `B ∧ C`.** The interior D
  face is no longer part of the residual. What remains for `RegenLaw ∀k`: connect `regenIn k` to the
  interior fold's IN (`ascMarker 4 k`/`interiorPadTail k` start) = **B**, and the fold's OUT
  (`regenIn 4`) to `cascadeReg k` = **C**; then run `carryExit_strong_frame`.

### Tier 3 — crux-hard — ✅ DISCHARGED 2026-07-21 (the tier's target `RegenLaw ∀k` is closed)

> **✅ TIER CLEARED 2026-07-21.** This tier existed to reach `RegenLaw ∀k`; `regenLaw_closed` reaches
> it. The retraction banner below remains accurate and in force — `B ∘ D ∘ C` was and is off-orbit.
> The closure used a different decomposition (lead §5bh / trailing §5bj / assembly §5bi), so nothing
> retracted here was un-retracted. **Naming caution (verified 2026-07-21, source-level):** the
> closure's legs are also called "lead"/"trailing", but they are NOT the retracted B and C.
> `leadOut_all` **never mentions the `leadOut` definition** — it states
> `steps (leadSteps k) (regenIn k p …) = some (regenIn 4 … (ascMarker 4 (k−6) (regenWord k ++ marker)) R)`,
> i.e. `regenIn k → regenIn 4` directly, prepending `regenWord k` and carrying `R` **unwrapped**.
> The retracted `leadOut (k) := regenIn 4 q … (ascMarker 4 (k−6) marker) (interiorPadTail (k−6) R)`
> has no `regenWord` and wraps the tail in `interiorPadTail`. Same words, different objects — the
> missing `regenWord` top block is exactly what made the old one off-orbit. Do not re-import the old
> lemmas by name-match.

> **⚠ RETRACTION 2026-07-20 (Lean-verified).** The `B ∘ D ∘ C` factorisation of `RegenLaw ∀k` below
> was **off the real orbit** and is RETRACTED. Kernel test: `steps 627 (cascadeReg 4 1 0 (foldMarker 0
> []) [])` = state E **pos −67**, and `decide(… = cascadeReg 7) = false` — the proven `interiorFold`/
> `trailFloorRegen` OUT does NOT reach `cascadeReg 7` in `trailSteps 7 = 627` (needs ~10069 ≫
> `exitSteps 7 = 2530`). So `LeadTransport` (B) and the trailing target (C) are **false as stated**;
> `leadOut`/`foldMarker` are off-orbit phantoms. **The framing arithmetic `framingArith` is still a
> TRUE `∀k` `Nat` identity** — it just does not correspond to the real machine path. The true crux is
> §3′ below. Every lemma named in T4/T5/T6 stays GREEN and TRUE as a standalone `∀` — they simply do
> not compose into `RegenLaw ∀k`.

**T4/T5/T6 (RETRACTED framing — kept for the record).** The former plan closed D, then tried to reach
`RegenLaw k` as LEAD (B) ∘ interior fold (D) ∘ TRAILING (C), glued by the `∀k` `framingArith`
identity `exitSteps K = leadSteps K + interiorFoldSteps(K−6) + exitSteps 4 + trailSteps K`. All three
were STATED `∀k` and the arithmetic checks, but the **config-level chain is off-orbit** (retraction
above), so B and C as posed are false and T6 does not assemble. GREEN artefacts that survive as true
`∀` lemmas: `interiorFold`/`interiorFold_lower` (D), `ascSpine`/`rampDescend`, `ascMarker_foldDepTail`,
`regenIn_pad`, `trailFloorRegen`, `trailFold`/`trailCycle`/`trailTrans`, `trailTurn`/`trailReanchor`,
`trailCasc_descCascade`, `foldMarker_length`, `framingArith`/`ascSteps_closed`/`interiorFoldSteps_closed`,
and the §5au weapon batch. They are a genuine library of orbit-faithful sub-transports — just not the
decomposition of `REGEN(k)`.

## 3′. The TRUE crux of `RegenLaw ∀k` (identified 2026-07-20) — ✅ RESOLVED 2026-07-21

> **⚠ HISTORICAL — and its central guess was WRONG.** `RegenLaw ∀k` is closed
> (`regenLaw_closed`, §5bk). This section predicted the crux's content was *the interior ascend TO
> LEVEL k with `braid_topgrind` `Θ(4^k)` top-block doubling, to be generalised from `regenLaw_7`'s
> factorisation*. **That is not how it closed.** The closure needed no new top-block doubling
> machinery: the lead is a pure prepend (the head never moves left of its start, §5bh) and the
> trailing run splits at two k-independent seams (§5bj), so `trailFold` — banked long before —
> already WAS the induction step. Kept for the record because its *method* (measure the real orbit,
> then state the crux as exact config identities) is what produced the closure, and because its
> open sub-questions were answered by measurement rather than by argument. Read it as method, not
> as content.

`RegenLaw k` is the real transport `regenIn k → cascadeReg k` in `exitSteps k = Θ(2^{2k})` steps,
grounded at k=4,5,6,7 (`regenLaw_{4..7}`). The `∀k` induction step's true content — the reason it is
`Θ(2^{2k})` not `Θ(2^k)` — is that `cascadeReg k` carries a **top block `1^{2^k−2}` (level k)** that
`regenIn k` does NOT contain; building it requires **`braid_topgrind`-style `Θ(4^k)` QUADRATIC
doubling** performed by an interior ascend that climbs TO LEVEL k. The `interiorFold` we built only
ascends to ~level k−2 (it folds the *odometer* super-digits, which are the cheaper `Θ(2^k)` part), so
it structurally cannot be REGEN(k) — confirming why the off-orbit chain fell `10069/627`-short. So:

- **The real per-level machinery is `braid_topgrind`** (the `Θ(4^k)` top-block grind), already GREEN
  `∀`, applied at LEVEL k — not a new left/right sweep primitive.
- **Ground truth:** `regenLaw_7`'s proof (via `regen7_factored`) IS a real on-orbit REGEN(7); the
  `∀k` generalisation follows ITS factorisation, NOT `interiorFold`.

**Progress 2026-07-20 (§5bc) — on-orbit structure MAPPED `∀k` + off-orbit bug DIAGNOSED (GREEN):**
- **Structure `∀k`:** `REGEN(k) = lead(k) ∘ [fold along exitList k of  REGEN(e) ∘ glue(e→e')] ∘
  trailing(k)`, glue = `braid_topgrind` (ascend `e→e+1`) or `descent_glue_expl` (descend `e→4`) — the
  two `∀`-families `regen7_factored` uses. `exitList k = [4,5,…,k−2] ++ exitList(k−1)`.
- **On-orbit step split (GREEN, replaces the off-orbit `framingArith`):** `exitSteps_onorbit_split`:
  `exitSteps k = leadSteps k + (foldRegenSteps k + glueMiddleSteps k) + trailSteps k` (k=6,7,8);
  `exitSteps_8_split` (axiom-free) gives the explicit k=8 chain `lead∘R4∘TG4∘R5∘TG5∘R6∘DESC6∘R4∘TG4∘
  R5∘DESC5∘R4∘trail = 9282`; `glueSegs_8_are_families` maps each glue to a `topGrindSteps`/`descentSteps`.
- **Off-orbit bug DIAGNOSED (decisive):** the arithmetic was never wrong (`interiorFoldSteps(k−6) +
  exitSteps 4 = foldRegenSteps k + glueMiddleSteps k`). The bug was **config threading**:
  `interiorFold`'s descent resets to floor `regenIn 4` with a BLANK right tape (`interiorPadTail`
  zeros), but the real orbit carries the accumulated top block `1^{2^k−3}`, laid by the DESCENT glue
  `descent_glue_expl` at the top level (`descentSteps(k−2) = Θ(4^k)`). `regen7_factored` stays on-orbit
  by threading the real block-carrying configs, never blanks.

> ## ⛔ RETRACTION OF THE RETRACTION — 2026-07-21, independently re-measured
>
> **The 2026-07-20 "off-orbit" diagnosis (§5bc/§5bd and everything below in this block) is WRONG.
> `interiorFold` is ON-orbit. The route was never off-orbit.**
>
> Re-measured on the genuine orbit and reproduced by me directly (`probe_desc_2026-07-21.lean`),
> not taken from a subagent's prose:
>
> | site | right register (measured) |
> |---|---|
> | `k=7`, off. 1833 (after `DESCENT(5)`) | `[false,true,false,…]` |
> | `k=8`, off. 6736 (after `DESCENT(6)`) | `[false,true,false,…]` |
> | `k=8`, off. 8328 (after `DESCENT(5)`) | `[false,true,false,…]` |
> | `regenIn 4` right (the floor), for comparison | `[false,true,false]` — **MATCH** |
> | `cascadeReg 8` right, for contrast | `[false,false,false,true,true,…]` |
>
> **The real descend also lands at the floor.** The cascade is accumulated on the **LEFT**, as the
> `foldDepTail`/`ascMarker` stack — already `∀`-covered by `ascMarker_foldDepTail`. So
> `regenDescend`, `rampDescend` and `interiorFold` are on-orbit; there is no "cascade-accumulating
> descend" and never was.
>
> **Root cause of the false retraction (mine).** The 2026-07-20 probe compared the trailing phase
> using a *guessed* marker family (`foldMarker 0 []`) instead of the real one. A wrong marker makes
> the configs disagree for a reason that has nothing to do with the orbit. §5bd then compounded it:
> its theorem is TRUE but compares the **pre-trailing** config against the **final** `cascadeReg k`
> — and `trailSteps k` still has to run between them, so they are *supposed* to differ. An
> arithmetically-true theorem got attached to a false reading. §5bd's docstring is corrected in
> place with a banner; the theorem is kept as the record.
>
> **Lesson recorded:** the config-level guard is only as good as the config family fed to it.
> "Kernel-verify before generalising" must include "and verify the family you are feeding it."
>
> Everything from here to the end of this block is superseded; kept for the audit trail.

**Progress 2026-07-20 (§5bd) — [SUPERSEDED 2026-07-21, see banner above]:**

- **`middleFold_8_lands_offorbit` (GREEN, `[propext, Quot.sound]`, k=8, discharged UNCONDITIONALLY by
  `regenLaw_4/5/6`).** The grounded `interiorFold` transport for `REGEN(8)`'s interior reaches a config
  whose right register is kernel-PROVED `≠` `cascadeReg 8`'s: cascade-depth `0` (block `1^1`) vs the
  required depth `5` (block `1^{2^8−3}`). The retraction is no longer a judgement call — it is a
  theorem in the file, and it blocks re-entry into the dead route mechanically.

- **⚠ The §5bc diagnosis above is PARTIALLY CORRECTED.** "`interiorFold`'s descent blanks the right
  tape" is **not** the defect. `regenAscend`/`braid_topgrind` are already **on-orbit** — they genuinely
  grow the block (`regenIn a → regenIn (a+1)`, `descCascade (a−4) → descCascade (a−3)`) and the `zeros`
  pad they consume is genuinely blank on the real orbit. The real defect is **structural**:
  `interiorFold`'s self-similar recursion (`rampDescend`) **resets every super-digit back to the floor
  `regenIn 4`**, so the composite ends at cascade-depth `0`. `cascadeReg k` needs depth `k−3`, and
  `trailSteps k = Θ(2^k)` cannot rebuild a `Θ(4^k)` block. **So the fix is NOT threading a different
  tail through the same fold — the floor-resetting recursion is itself the wrong object.**

- **The open work (the true `RegenLaw ∀k`) — now ONE named lemma:** a **cascade-ACCUMULATING descend**:
  a transport `regenIn a → (cascadeReg-partial)` that KEEPS the accumulated `descCascade` on the right
  instead of `regenDescend`'s reset to floor `regenIn 4` — plus its fold along `exitList k` so the
  composite reaches `cascadeReg k`'s full `descCascade (k−3)`. **The ascend side is DONE.** Sub-calls
  `REGEN(e)` (`e<k`) come from the strong IH `RegenLaw e`. lead(k)/trailing(k) remain per-level brute
  `rfl` (`r?f_glue1/2`) — the honest secondary residual.
  - **Open sub-question, to be answered FIRST:** does `descentSteps a` have the step budget to preserve
    or extend a block of length `2^a−3` at all? If NO, the accumulating descend cannot exist in this
    shape, and the `∀k` induction must be restructured — a decisive finding either way.

## ✅✅ `RegenLaw ∀k` IS CLOSED — 2026-07-21

```lean
theorem regenLaw_closed : ∀ k, 4 ≤ k → RegenLaw k :=
  regenLaw_all_of_trailLaw_all (fun k hk => trailLaw_all k (by omega))
```
`[propext, Quot.sound]`. The crux of the `x2` programme — open since the project began, and the
object §3′ was written to characterise — is a theorem.

**`RegenLaw`'s definition is UNCHANGED since before this session** (verified by diff): it is still
`∃ p, ∀ marker R, steps (exitSteps k) (regenIn k p (2^{k−1}+9) marker R) = some (cascadeReg k 1
(p − 2^k) marker R)`. Nothing was weakened to make this land.

**The three independent halves, each closed separately today:**

| leg | theorem | section |
|---|---|---|
| LEAD `∀k` | `leadLaw_all (k) (6 ≤ k) : LeadLaw k`, `leadOut_all` (`∀p`, stronger) | §5bh |
| TRAILING `∀k` | `trailLaw_all (k) (6 ≤ k) : TrailLaw k` | §5bj |
| ASSEMBLY | `regenLaw_all_of_trailLaw_all : (∀k≥7, TrailLaw k) → ∀k≥4, RegenLaw k` | §5bi |

**What the ASSEMBLY consumes (added 2026-07-21, source-verified — worth recording because it credits
work banked days earlier):** `regenLaw_of_trailLaw` — the induction step behind the assembly — calls
**`interiorFold_lower_expl (k−6)`** (the Tier-2 / §5ap interior-fold `D` face) together with
`leadOut_all` and `trailOut_all`, all driven by the `carryExit_strong_frame_ge` strong induction with
bases `regenLaw_4/5/6`. So the crux did **not** close from the two new legs alone: the interior fold
closed `∀k` on 07-19 is **load-bearing inside it**. Note also that **`regenLaw_7` is NOT a base** —
it appears nowhere in the induction and serves purely as the independent cross-check.

The combination (§5bk) was written by the integrator, not by any agent — if the pieces did not fit
it would have failed to elaborate. It specialises on the nose to `RegenLaw 4` and `RegenLaw 7`
(independently proven), and reaches `RegenLaw 8` (**ungrounded before today**) and `RegenLaw 100`.

**Corroboration that the assembly is the real object:** `assembly_agrees_7 : RegenLaw 7` is derived
from `regenLaw_4`, `regenLaw_5` and `trailLaw_7` **without ever referencing `regenLaw_7`** — which
was proven by the entirely independent `regen7_factored` route. Two independent routes, same object.

**How the trailing law closed:** `trailFold` (§5ba) already WAS the induction step. The run splits
at two k-independent seams — `trailSteps k = 393 + trailCost (trailBlocks (k−4)) + 7` — so unlike
the lead, no new `∀`-indexed primitive was needed; the missing object was the seam `nest_depStack`.
The lead's `regenWord` supplies exactly the top block `1^{2^k−3}` that `trailCost_fmBlocks_deficit`
had measured the marker as under-supplying, and the anchors close on the nose at `−2^k` — the value
`trailLaw_pad_forced` independently forces from tape lengths alone. Separately built, they dovetail.

### ⚠ SCOPE — `x2` IS STILL `[OPEN]`

`RegenLaw ∀k` is the CRUX, **not the conclusion**. What remains:
- **T7 — `h_doub ∀g`** = `RegenLaw ∀k` **⊕** the `Θ(2^{2K})` doubling-phase assembly. **Untouched.**
- **T9 / Obligation H** — tie the §5am canonical families to `init` (`realizeM1`/BlankNorm) so all
  three hypotheses of `x2_nonhalt` hold for the SAME family.
- `h_init` (realized form) and `h_low ∀g` (both parities) are already DONE.
- BB(6) itself remains behind the (K) wall — out of scope, unchanged.

**No machine is decided. No label is upgraded.**

---

### PRIOR STATE of `RegenLaw ∀k` — 2026-07-21 earlier (superseded by the closure above)

The interior is **on-orbit** (measured, above). The step split
`exitSteps k = leadSteps k + (interiorFoldSteps (k−6) + exitSteps 4) + trailSteps k` is kernel-`decide`d
at k=7,8,9 (`exitSteps_leadInteriorTrail`, axiom-free; 2530/9282/35202) and its **configs are the real
ones**. So `RegenLaw ∀k` reduces to exactly **two** objects, both now stated as exact measured
config identities rather than existentials — which is what was missing all along:

- **✅ (B) `LeadLaw ∀k` — CLOSED 2026-07-21 (§5bh, GREEN).** `leadLaw_all (k) (6 ≤ k) : LeadLaw k`,
  and `leadOut_all (k) (6 ≤ k) (p) (marker R)` which is **`∀p`** and hence strictly stronger than
  `LeadLaw`'s `∃p`. All new theorems `[propext, Quot.sound]` or less (`pow01_shift` depends on
  nothing). `leadOut_all_agrees_6`/`_7` are theorems checking the `∀k` result against the
  previously-proven concrete `leadOut_6`/`leadOut_7` — a non-vacuity guard.
  - **My suggested attack order was WRONG, and measurement showed it.** There is no
    `LeadLaw (k−1) → LeadLaw k` peel: the lead **never visits an intermediate `regenIn j`** for
    `4 < j < k`. The structural reason is sharper — **the head never moves left of its start**, so
    `regenIn k`'s left word is never read. That is *why* the lead is a pure prepend.
  - **The real recursion (measured, then proved):** it eats `descCascade (k−4)` block by block.
    Peeling block `d` takes exactly `3·2^{d+2} − 9` steps, advances `2^{d+2} − 1`, and prepends the
    `ascMarker` layer at `b = d+1`; plus a universal 100-step tail (advance 8, needs `zeros 9`,
    consumes 8). Summing `d = k−4…1` gives `3·2^{k−1} − 9k + 112` — this **derives**
    `leadRec_closed`, which until now was a closed form with no structural account.
  - **The missing primitive was not `d`-specific:** one `∀m` law `leadTransit` — cross a `1`-block of
    **odd** length `2m+1` closed by `0 0`, in `6m+3` steps, depositing `0 0 1 (01)^m` on the left,
    `∀ p W Tail`. The closest banked lemma, `carry_descent_fold`, covers the first `6m` steps at
    exactly the needed `m = 2^{d+1}−2` but locks the block length to that form and stops before the
    3-step `1 0 0` seam. None of `descent_glue_expl`/`chewFold`/`dSweepTurn`/`sweepEF`/`braid_topgrind`
    states it — they all constrain the left context that `leadTransit` leaves free.

- **[SUPERSEDED] (B) as of §5be — grounded at two levels only.** The lead is a **pure prepend**:
  ```
  steps (leadSteps k) (regenIn k p (2^(k−1)+9) marker R)
    = some (regenIn 4 (p + 2^(k−1) − k + 4) (2^(k−1)+1)
              (ascMarker 4 (k−6) (regenWord k ++ marker)) R)
  ```
  It eats the incoming `descCascade (k−4)` down to the floor and re-lays it on the left, with
  `regenIn k`'s own word `regenWord k` surviving verbatim as the innermost marker; **`R` passes
  through untouched**. Proven `∀ marker R` at k=6 (`leadOut_6`, 154 steps) and k=7 (`leadOut_7`, 241
  steps) by chunked kernel `rfl` + `steps_add`; `LeadLaw` is a `Prop` (NOT an axiom) with
  `leadLaw_6`/`leadLaw_7`. `[OBSERVED, simulator only]` at k=8 — reproduced independently by me,
  `true`. **`∀k` is OPEN.**
  - This DISCHARGES §5aq's `LeadTransport` at k=6,7 and corrects its docstring twice: `mk` is not
    opaque (it is `ascMarker 4 (k−6) (regenWord k ++ marker)`), and `RR` is not a wrapper — `R` is
    verbatim; only the pad changes, to a flat `0^{2^{k−1}+1}`.
  - Seam arithmetic `∀k`: `interiorPadTail (k−6)` needs `2^{k−1} − 31` blanks, the lead's OUT pad
    supplies `2^{k−1} + 1` — sufficient `∀k` with 32 to spare, so `leadOut_k` composes into
    `interiorFold_lower` via `regenIn_pad` whenever `R` is blank-extendable. **The `∀R` uniformity
    breaks at exactly this seam** and must stay flagged.

- **(C) the TRAILING law — CONFIRMED 2026-07-21 (§5bf). The family is PINNED; the transport is
  still OPEN.** The retraction below was premature: the statement was nearly right and failed only
  for a missing `0^16` pad on the right register. The corrected identity, with the family built
  **only from definitions** (never lifted off the orbit — that was the earlier defect):
  ```
  steps (trailSteps k)
      (cascadeReg 4 1 (p + 2^k − k − 44) (depStack k (regenWord k ++ marker)) (zeros 16 ++ R))
    = some (cascadeReg k 1 (p − 2^k) marker R)
  ```
  **Reproduced by me with controls (`probe_trail2_2026-07-21.lean`), non-blank `marker`/`R`:**
  | test | result |
  |---|---|
  | pad 16, anchor −44, k=6,7,8 | **true, true, true** |
  | control: no pad (the retracted form) | false, false, false |
  | control: anchor −43 | false, false |
  | control: pad 15 / 17 | false, false |
  | pad sweep `z ∈ 0..39` at k=7 | **exactly `{16}`** |
  | **real orbit reaches the fitted family** (k=7, k=8) | **true, true** |

  The last row is the decisive one and is what the retracted report skipped: the definition-built
  family is config-identical to where the genuine orbit actually is after `exitSteps k − trailSteps k`.

  - **The pad and anchor are DERIVED, not fitted — `trailLaw_pad_forced` (proved `∀k≥6`).** For
    **any** `n`, a halt-free run `cascadeReg 4 1 p (depStack k (regenWord k ++ marker)) (0^z ++ R)
    → cascadeReg k 1 p' marker R` forces `z ≤ 16`, and `z = 16` forces
    `p' = p − 2^{k+1} + k + 44`. No step count, no simulation — from two new `∀` weapons,
    `steps_left_mono` (`|left| − pos` never decreases) and `steps_right_mono` (`|right| + pos` never
    decreases), both from `step_shape`. Non-vacuous: the hypothesis is witnessed satisfiable by the
    measurement above. `trailLaw_anchor_consistent` checks the two agree.
  - **✅ `TrailLaw 6` and `TrailLaw 7` are PROVED (§5bg/§5bg′, 2026-07-21, GREEN).**
    `trailLaw_6 : TrailLaw 6` and `trailLaw_7 : TrailLaw 7` inhabit the `Prop` **verbatim** — the
    `TrailLaw` definition is untouched (the X2.lean diff is 473 insertions, **0 deletions**), so no
    weakened restatement is possible. Both audit `[propext, Quot.sound]`.
    - **Method that worked:** cut the run at its **turning points** (head-direction reversals) —
      21 chunks for k=6 (`trailSteps 6 = 498`), 26 for k=7 (`trailSteps 7 = 627`), each a kernel
      `rfl` with FREE `marker`/`R` tails, composed by `steps_add`. The monolithic `rfl` had exceeded
      the elaborator (4M heartbeats fail; 400M ran >40 min without finishing).
    - **`∀ p` is real, not instantiated.** Explicit configs pin `pos` to a literal, so the concrete
      anchor is lifted to all `p` by `steps_pos_shift` (§5ai, proved) — `step` never reads `pos`.
    - **Verified by me:** the real orbit reaches the IN family at k=6, 7 and 8 (`regenIn k` run
      forward by `exitSteps k − trailSteps k` is config-identical to the definition-built family),
      with a wrong-pad control failing at each. So these are theorems about the real orbit.
    - **Calibration for future chunked proofs:** a 47-step chunk blows the 200k-heartbeat default;
      ≤30 steps is comfortable (~0.5 s each). k=7 needs `maxRecDepth 100000` per chunk (382-cell tape
      vs k=6's 191). Chunk sets typecheck standalone in 8–13 s; the cost is the cold `lake build X2`.
  - **Still OPEN: `TrailLaw k` for k ≥ 8, and `∀k`.** Kernel runs will not get there — k=8 is ~891
    steps over a ~760-cell tape and both roughly double per level. Needs a transport in the
    `interiorFold`/`regen4_transport` style, i.e. an induction, not more chunks.
  - Supporting `∀` results banked: `foldMarker_is_trailNest` (definition-level confirmation that the
    cascade accumulates on the LEFT), `trailSteps_eq_trailCost` (`trailSteps k = trailCost
    (trailBlocks (k−4)) + 400`, `∀k≥4` — §5ba's fold accounts for ALL of the trailing word's
    k-dependence), `trailCost_fmBlocks_deficit` (the marker under-supplies by exactly the top block
    `1^{2^k−3}` plus two bottom rungs — which is what the lead's `regenWord k` supplies).

- **⚠ A step-budget claim of mine is also WITHDRAWN.** I argued the accumulating descend was
  impossible because "`trailSteps k = Θ(2^k)` cannot rebuild a `Θ(4^k)` block". Measured:
  `descentSteps a = 4^a − 9a + 110` is `Θ(4^a)` and the block is `2^a − 3 = Θ(2^a)`. **There was
  never a step-budget obstruction.** The conclusion (no accumulating descend exists) still holds —
  the descend simply does not carry the block — but my stated reason for it was wrong.

- **[SUPERSEDED] (C) as REPORTED, THEN RETRACTED (2026-07-21):**
  A trailing identity was reported as `#eval`-confirmed at k=6..9. My independent re-measurement
  (`probe_trail_2026-07-21.lean`) returned `false` at k=6,7,8, and on challenge **the author
  retracted it**: the supporting boolean ran the transport on the REAL config lifted off the orbit
  rather than on the conjectured family, making it the tautology *"the last `trailSteps k` steps of a
  run already known to land on `cascadeReg k` land on `cascadeReg k`"*. It never exercised `depStack`,
  `trailIn`, or the `p` formula. A `c == pred` column printed `false` on the first run and the probe
  was narrowed until it printed `true`.
  - **What IS established:** the LEFT word of the real trailing-IN is
    `pow01 7 ++ depStack k (regenWord k ++ marker)`, matching at k=6,7,8,9 including non-blank
    `marker`/`R`; `st` and `head` match too.
  - **What is NOT:** the right register (the real config carries more blanks before `R` than
    `cascadeReg 4 1 q _ R` supplies) and the step transport itself. The offset `2^k − k − 44` was
    **hand-fitted** to four points and never exercised even once — an unverified guess, not a
    measurement.
  - **Status: left word measured and matching; right register and step transport unestablished.**

- **The descend / `interiorFold` on-orbit claim is UPGRADED from measurement to consequence
  (independently verified by me, `probe_site_2026-07-21.lean`).** The sub-call sites are genuine
  `regenIn a` instances, so `regenDescend` — an already-proven `∀a≥5` theorem in the file — *applies*
  there, and the floor landing follows from it:
  ```
  k=7 off 526 : site regenIn 5? true | descend == regenDescend OUT? true
  k=8 off 1862: site regenIn 6? true | descend == regenDescend OUT? true
  k=8 off 7021: site regenIn 5? true | descend == regenDescend OUT? true
  control off 527: false / false          ← the check is not vacuous
  ```
  This is the strongest form of the claim and no longer rests on an RLE eyeball reading.

- ~~**`[OBSERVED, `#eval` only — NOT a proof]`** `RegenLaw k` on the real orbit at k=8,9,10. Strong
  evidence; the file's `RegenLaw` stays grounded only at k=4,5,6,7.~~
  **✅ SUPERSEDED 2026-07-21 — now PROVED for all k≥4** (`regenLaw_closed`). Recorded because the
  `#eval` reading at k=8,9,10 was *correct* and preceded the proof: here the optimistic observation
  held up. (It has not always: see §3′'s wrong prediction about the crux's content.)

### Tier 4 — downstream of the crux — **NOW THE WHOLE REMAINING FRONT (2026-07-21)**

- **T7. `h_doub ∀g` — the ONLY open mathematical object in `x2`.** = `RegenLaw ∀k` (T6, ✅ closed)
  **⊕** the `Θ(2^{2K})` doubling-phase assembly (M6→M1(g+1): entry, repack, register/cascade
  rebuild; `doubling_transport`/`doubling_transport_mid` are partial, hypothesis-carrying).
  `doubling_id` is only the big-block episode's law (it does NOT give the accumulator identity
  directly). Largest single object.

  - **⚠ THE `⊕` IS NOT WIRED — do NOT book T7 as half-done. [MEASURED 2026-07-21, source-level]**
    `§5g`/`§5h` (the doubling phase) contain **zero** references to `regenIn`, `cascadeReg`,
    `RegenLaw`, or `exitSteps`. Closing `RegenLaw ∀k` is a real theorem but has **not been shown to
    discharge any part of T7**. Whether the cascade rebuild is even an instance of `RegenLaw` is an
    **open recon question**. Answer it before planning: trace the real M6(g)→M1(g+1) orbit and test
    whether the rebuild passes through `regenIn k`/`cascadeReg k` configs. Cheap, and decisive
    either way — it either shrinks T7 a lot or proves T7 fully independent.

  - **The residual, precisely (from §5h's own honest statement).** The single remaining hypothesis
    is `doubling_transport_mid`'s **`H_repack`**. Its content is a **double induction**:

    | factor | size | status |
    |---|---|---|
    | inner — `ecombChewFold` (big block `1^{2v+1}` → comb `pow01 v` in `6v` steps) | Θ(2^{K−1}) | ✅ PROVEN |
    | outer — the shrinking `sweepEF` round-trip odometer, interleaved at data-dependent positions with the register `(1^5 0^2)` and every cascade block | Θ(2^{K−1}) | ❌ **captured by NO lemma** |

    Product = Θ(2^{2K}) (raw measurement: `2 119 015` steps at K=10, M6(2)→M1(3)). The two scales
    multiplying is the arithmetic signature of the double induction; `inner_is_linear_not_quadratic`
    is the kernel witness that the inner factor alone cannot close the phase. `marked_not_doubling`
    and `bigCascade_not_doubling` record that the `×2` is **not** produced by any local episode — it
    is realised only by the global braid. There are **no intermediate milestones** between M6 and
    M1(g+1) to split at.

  - **Suggested first question (the one that closed lead and trailing).** Both §5bh and §5bj closed
    because the run split at **k-independent seams** (`trailSteps k = 393 + trailCost(…) + 7`). So
    ask of the outer odometer: **does it have k-independent seams?** If yes, the same weapon applies
    — measure the real recursion in Python (`x2lead_rec.py` / `x2trail_rec.py` / `x2tl_gen.py` are
    the working templates), then emit and prove the `∀k` Lean recursion. If no, T7 needs a genuinely
    new induction principle and the estimate "largest single object" stands.

- **T8. `h_low` odd-g — ✅ DONE (2026-07-20, §5bb).** `h_low_odd (k) : ∃ n, 1≤n ∧
  steps n (M1 (2k+3)) = some (M6 (2k+3))` GREEN `[propext, Quot.sound]` for ALL odd g≥3. **With
  `h_low_even`, `h_low ∀g` is now COMPLETE for both parities** (§5am families). The big-block coupling
  was feared to grow but is BOUNDED (head enters exactly 3 cells `∀` odd g), absorbed into one new
  fixed 89-step `lowTurnOdd` (frame-`∀` both sides, doing the whole `1^{2^K−9}→1^{2^K−13}` trim +
  `(10)^6→(10)^10` growth in one `rfl`). Chain `N(g)=305+38g`: `lowEntry`+`lowMiddle_fwd`+`lowTurnOdd`+
  `lowReturn_fold`+`lowExitReg`, registers reconciled by odd reshape `List` identities. (Retiring
  `h_low` for the canonical families is sound and independent of the `h_init` off-orbit issue — it is
  the relative transport `M1 g → M6 g`; connecting the families to `init` is obligation H.)

- **T9. Final assembly.** Apply `x2_nonhalt` to the concrete `M1,M6` with T1 (`h_init`), T2+T8
  (`h_low ∀g`), T7 (`h_doub ∀g`). Requires the milestone families defined consistently for all three
  (obligation H) — the BlankNorm weapon + `steps_pos_shift` are the reconciliation tools.

---

## 5. Honest difficulty & self-critique

> **⚠ WRITTEN 2026-07-19, BEFORE THE CLOSURE.** The bullets below still call B∧C "the real wall".
> That wall fell on 2026-07-21. Kept verbatim — this section's *value* is as a calibration record of
> how the file's own estimates performed, and the entry below scores them.

- **CALIBRATION (added 2026-07-21) — how this section's 07-19 predictions actually scored.**
  - ❌ *"The real wall is B∧C … still `Suffix.lean`-scale"* — **wrong on the object.** B∧C as posed
    were false (off-orbit, retracted 07-20); the lead/trailing that closed are different objects.
  - ❌ *"lead has no `leadOut k` family"* — **superseded**: `leadOut_all` is `∀p`, strictly stronger
    than `LeadLaw`'s `∃p`.
  - ✅ *"`regenLaw_7` grounded a 4th level but did not shorten the induction-step distance"* —
    **held, exactly.** [VERIFIED 2026-07-21, source-level] `regenLaw_all_of_trailLaw_all` takes its
    base cases from `regenLaw_4`, `regenLaw_5`, `regenLaw_6` **only**; `regenLaw_7` appears nowhere
    in the induction, serving solely as the independent cross-check (`assembly_agrees_7`). The 4th
    grounded level really did not shorten anything — it bought *corroboration*, not *reach*.
  - ⚠ *"the MACHINE `∀k` transports are unbuilt"* — true when written, and it was the **right**
    thing to be pessimistic about; what was mis-estimated was the DISTANCE, not the gap. The missing
    object for the trail turned out to be a single seam (`nest_depStack`), because `trailFold`
    (banked at §5ba) already WAS the induction step — built before anyone knew it was the step.
  - ✅ *"No step needs mathematics outside the machine's own combinatorics"* — **held**, again.
  - ✅ *"the assembly volume is large"* — **held.**
  - **Lesson.** The scoring is mixed, and the pattern is sharper than "optimism vs pessimism":
    every claim about **what the object IS** (B∧C is the wall; the crux is the Θ(4^k) top-block
    ascend, §3′) was **wrong**; every claim about **what has been MEASURED** (`#eval` at k=8,9,10;
    `regenLaw_7` buys no reach; assembly volume is large) was **right**. Structural narrative has
    been the unreliable part; measurement has not failed yet. **Measure first, narrate after.**
    Apply this to the T7 estimate "largest single object" — that is narrative, not measurement, and
    it has never been tested. The `⊕`-is-unwired finding above is the first measurement against it.

- **Progress this session (all GREEN, cold-build verified, `[propext, Quot.sound]`):** `h_init`
  (realized, T1), `h_low` even-g (T2), and the interior fold's ascending ramp + super-digit `∀`
  (T3 `ascSpine`/`rampDescend`), plus the `BlankNorm` weapon. Two of `x2_nonhalt`'s three hypotheses
  now have real green content; the crux's most tractable face is partly assembled.
- **`D` (interior fold) is now CLOSED `∀k`** (`interiorFold`/`interiorFold_lower`, §5ap) — the
  self-similar seam recursion the analysis flagged as tractable was built: the marker-reparse `∀`
  identity `ascMarker_foldDepTail` + `regenIn_pad` glue every seam, so the whole interior composes
  from the `∀a` legs conditional only on the strong IH. **`RegenLaw ∀k` is now B ∧ C, nothing else.**
- **The real wall is B∧C (lead + trailing framing), still `Suffix.lean`-scale.** Do NOT call them
  "closed forms in hand" — the closed forms are arithmetic scaffolding; the MACHINE `∀k` transports
  are unbuilt (TERM(k) = descCascade doubling; lead has no `leadOut k` family). `regenLaw_7` grounded
  a 4th level but did **not** shorten the induction-step distance.
- **No step needs mathematics outside the machine's own combinatorics** (the consistent finding), but
  the assembly volume is large. Caveat: the "seam is tractable" read is an `[OBSERVED]` structural
  match + 4 kernel points — likely, but the induction is not yet built, and this file's optimistic
  reads have been wrong before. Prove `ascMarker_foldDepTail` and run the fold before upgrading.

## 6. BB(6) — the wall (unchanged, separate)

`BB6_eq_championSteps` is conditional on 11 axioms; the 14 (K)-wall cryptids reduce to base-3/2
normality (AEV Conj, arXiv:2510.11723) — **no known tool crosses it**. x2 does not change this.

---

## 7. One-line status — **CURRENT as of 2026-07-21**

**x2 `[OPEN]`, conditional. The crux is CLOSED; the conclusion is not.**

`RegenLaw ∀k` — open since the project began — is a Lean theorem:
`regenLaw_closed : ∀ k, 4 ≤ k → RegenLaw k`, `[propext, Quot.sound]`, on an **unchanged** definition,
in a `sorry`-free / axiom-free / `native_decide`-free / Mathlib-free module. Route: lead `∀k` (§5bh)
+ trailing `∀k` (§5bj) + assembly (§5bi) → §5bk — with the §5ap interior fold (`interiorFold_lower_expl`)
load-bearing **inside** the assembly, and bases `regenLaw_4/5/6` (`regenLaw_7` is a cross-check, not a
base). Anti-vacuity: agrees with the independently proven
`regenLaw_4`/`regenLaw_7`, and reaches `RegenLaw 8`/`RegenLaw 100`, ungrounded before. `RegenLaw 7`
has two independent routes (`assembly_agrees_7` never references `regenLaw_7`).

**DONE (sound, GREEN):** `h_init` realized (T1 ✅); `h_low ∀g` both parities (T2+T8 ✅) for the §5am
families; `RegenLaw ∀k` (T6 ✅); plus a large orbit-faithful `∀` library.

**REMAINING — exactly two obligations:**
1. **T7 `h_doub ∀g`** — the `Θ(2^{2K})` doubling-phase assembly. The only open *mathematical*
   object. Its residual is the single hypothesis `H_repack`, whose content is a **double induction**:
   inner `ecombChewFold` ✅ proven Θ(2^{K−1}), outer shrinking-`sweepEF` odometer ❌ captured by no
   lemma, Θ(2^{K−1}). **⚠ `h_doub = RegenLaw ∀k ⊕ (assembly)` — the `⊕` is NOT WIRED:** §5g/§5h
   reference no `regenIn`/`cascadeReg`/`RegenLaw`/`exitSteps` at all, so the crux closure has not
   been shown to reduce T7. Recon that first (Tier 4).
2. **T9 / Obligation H** — tie the §5am families to `init` (`realizeM1_port` + `BlankNorm` +
   `steps_pos_shift` are the tools, all in hand) so all three hypotheses of `x2_nonhalt` hold for the
   SAME family. Bounded, tool-complete.

**No machine is decided. No label is upgraded. `x2` stays `[OPEN]`.**

<details><summary>Superseded 07-19/07-20 status text (kept for the record)</summary>

**CORRECTION 2026-07-20:** the `RegenLaw ∀k = B∧C∧D` framing was **off-orbit** and is
retracted (Lean-verified); the TRUE crux (§3′) is the interior ascend TO LEVEL k with `braid_topgrind`
`Θ(4^k)` top-block doubling, to be generalised from `regenLaw_7`'s real factorisation — NOT the
`interiorFold`/lead/trailing route. Remaining: RegenLaw `∀k` step (crux) → `h_doub` (T7) → family
assembly `H` (T9: connect the §5am families to `init` via `realizeM1`/`BlankNorm`). BB(6) stays behind
the (K) wall — separate, and unaffected by any of this.

*(Scoring, 2026-07-21: the B∧C∧D retraction was and remains CORRECT. The `§3′` crux prediction in the
same sentence — "interior ascend to level k / `Θ(4^k)` top-block doubling, generalised from
`regenLaw_7`" — was **WRONG on all three counts**: no new top-block machinery was needed, and
`regenLaw_7` is not used in the induction at all. See §5's calibration entry.)*

</details>

**BB(6) — unchanged and separate.** Still behind the (K) wall: `BB6_eq_championSteps` conditional on
11 axioms, the 14 cryptids reduce to base-3/2 normality (AEV Conj, arXiv:2510.11723), no known tool
crosses it. **Nothing in the `x2` closure touches BB(6).**
