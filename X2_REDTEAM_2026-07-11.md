# Red-team of the integer-×2 non-halt claim (2026-07-11)

Adversarial attack on `M = 1RB0RE_1RC---_0LD1LE_0RE1LD_1RF0LC_0RA1RE`. Default posture:
assume the non-halt claim of `X2_CARRY_CALCULUS_2026-07-11.md` is WRONG; try to expose a
halt, an odd/gap-3 carry, or a lemma failure. Interpreter
`/Users/aokiyousuke/quantum-ecc/.venv/bin/python`, exact big-int. Scripts `x2rt_*.py`.
STRICT: only what was actually found. Not committed.

## Verdict

**No break found. No `[CANDIDATE HALT]`. No `[UNSOUND]`.** Every attack was exhausted to
the stated depth and produced adversarial SUPPORT (not proof) for non-halt. The specific
failure modes named in the task — a gap-3 reaching E, an odd-length carry, a lemma failure,
or an accelerated-sim mismatch — did **not** materialize anywhere in the tested envelope.

## 1. Accelerated-simulator re-validation (the foundation) — HELD

The prior claim rests entirely on `x2cc_fast.py` being step-exact. Its own `--validate`
only compares `(step,pos,state)` at checkpoints plus the final 1-count — it never compares
tape interiors, so a bulk-write bug that leaves head/state correct but corrupts cells would
pass. `x2rt_validate.py` re-derives the transition table independently from the spec string
and compares the **entire written tape region `[lo..hi]`** raw-TM vs fast at **200
checkpoints to 1e8 steps**: **0 mismatch, FULL-TAPE VALIDATED step-exact**. The foundation
holds under a strictly stronger test than the original. (`x2cc_fast --validate 1e7` also
re-passes.)

## 2. Large-g gap hunt — no gap-3, no odd gap, no new value (`x2rt_gaphunt.py`)

Ran the validated accelerated sim recording every E-met gap `L>=3`, flagging any `L==3`,
any odd `L`, any `L` outside `{6,10,18}`, and direct halt (B@1).

- Reached **g=14 at 3.75e11 steps (240s)**; a deeper 1500s run reached **g=15 at 2.32e12
  steps** — still no gap-3, no odd gap, no halt; mature pattern perfect through gen#15.
- Mature multisets from gen#5 on: **exactly `{10,18}` (odd g) / `{6,10,18}` (even g)** — the
  claimed period-2 pattern, verbatim, every generation.
- Transient gens 1–4: `{4}, {8}, {6,14}, {6,14,22}` — reproduces Route A's historical
  multisets exactly; **all values EVEN**, none is 3.
- **gap==3 events: 0. ODD gaps: 0. Machine halts: 0.**

The task's g=20/30/50 targets are **physically unreachable** with this simulator: the big
block is `2^(g+8)` cells, so the tape itself caps g near ~18–19 regardless of time (an
honest limitation, not a skipped attack).

## 3. Odd-carry structural scan — register is all-even (`x2rt_oddrun.py`)

Stronger than "E never *reaches* a gap-3": at every milestone, scanned the **entire written
tape** for any maximal 0-run of **odd length ≥3 bounded on both sides by 1**. Across **90
milestones through g=14 (1.9e11 steps): ZERO**. The register contains only even 0-runs plus
length-1 comb separators — there is no *latent* gap-3 or odd carry anywhere on the tape, not
merely one unreached by E. This is the sharpest support for the "no odd carry" reduction.

## 4. The halt gate itself is correctly a gap of exactly 3

Directly simulated E entering an isolated gap. **A `0^3` immediately followed by a 1,
entered by E from the left, halts in exactly 3 steps regardless of context** (E,F,A consume
the three 0s; B lands on the bounding 1 → B@1 = HALT). So the `gap-3 ⟹ halt` reduction is
robust and context-independent — the correct and only fatal target. (Other lengths halting
in the toy `1 0^L 1^6` probe are boundary artifacts of the fake context; in their real
register contexts L=2/6/10/18 are the safe, observed events. The sim detects halt *directly*
via B@1 independent of any gap classification, and never did.)

## 5. Lemma stress test — clean past the certified range (`x2rt_lemmas.py`)

Independent brute micro-stepper (does not import the prover):
- **R/L/D closed forms re-certified to length 200** (prior certification: ≤40): all exact,
  no gap-3, no odd gap.
- **Chew-body lemma** `(01)^k [D] 0^3 1^(2r+5) → (01)^(k+1) [D] 0^3 1^(2r+3)`: brute-checked
  over k∈[1,60), r∈[0,60) — end-state D, **no gap-3, no odd gap**.
- **Separator crossing** `[D] 0^3 1^3 0^2 1^(2s+5) → … 1 [D] 0^3 1^(2s+3)`: k∈[1,40),
  s∈[0,60) — **no gap-3, no odd gap**.
- Prover self-certs `check_rules` + `check_fold` re-pass.

Symbolic side (`x2cc_gencheck.py`, which raises `Halted` on any gap-3): independently
reproduced **g=2 (`{6,10,18}`), g=3 odd (`{10,18}`), g=4 even (`{6,10,18}`)** — each with
M1(g+1) template EXACT and **no halt**.

## 6. Adversarial-support envelope (depth reached)

| attack | depth | result |
|---|---|---|
| full-tape sim validation | 1e8 steps, 200 checkpoints | 0 mismatch |
| direct halt / gap-3 hunt | g=15 (2.32e12 steps) | no halt, no gap-3 |
| odd-run structural scan | 90 milestones, g=14, 1.9e11 steps | 0 odd runs ≥3 |
| R/L/D lemma brute-cert | length 200 | exact |
| chew / separator episode lemmas | k≤60, r,s≤60 | no gap-3 |
| symbolic per-generation | g=2,3,4 (both parities) | EXACT, no halt |

## Honest conclusion

Every attack failed to break the claim. This is **adversarial support, NOT proof** — the
open core (the symbolic composition of the doubling-phase episodes, ∀g) is untouched by
empirical search, and depth is hard-capped near g≈18 by the `2^(g+8)` tape. But within the
tested envelope the machine shows no halt, no length-3 gap ever reaching E, no odd carry
anywhere in the register, and no lemma failure. The framing is sound (gap-3 ⟹ halt is
robust) and the accelerated evidence it rests on is re-validated under a stronger test.

No machine decided. No label upgraded.
