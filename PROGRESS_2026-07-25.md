# BB(6) complete proof — consolidated progress & information map (2026-07-25)

The single organizing document for the complete-proof program after the 2026-07-25 session.
Supersedes the scattered status notes for orientation; the cited docs remain the detail of record.
**No machine decided. No label upgraded.**

---

## I. The goal and its structure

**Target:** a complete proof of BB(6) = decide the halting of all 17 remaining BB(6) cryptid
Turing machines (the value is otherwise pinned). The 17 split, by proof character, into TWO
disjoint classes:

| class | machines | proof character | our status |
|---|---|---|---|
| **(K) class** | ~14 (o4, o7, SN, o17, Antihydra/H₂, …) | each reduces to single-orbit base-3/2-type digit normality = **Mahler 3/2 = AEV Conj 1.6** (OPEN external math) | **blocked at (K)** |
| **template class** | ~3–8 carry-transparent (x2, …) | finite exact transport + `∀g` induction, formalizable in Lean | **in progress (x2)** |

The two classes are the whole difficulty split: (K) is external open mathematics; the template
class is finite engineering. **The complete proof of BB(6) requires cracking (K); nothing in the
template class touches it.**

---

## II. The (K) wall — the real blocker — is CLOSED to every internal route

This session fired the last genuinely-untried (K) attacks (ATTACK_CATALOG §IV routes) against the
o4-first scalar `freq{ρ=1}(43) < 4/5` (the program's weakest open (K) statement). All closed.

### The unifying fact (re-verified this session)
`T(G) = (4G + e(G mod 3))/3`, `e={0:9,1:14,2:1}` is a **single expanding map of multiplier 4/3
plus a locally-constant additive cocycle**. Furstenberg/Rudolph/Host rigidity — the only machinery
that pins a single orbit's digit frequency — needs TWO multiplicatively-independent maps. Pinning
φ(43) ⟺ manufacturing a second such structure. The three sources of one are each dead:

| source | route | mechanism [PROVEN, re-verified 10⁵ steps] |
|---|---|---|
| the analytic bound itself | #30 | **margin ≠ provability**: φ=1 realized at fixed point −14 ⟹ no seed-uniform bound; seed-specific = quenched digit freq = (K) |
| the 2-adic side (×2) | #17 | 2-adic data is a **subordinate factor**: `parity(G_{n+1})=[ρ_n=1]`, `v₂∈{0,1}`, `v₂=1⟺ρ_{n−1}=1`. No 2-adic weapon bites |
| a transport target | #16 | only exact transport is the **base-3 digit skew-shift** `T(G)=4(G//3)+s`, `s=(3,6,3)`; its target is the FULL shift (no per-point frequency) |
| algebraic symmetry | #21 | affine-automorphism group is **trivial**; only Koenigs self-conjugacy `A(43)=49.9345…`, `A∘T=(4/3)A`, frequency-inert |

Detail: `O4_KWALL_CLOSURE_2026-07-25.md`. Sharpest restatement (banked, #16):
**o4 (K) = "is the base-3 expansion of the ×(4/3)-orbit of 43 simply normal at digit 1?"**

### Consequence
The prior FEASIBILITY verdict "unconditional BB(6) is not reachable by this program" is upgraded
from an **impression to a mechanical consequence**: (K)'s last internal routes are closed by proven
no-gos, on top of the pre-existing single-map multifractal obstruction (`bb6-frontier-state`
memory: ~20 prior routes, all reduce to (K); framework package + No-Structure theorem + obstruction
dichotomy). What could still touch (K) is a NEW external AEV/normality theorem — which the program
cannot manufacture internally and (owner policy) does not pursue via outside contact.

**Bottom line: the complete proof of BB(6) is blocked at (K), and (K) is now demonstrably — not
just believed — closed to everything internal.**

---

## III. The template class — x2 — where green theorems remain

x2 (`1RB0RE_1RC---_0LD1LE_0RE1LD_1RF0LC_0RA1RE`) is the lead template machine. Its non-halting is
`x2_nonhalt` with FREE families M1/M6 (so obligation H = "define one family," a weak obligation).
Build: **59/59 GREEN, 308 T7 theorems, all `[propext, Quot.sound]`.**

### GREEN (end-to-end, ∀)
- head descent, ladder, top rung, seam, tail (`descLaw/headLaw/ladderToCascade/topRung/seam74/tailLaw`)
- **even doubling phase** `doubPhaseEven` (M6→M1-frame, ∀h) — the full even branch
- **odd topEntry** `topEntryOddFull` (the Θ(2^g) odd carry fold `carryFold46`, ∀)
- low phase both parities; H lower bound `hlow_j_ge` (j≥10); `cascadeReg_topgrind_Lc` (∀Lc topgrind, NEW today)

### The odd branch — precisely structured this session (R1)
The only remaining dynamics were the odd top→tail seam. Now fully measured:
- **odd top rung is a clean `cascadeReg k 6`** — Lc=6 is a **g-independent constant** (g=1, g=3
  both; even and all internal ladder rungs are Lc=1). No "oddPre reconciliation" (that was a
  misreading). The whole odd-specific difference is `Lc: 1 → 6`.
- topgrind reuses at Lc=6 verbatim (`cascadeReg_topgrind_Lc` GREEN).
- **CORRECTION (measured):** the exit REGEN is NOT Lc-inert — post-topgrind the `pow10 6` puts an
  `(01)^5` comb into the exit's 5-cell read window (`0 1 0 1 0 1…` vs even `0 1 0 0 1…`), so
  `regenLawGen_closed` does not fire. The odd exit REGEN + seam are comb-aware odd-specific
  episodes, not a mechanical Lc thread.

### x2 remaining task list (all measured; no new mechanism)
1. **`oddExitRegen`** — comb-aware `∀`-generalization of the exit REGEN (reads `false::true::(01)^5::…`).
   E1-exit-scale ∀-theorem. NEXT: measure the odd exit's landing form (is it a regular
   comb-carrying `cascadeRegGen` variant?) before designing the lemma.
2. **`oddSeam`** — the bounded tail episode (M3′, 3 chunked-rfl tiles; endpoints in `x2r1_oddseam.py`:
   20-step odometer, head span 11, lands on tail-top `0^13 1^4093 0^2 1^2045 0^2`).
3. **`oddTopRungToMilestone` → `oddSpine` → `doubPhaseOdd`** — M8 assembly (mirror of even).
4. **H upper bound** (`R5`): pin j=10 (arithmetic done, needs `steps_pos` invariant or ++LL port).
5. **Obligation-H assembly** — `hlow16 ∘ doubPhase{Even,Odd}` = M1(g)→M1(g+1), both parities.
6. **F** — `exact x2_nonhalt …`, cold build, `#print axioms` audit, red-team.

**Scale:** items 1–3 are the odd mirror of even E1/E2/E3 (~1–2 sessions); item 4 is one lemma;
5–6 are assembly + audit. Closing x2 decides ONE machine and validates the template for the
carry-transparent island (~5–8 machines). It does **not** touch (K).

---

## IV. Reachability verdict (organized)

| objective | reachable by this program? | basis |
|---|---|---|
| **x2 non-halt** (one machine) | **YES** — finite, no unmapped dynamics | 308 green theorems; odd branch fully measured |
| **template-class machines** (~5–8) | **YES** in principle (per-machine engineering) | x2 validates the method |
| **(K)-class machines** (~14) | **NO** — needs external AEV/normality math | (K) closed to all internal routes (§II) |
| **unconditional BB(6)** | **NO** by this program | requires (K); mechanical consequence, not impression |

The honest frame: the reachable deliverable is **x2 + the template island** (a real, publishable
chunk of BB(6)); **unconditional BB(6) is (K)-blocked** with the block now internally proven-closed.

---

## V. Information map (where the detail lives)

- **(K) closure:** `O4_KWALL_CLOSURE_2026-07-25.md`; `ATTACK_CATALOG_2026-07-24.md` (§IV routes,
  §V second layer, (K)-collapse note); evidence `route16/17/21_*.py`, `route16_*` (repo root + here).
- **Prior (K) corpus:** `bb6-frontier-state` memory (framework package, No-Structure theorem,
  obstruction dichotomy, solenoid placement, 3 publishable partials, cross-field empty-toolbox).
- **x2 odd branch:** `R1_ODDSEAM_2026-07-24.md` (Lc=6, exit-reads-comb correction);
  `x2r1_oddseam.py`, `x2r1_oddpre.py`, `x2r1_lcparity.py`; `lean/T7OddRung.lean`.
- **x2 remaining:** `REMAINING_2026-07-24.md`; `X2_STATE_2026-07-24.md`.
- **Feasibility:** `FEASIBILITY_2026-07-24.md` (now sharpened by §II).
- **Methods/discipline:** `METHODS_2026-07-24_v3.md` (M0–M9, M6-5, M7-4, M9, M1-5).

---

## VI. The one honest sentence

We have proven, internally and mechanically, that the complete proof of BB(6) is blocked exactly at
the (K) wall and nowhere else; the reachable work is to finish x2 and the template-class island,
which we have reduced to a measured, mechanism-free task list with one ∀-theorem (odd exit REGEN)
as its rate limiter.
