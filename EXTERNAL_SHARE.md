# Cover note for sharing the Antihydra kernel (AEV / Tao-line / bbchallenge)
A short, honest framing to accompany `MINIMAL_OPEN_KERNEL.md` (+ `PROOF_STATUS.md`, `NEW_FRAMEWORK.md` on
request). We are **not** claiming a solution. We are sharing a reduction, an obstruction map, an elementary
complexity floor, and a self-consistent-operator framing, and asking whether known tools reach the kernel.

---
**Subject:** A self-consistent-operator framing of Antihydra (BB(6)) non-halting — can your tools reach this kernel?

Dear [name],

We have been studying the Busy-Beaver machine "Antihydra" (the BB(6) frontier: orbit `c_{n+1}=⌊3c_n/2⌋`,
`c_0=8`; it never halts iff the running even-density stays `≥ 1/3`). We are **not** claiming to decide it —
it is the single-orbit case of Mahler's 3/2 problem / your (AEV) normality conjecture. We are writing because
we found a chain of **machine-checked reductions** ending in a clean functional-analytic statement, and we
would value your judgment on whether any existing tool reaches it.

What we have (all labelled PROVEN / MEASURED / OPEN in the attached notes):
1. **Reduction (PROVEN, exact):** non-halting ⟺ the single specified orbit `c_n mod 2^k` equidistributes;
   equivalently the parity `c_n mod 2 = bit_n(8·3^n) ⊕ bit_n(T_n)`, where `T_n` is the parity-history
   carry-sum (so the digit driving the parity is generated by the orbit's own history — a closed loop).
2. **Elementary complexity floor (PROVEN):** the parity sequence is not eventually periodic (transience),
   giving subword complexity `p(ℓ) ≥ ℓ·log₂(3/2)⁻¹ ≈ 1.71ℓ` (matching your Dubickas constant); the ceiling
   `p(ℓ)=2^ℓ` is exactly the equidistribution (= the open kernel).
3. **Self-consistent framing (the part we'd most value feedback on):** the governing transfer operator
   depends on its own invariant measure, `ν = L_ν ν` (a 2-adic McKean–Vlasov-type equation). The annealed
   operator `L` is one-step exact (full spectral gap); linearizing at Haar, **the complete proof is
   equivalent to a single subcriticality inequality `‖F‖<1`** for the self-generation feedback `F`. We
   measure `‖F‖≈0.04` (the annealed part is null; the remainder is a pure correlation correction
   `≈ Σ_h K(h)C(h)`).

The honest gap: every route reduces to controlling **one deterministic, endogenous (self-generated) cocycle
of a single computable orbit** — a "propagation of chaos for one trajectory" — where standard mean-field /
Gibbs–Markov / random-walk-in-random-scenery theory stops at *a.e. random* realizations.

Our questions:
- Is the self-consistent / self-generated-cocycle framing a known object, with a tool we've missed?
- Does any effective single-orbit equidistribution for `⌊(p/q)^n⌋` exist that we overlooked?
- Could `‖F‖<1` (a correlation correction to a null annealed baseline) be attacked by Gowers-norm /
  Mauduit–Rivat carry-lemma methods extended to a self-generated carry?

A sharp "no, because …" is as useful to us as a pointer. Notes attached.

With thanks,
[name] — GeoAlpine LLC — ORCID 0009-0002-3791-2372
