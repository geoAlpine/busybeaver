# RESUME — 2026-07-26 close (written before a planned PC restart)

Everything is committed; nothing is in flight. Pick up from **§3 next action**.

## §0 Where the frontline is

```
worktree : /Users/aokiyousuke/busybeaver/.claude/worktrees/roadmap-post-closure-sync
branch   : worktree-roadmap-post-closure-sync   (main is ~206 commits behind; ignore main)
tip      : 0cced10   (167 commits ahead of origin — NOT pushed)
lean     : lean/ has its own warm .lake; `lake build` from lean/ works in-place
python   : /Users/aokiyousuke/quantum-ecc/.venv/bin/python   (exact big-int)
```

Read `SYNTHESIS_2026-07-26.md` first — it is the complete-proof map and the ranked plan.

## §1 What this session added (4 commits)

| commit | content |
|---|---|
| `76cf743` | `SYNTHESIS_2026-07-26.md` — the complete-proof dependency tree + method inventory + ranked plan |
| `bd9b10d` | `lean/O4Run.lean` — o4's drain-run law transported to the **real** ledger orbit |
| `7739d54` | `D_SPEC_2026-07-26.md` + the **correction** that D is a CASCADE, not a pure COMB |
| `0cced10` | `lean/DMachine.lean` — D's machine in Lean + the rung tile's ∀-uniform **atom** GREEN |

Build state: full `lake build` **EXIT=0**; `lake build O4Run` and `lake build DMachine` both
"completed successfully". Corpus-wide: `sorryAx` 0, `Classical.choice` 0.

## §2 The one number that matters

BB(6)'s complete proof (`lean/Completion.lean`, `BB6_eq_championSteps`, assembly already PROVEN)
rests on **4 obligations**; **2 are (K)-independent engineering** (`champion_lower`,
`enumeration_upper`) and **2 are open mathematics** (the 17 named `*_nonhalt`,
`holdouts1087_nonhalt`). Only the carry-transparent **template island** (≥7–10 graphs, of which
`x2` and `C` are PROVEN) is mechanically closable. Everything else merges into the (K)/normality
wall. Do not let a local success suggest otherwise.

## §3 NEXT ACTION — finish D's ∀ rung tile

`lean/DMachine.lean` has the machine (`dT`), the words (`pow10`/`pow01`/`ones`), the config
family `IN`, the ∀ target `RungTile : Prop`, kernel-grounded tile instances at spans 21/33/45,
and the outward sweep already proven:

```lean
crawl     : steps dT 4  ⟨.A,p,⟨true::false::L, false, R⟩⟩ = ⟨.A,p-2,⟨L,false,true::false::R⟩⟩
crawlFold : steps dT (4*n) ⟨.A,p,⟨pow10 n ++ L,false,R⟩⟩ = ⟨.A,p-2*n,⟨L,false,pow10 n ++ R⟩⟩
```
both `[propext, Quot.sound]`.

### The full measured itinerary of one rung

Trace at `u=2,m=3,c=2,g=4` (span 45):
```
(ABED)³ · A · (ABED)³ · B · (EB)^i · (CB)^j · CD · A
```

### The two remaining atoms — READ/WRITE MEASURED, ready to write as `rfl` lemmas

**`EB` atom** (2 steps, head +2, converts `0 1 → 1 0`; the E-phase of the return sweep):
`E0: 0→1,R,B` then `B1: 1→0,R,E`.
```lean
theorem ebAtom (p : Int) (L R' : List Bool) (b : Bool) :
    steps dT 2 ⟨.E, p, ⟨L, false, true :: b :: R'⟩⟩
      = some ⟨.E, p + 2, ⟨false :: true :: L, b, R'⟩⟩ := by rfl   -- may need the p+1+1 rw
```

**`CB` atom** (2 steps, head +2, converts `1 0 → 0 1`; the C-phase):
`C1: 1→0,R,B` then `B0: 0→1,R,C`.
```lean
theorem cbAtom (p : Int) (L R' : List Bool) (b : Bool) :
    steps dT 2 ⟨.C, p, ⟨L, true, false :: b :: R'⟩⟩
      = some ⟨.C, p + 2, ⟨true :: false :: L, b, R'⟩⟩ := by rfl
```

Then fold each (mirror `crawlFold`, using a `pow01`/`pow10` snoc lemma — `pow10_snoc` is already
proven; write `pow01_snoc` the same way).

### The three boundary events (all constant-size ⇒ `rfl` after the fold lemmas)

1. **The `[1,1]` marker step** — the single `A` between the two `(ABED)` runs: `A1: 1→0,L,A`.
   It fires when the crawl reaches the *second* `1` of `[1,1]`. (The crawl runs `u+1` times:
   `u` over `pow10 u`, plus once over the first `1` of `[1,1]`.)
2. **The phase seam `E→C`** — the return sweep switches from `(EB)` to `(CB)` when `B` reads `0`
   instead of `1` (trace steps 33–34). This is the `pow01`/`pow10` boundary.
3. **The turn `CD·A`** — terminate when `C` reads `0`: `C0: 0→0,R,D` then `D0: 0→1,L,A`,
   landing `A` at `pos+3`. This is the `+3` head advance.

Compose ⇒ discharge `RungTile`. Keep every lemma `∀ L R` from the start (M2 discipline) — that
is the only thing that will make `H` cheap.

### Verification discipline that already paid off twice today

* **Measure in Python before asserting in Lean.** `d_tile_check.py` refused my first concrete
  spans (15/27/39) — I had used `RungTile`'s shifted `(m+1)` convention; the true spans are
  21/33/45.
* **Re-derive a subagent's claim independently.** `d_independent_check.py` (shares no code with
  `d_spec.py`) is what confirmed D is a cascade and killed the "pure COMB" reading.
* Instruments at repo root, never `scratchpad/` (gitignored — citations rot).

## §4 After the tile (D's remaining road, honest)

1. `k+1` turn phases per epoch — additive constants have **no closed form**; needs a uniform
   `0`-run crawl lemma. **Largest remaining reverse-engineering gap (RF-4).**
2. The 3 shifted even-`k` `S1` segments (RF-5) — second tile, internal step law not derived.
3. Cascade-level inner induction (the epoch is `k+1` phases).
4. Milestone family: pinned `k=4..9`; `k=1,2,3` are **off-family** ⇒ enter the induction at
   `M1(4)`. `k ≥ 10` unmeasured (`M1(10) ≈ 1.25e9` steps).
5. Entry segment: blank → `M1(4)` = **291,168 steps** (40% of x2's 732,733) ⇒ ~3–4 kernel-`rfl`
   chunks.
6. Assembly via `TapeCalc.nonhalt_of_invariant`.

## §5 Other live threads (not started)

* **`H`** — A's claim that it is a cheap same-species re-instantiation is **demoted to unverified**
  (it rested on D being a pure comb). Cheap insurance: test whether D's rung tile fires on H
  before or alongside the D build.
* **o4** — `O4Run.lean` pinned o4's residual difficulty to the **first moment alone**
  (`limsup D(n)/n ≤ 4/5`). That remains (K)-hard; `run = O(log n)` is also (K)-hard (Subspace does
  NOT apply — `Gseq` is the *iterated* orbit). Nothing further to try internally.
* **`enumeration_upper`** — the (K)-independent Coq-BB5-scale port; multi-person-year, no new math.
* **Push** — the branch is 167 commits ahead of `origin`. Not pushed; owner decision.

**No machine decided by this document. No label upgraded. `D` remains `[OPEN]`.**
