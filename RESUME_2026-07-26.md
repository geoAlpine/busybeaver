# RESUME — 2026-07-26 (second close; supersedes the first)

Everything is committed; nothing is in flight. Pick up from **§3 next action**.

## §0 Where the frontline is

```
worktree : /Users/aokiyousuke/busybeaver/.claude/worktrees/roadmap-post-closure-sync
branch   : worktree-roadmap-post-closure-sync   (main is ~208 commits behind; ignore main)
lean     : lean/ has its own warm .lake; `lake build` from lean/ works in-place (~10s cold)
python   : /Users/aokiyousuke/quantum-ecc/.venv/bin/python   (exact big-int)
```

Read `SYNTHESIS_2026-07-26.md` for the complete-proof map, then
`D_RUNGTILE_2026-07-26.md` (the tile, and how it became machine-independent),
`ATOMS_ISLAND_SCAN_2026-07-26.md` (how much of the residual it reaches),
`D_RF4_2026-07-27.md` (the turn phases) and `D_CASCADE_2026-07-27.md` (the word family, the
entry segment, and the reduction of `D` to one obligation).

Build state: full `lake build` **EXIT=0, 89 jobs**. Corpus-wide `sorryAx` 0,
`Classical.choice` 0. No `native_decide`, no `decide`.

## §1 What the second half of this session added

| commit | content |
|---|---|
| `92ebc34` | `D`'s rung tile **[PROVEN]** `∀ u m c g p TAIL REST` + two corrections to the old `Prop` |
| `30b207a` | `lean/RungCalc.lean` — the tile made **machine-independent**; `H`'s tile **[PROVEN]** for six `rfl`s |
| `a314e15` | `Atoms` scanned over the residual: **17/1104 hits**, all 17 tiled in `lean/IslandTiles.lean` |
| `50a741a` | `Atoms` parameterized by step counts; behavioural rescan → **18/1104**, and **0** near-misses left |
| `9a3c2a6` | **RF-4 half closed**: the rightward turn phase is the rung with a longer return sweep |
| `123e466` | RF-4 part 2: the leftward turn is `rung0` + `descend`; **99.959% of the epoch under proven laws** |
| `d979362` | Cross-epoch check (k=4,5) + greedy chaining: **99.972% / 99.982%**; `sweep10At` at any anchor |
| `1754036` | `lean/DCascade.lean` — D's milestone word family, re-measured k=1..9 and pinned by 15 `rfl`s |
| *(this)* | `DEntry` (entry segment kernel-proven) + `DReduce` (**D reduced to the single obligation `EpochLaw`**) |

### The two corrections (both measured before asserting)

1. `RungTile : Prop` said span `6*(u+m)+15` on `IN u (m+1) …`. Wrong by exactly one
   crawl-plus-return period: the span is `6*(u+m)+21`. The kernel-`rfl` instances
   (21/33/45) were always right; only the `Prop` was off. Last session *saw* this
   (the RESUME recorded `d_tile_check.py` rejecting spans 15/27/39) and worked
   around it instead of fixing it.
2. `c ≥ 1` is not a hypothesis — the outward sweep dies on the `0 0` gap one cell
   short of the `1^c` block, so the counter is never read (`tile_c_zero`, `c = 0`).
   `g ≥ 3` **is** real: at `g = 2` the machine reaches `F` instead of turning.

### The architecture that came out of it

The six atom *interfaces* mention only two state names — the outward-sweep state
`sA` and the return-sweep state `sB`. Everything else hides inside an atom. So the
tile cannot see the transition graph. `lean/RungCalc.lean` makes that literal:

* `Atoms T sA sB` — a `Prop`-valued structure with the six atoms;
* `crawlFold` / `sweep10` / `sweep01` — the three `∀`-uniform folds;
* `rung_core` / `tile` — the nine-phase tile, **[PROVEN] for every machine satisfying `Atoms`**.

Per-machine cost is now exactly **six closed kernel `rfl`s**:

| file | machine-specific content | tile |
|---|---|---|
| `lean/DMachine.lean` | `dT` + `dAtoms` (`sA=A`, `sB=B`, counts `4 1 1 2 2 3`) | `RungCalc.tile dAtoms` |
| `lean/HMachine.lean` | `hT` + `hAtoms` (`sA=D`, `sB=A`, counts `4 1 1 2 2 3`) | `RungCalc.tile hAtoms` |
| `lean/IslandTiles.lean` | 18 residual holdouts, machine-generated | `RungCalc.tile` each |

`H` was worth doing: its graph is **not** a relabeling of `D`'s (state-permutation
search over `D`/`Dᴿ` × `H`/`Hᴿ` returns zero isomorphisms — the mechanism that made
`C` free does not apply), yet the tile fires verbatim, 23040/23040, and the two
itineraries are the same word. `H` splits a state `D` reuses (`Dᴿ`'s `D` is entered
from `E` inside the crawl and from `C` inside the turn; `H` uses `F` and `C`).
The tile is an invariant modulo merging states that behave identically inside an atom.

## §2 The one number that matters

BB(6)'s complete proof (`lean/Completion.lean`, `BB6_eq_championSteps`, assembly already PROVEN)
rests on **4 obligations**; **2 are (K)-independent engineering** (`champion_lower`,
`enumeration_upper`) and **2 are open mathematics** (the 17 named `*_nonhalt`,
`holdouts1087_nonhalt`). Only the carry-transparent **template island** (≥7–10 graphs, of which
`x2` and `C` are PROVEN) is mechanically closable. Everything else merges into the (K)/normality
wall. **The rung tile is one lemma of one machine's proof — do not let it suggest otherwise.**
`D` and `H` are both still `[OPEN]`.

*Do not conflate two 17s.* The **17 named `*_nonhalt`** above are the named-machine obligations of
`BB6_eq_championSteps`; the **17 `Atoms` hits** of §3 are holdouts in the 1087 residual that now
have a rung tile. Different sets, unrelated counts.

## §3 NEXT ACTION

**Option A of the previous revision is DONE** (2026-07-26, `ATOMS_ISLAND_SCAN_2026-07-26.md`):
the residual was scanned for `Atoms` and **17 of 1104 satisfy it**, all 17 now have their rung
tile in Lean (`lean/IslandTiles.lean`, machine-generated, builds in 0.6s), and all 17 have the
tile firing on their real blank-tape orbit (100% of IN-shaped configs, 231-398 per machine, no
halt in 300k steps). Named island candidates: `D`, `E`, `H` are hits; `x2` (1/6), `F`, `G`, `I`
are not -- **this tile is not x2's mechanism**.

Honest caveat recorded there: `Atoms` pins 9-10 of the 12 transition entries, so 17 is an upper
bound on what this one tile can ever reach, not a measure of the island's width.

**Items 1 and 2 of the previous revision are also DONE** (2026-07-27, `ATOMS_ISLAND_SCAN` §7-§10).
The near-miss's turn takes **5** steps and reaches the *identical* output configuration, so the
fixed `(4,1,1,2,2,3)` step counts were an accident of `D`, not part of the mechanism.
`RungCalc.Atoms` is now parameterized by the six step counts with `RungCalc.span` linear in them,
and detection is behavioural (`atoms_flex_scan.py`): match each atom by its exact input/output
configuration at whatever step count the machine takes, requiring agreement across tape contexts.

  fixed-shape:  6/6: 17   5/6: 1   4/6: 4   3/6: 31   2/6: 377   1/6: 674
  behavioural:  6/6: 18   5/6: 0   4/6: 4   3/6: 36   2/6: 401   1/6: 645

**18** hits, and **zero** 5/6 near-misses -- this relaxation is EXHAUSTED, no machine is within
one atom any more. Only two step-count signatures exist: `(4,1,1,2,2,3)` span `6(u+m)+21` (17
machines) and `(4,1,1,2,2,5)` span `6(u+m)+23` (1, `M17` in `IslandTiles.lean`). All 18 fire on
100% of their real-orbit IN-configs, per-machine span. Gotcha worth keeping: the 5-step turn
back-tracks, so the position normalisation is `p + 1 - 1 + 1 + 1 - 1`, not derivable from the
step count -- the generator now derives it from the actual move sequence.

### Next

**RF-4, D's `k+1` turn phases.** Now the only item left on this thread's list, and the one on D's
critical path. Still the largest remaining reverse-engineering gap, still untouched, and **not**
covered by the tile: the additive constants have no closed form and a uniform `0`-run crawl lemma
is needed. Measure first (`d_spec.py`, `d_g4_*`), Lean second.

Anything further from the tile route needs a *different* interface, not a weaker one: the
behavioural scan proves nothing is one atom away. The 4/6 and 3/6 bands (4 and 36 machines) would
need at least two new atom specifications, so they are no longer cheap.

## §4 D's remaining road (honest, unchanged by this session)

1. **RF-4** `k+1` turn phases per epoch — no closed form. **Largest gap.**
2. **RF-5** the 3 shifted even-`k` `S1` segments — second tile, internal step law not derived.
3. Cascade-level inner induction (the epoch is `k+1` phases).
4. Milestone family: pinned `k=4..9`; `k=1,2,3` are **off-family** ⇒ enter the induction at
   `M1(4)`. `k ≥ 10` unmeasured (`M1(10) ≈ 1.25e9` steps).
5. ~~Entry segment: blank → `M1(4)` = 291,168 steps~~ → **[PROVEN]**, `DEntry.entry`, 3 chunks.
6. ~~Assembly via `TapeCalc.nonhalt_of_invariant`~~ → **[PROVEN]**, `DReduce.nonhalt_of_EpochLaw`.

`H`'s equivalents of 1–6 are **entirely unmeasured**. The tile was the cheap part.
(Item 1 is now *half* done — see §3.)

## §5 Other live threads (not started)

* **o4** — `O4Run.lean` pinned o4's residual difficulty to the **first moment alone**
  (`limsup D(n)/n ≤ 4/5`). That remains (K)-hard; `run = O(log n)` is also (K)-hard (Subspace does
  NOT apply — `Gseq` is the *iterated* orbit). Nothing further to try internally.
* **`enumeration_upper`** — the (K)-independent Coq-BB5-scale port; multi-person-year, no new math.
* **Push** — the branch is ~170 commits ahead of `origin`. Not pushed; owner decision.

## §6 Verification discipline that paid off three times today

* **Measure in Python before asserting in Lean.** It caught the 6-step span error in
  the committed `Prop` — twice, since the first sighting was recorded and then
  ignored. **A recorded anomaly that was worked around is still an open bug.**
* **Re-derive a claim independently.** The H result only landed because the
  "same species ⇒ cheap" claim was tested as *two separate questions* (same graph?
  same atoms?) instead of one. The answers were opposite.
* **State the interface, not the instance.** `Atoms` was extractable only because
  every atom had been kept `∀ b L R` from the start (M2 discipline).
* Instruments at repo root, never `scratchpad/` (gitignored — citations rot).

**No machine decided by this document. No label upgraded. `D` and `H` remain `[OPEN]`.**
