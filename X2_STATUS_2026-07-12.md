# The integer-×2 machine — corrected authoritative status (2026-07-12)

*Supersedes `X2_STATUS_2026-07-11.md`. The 07-12 Lean effort produced a MAJOR correction: the doubling phase is a
**quadratic braid**, not a sequence of local episodes, and several green Lean lemmas from 07-12 are **true but
OFF the real trajectory**. This map separates ON-PATH (real orbit) from OFF-PATH (valid isolated `step`-lemmas the
machine never visits) — the honest distinction the discipline exists to enforce. Machine
`1RB0RE_1RC---_0LD1LE_0RE1LD_1RF0LC_0RA1RE`. NOT decided.*

## 1. Top line (corrected)

**NOT decided.** The machine is carry-transparent (low phase proven ∀g, [OPEN] top-level). But the path to a decision
is **substantially harder than the 07-12 episode-composition suggested**: the doubling phase `M6(g)→M1(g+1)` is **one
braided milestone-to-milestone segment of length Θ(2^{2K})** (a shrinking-comb odometer *double* induction), NOT a
composition of local sweeps. Main-loop adjudicated by raw TM: doubling-phase step lengths 440k, 1.62M, 5.94M, ratios
**~3.7× ≈ 4×** (quadratic; a linear episode chain would be ~2×); agent's 2.12M(g=2)→8.48M(g=3) = 4.0×.

## 2. ON-PATH vs OFF-PATH `[the load-bearing correction]`

| Lean object | status | on the real orbit? |
|---|---|---|
| machine `step`/`steps`, `halt_gate` (B:1) | PROVEN, Lean | **ON-PATH** (foundational) |
| `sweepEF` `(01)^n→1^{2n}` | PROVEN, Lean | **ON-PATH** — the genuine ×2 repack primitive, run in a shrinking-comb loop |
| `dSweepTurn` block crossing | PROVEN, Lean | **ON-PATH** (D-sweeps cross the block, interleaved) |
| low-phase safety (gaps {6,10,18}, never 3) | PROVEN (Python `x2cc_prove`), not Lean | **ON-PATH** (the real low phase) |
| `doubling_id`, `cascadeBlocks_sum` | PROVEN, Lean | arithmetic facts — true regardless of path |
| `markedChew`/`markedTurn`/`markedBlock` | green, Lean | **OFF-PATH** — the `(10)^10`-marked big block occurs **0 times** on the real orbit (exhaustive scan) |
| `bigCascade`, `cascade_traversal`, `cascadeFold` | green, Lean | **OFF-PATH** — valid isolated `step`-lemmas the braid never realizes as clean sequential episodes |
| `doubling_transport(_mid)` | green, Lean | **OFF-PATH / vacuous** — its `H_entry`∧`H_repack` are jointly unsatisfiable on the real path |

**The lesson:** a green, `sorry`-free Lean lemma is *true*, but truth of an isolated `step`-lemma ≠ a proof about the
real machine. Several 07-12 lemmas prove nothing about the orbit because the machine visits none of their configs.
The 07-12 "doubling-phase middle composed" progress (`d226cb0`) was off-path. Caught by the humility-mandated
exhaustive real-orbit scan (`53a1b93`).

## 3. What the doubling phase actually is `[PROVEN structure]`

A **shrinking-comb odometer braid**: a round-trip is an E-sweep RIGHT over the live comb `(01)^n` (this IS `sweepEF`),
a turn into C at the next 1-block boundary, a leftward return; the next round-trip runs a comb one shorter — a *double*
induction (inner = the sweep over a comb of length n; outer = n descending while the register `(1^5 0^2)^{g-1}` and the
whole cascade are processed simultaneously). The ×2 couples to `2^K` **and the full cascade at once**; the `Θ(K)`
residual of `bigCascade_not_doubling` is realized only by the braid's simultaneous processing. Step count Θ(2^{2K}).

## 4. The honest remaining work (for a decision)

The real doubling-phase transport needs a **braided double-induction** in Lean — the whole cascade+register carried as
the outer invariant, `sweepEF` as the inner primitive — landing on `M1(k+1)` halt-free, with the net `2^K→2^{K+1}`
emerging from the braid. This is **more than o4's `generation_odometer`** (which was a *linear* prefix·body·suffix
composition); it is a nested induction. Plus the low-phase Lean port (Python-proven) and the top-level `x2_nonhalt`
assembly. Substantial multi-session effort; the on-path primitives (`sweepEF`, `dSweepTurn`, halt gate) are the genuine
building blocks; the off-path episode lemmas are set aside.

## 5. Correction cascade (the full honest arc)

launch optimism (mine, ×2) → refuted; route A "counter-dependent o4-wall" → refuted by carry-calculus (mature gaps
constant, main-loop adjudicated); carry-calc "engineering-volume only" → refuted (3 gaps); my "G3 accumulator→2^K" →
refuted (doubling in G2); my "G2 sweep = the doubling" → refuted (`marked_not_doubling`, it's the chew half); "episode
composition" (bigCascade middle) → refuted (**off-path; the phase is a quadratic braid**). **~7 optimisms/framings
refuted by careful work; every correction in the git log; no soundness lie survived** — but the honest cost is that
the decision is much further than any single step suggested.

## 6. Significance even un-decided
The carry-transparent/opaque dichotomy (the exact (K) boundary); the register decoded; the low phase proven ∀g; and now
the precise structure of the wall to a decision — a **quadratic shrinking-comb braid** coupling to 2^K + the full
cascade, with the on-path primitive (`sweepEF`) identified. The machine remains the frontier's best-mapped candidate;
the path is now honestly (and much more steeply) drawn.

## Sources
`X2_STATUS_2026-07-11.md` (superseded intro), `X2_CARRY_CALCULUS`, `x2_verify_gaps.py` (adjudication),
`X2_COMPOSITION`, commits `d226cb0` (off-path middle) → `53a1b93` (the braid correction); `lean/X2.lean` §5f–5h.
No machine decided. No label upgraded.
