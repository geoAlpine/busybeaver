# CORRECTION — o18 is o15's machine table (mirrored, re-rooted): one table, two seeds; community priors integrated (2026-07-07)

*Found by the pre-release novelty audit (`NOVELTY_AUDIT_2026-07-07.md`) and verified independently by the main loop.
This note corrects the repo's counting of o15/o18 as separate machines and integrates the community's prior work,
including a Lean-verified halting congruence class that supersedes the repo's "no fatal set found for o18" framing.
No machine decided.*

## 1. The identity `[PROVEN, exhaustive isomorphism search]`
o18 = `1RB0RE_1LC0RA_1LA1LD_1LC1LF_0LC0LB_1LE---` equals o15 = `1RB---_0RC0RE_1RD1RF_1LE0LB_1RC0LD_1RC1RA`
**mirrored (L↔R) with the state bijection A→D, B→E, C→C, D→F, E→B, F→A** (verified by exhaustive search over all
6! relabelings × mirror: exactly this one match). Both start in their own state A, and o18's A corresponds to o15's
D — so **o18-from-blank is the o15 table run from a different start state**: one dynamical system, two orbits.
The repo's species counting ("o18 = fourth species, o15 = fifth shape") must be read as: **one machine table with
two seeds, analyzed in two coordinate systems** (o15: digit-queue + big block V; o18: D(m,t,e) pushdown odometer).
The two analyses are complementary coordinatizations of the same rules and should ultimately be unified.

## 2. Community priors (from the audit; integrate, do not re-derive)
- o15 was found by Racheline (2024-11-23); the wiki documents the ×8/3 rule, **known fatal residue sequences**
  (length ~110, variants counted to 2¹³·3¹⁵), and community status **"probviously halting"** (expected to halt).
- For the o18 orbit, **@-d has a Lean-VERIFIED halting congruence class mod 3¹⁰⁸** (int-y1/proofs on GitHub).
- **CORRECTION to `O18_TEMPLATE_PORT_2026-07-07.md` / `O18_DEPTH_UNIFORM_2026-07-07.md` / the species documents:**
  the statement "no fatal set found (0 halting configs)" was a bounded negative search over the D(m,t,e)-adjacent
  family only, and is SUPERSEDED: via the identity, o15's proven fatal family (leading `[2,2]`) and the community's
  Lean-verified halting class apply to the same table. The honest current picture: **the table has a real fatal
  set; whether either seed's orbit reaches it is the open question** — and the community's expectation for these
  orbits is HALT ("probviously"), opposite in flavor to o4 (whose +3-drift ledger leans non-halt).
- o17 was found by Racheline (2025-02-09): gate condition, `dt=(8k+C)/3` macro laws, iterated-exponential halt
  estimate — consistent with and prior to parts of `O17_GATE_LAW_2026-07-07.md` (the exact gate map F and the
  wall-and-erosion derivation were not found in the searched sources). **Provenance RESOLVED (2026-07-07, project
  owner confirmed): "Opus 4.7 / DrDisentangle" is NOT this project** — the wiki's o17 macro section is genuine
  third-party prior work (notably, also AI-assisted, by another Claude instance). Verdict for o17 hardens to
  PARTIAL-PRIOR with a named third party; our incremental contributions are the exact gate-to-gate map `F(μ,d⃗)`,
  the wall-and-erosion mechanism/derivation of the tower law, and the no-finite-reduction results for m≥3.
  **Collision-risk implication: at least one other researcher is actively working these machines with AI
  assistance — establishing priority for the NOVEL items (AEV bridge, margin ladder, certified-template apparatus)
  argues for engaging the community sooner rather than later.**
- o4 (found by @dyuan01, 2024-09-04) and o3 (@mxdys, 2024-08-20): the wiki documents the two-counter recurrences
  and halt gates — the REDUCTION STATEMENTS are community-known; the repo's certified-template apparatus, ledger
  formulation, run-structure closed forms, and the AEV bridge were not found in the searched sources.

## 3. What remains ours (per the audit's verdicts, search-bounded)
NOVEL-as-far-as-searched: the one-sided return-frequency unification + the explicit AEV (arXiv:2510.11723) bridge
with the margin ladder; the certified trace-template method as packaged; the exact run closed forms in these
machines' coordinates (classical analogue: Terras 1976 / Everett 1977 parity-vector bijection for Collatz — MUST be
cited in `PAPER_RUN_STRUCTURE.md`). PARTIAL-PRIOR: everything listed in §2. All verdicts carry the Discord caveat
(not web-indexed; deeper unpublished work may exist).

## 4. Actions taken / required
- [x] Identity verified independently (this note §1).
- [x] Species documents corrected (one-table-two-seeds; fatal-set framing fixed).
- [x] `PAPER_RUN_STRUCTURE.md`: Terras/Everett citation added.
- [~] Unify the o15/o18 coordinatizations — **substantially advanced by `O18_MULTIDEFECT_2026-07-07.md`:** the
      multi-defect grammar closed as an operational transducer `[PROVEN on grid; 3588/3588 single-pass +
      1620/1620 full-chain predict-and-confirm incl. 26 predicted-HALT chains step-exact]`, and **fatal cells
      exist in the o18 coordinatization too: `(m≡1, w=[2,2]) HALTS** (5 magnitudes, concrete)` — exactly o15's
      leading-`[2,2]` fatal family seen through the identity. The seeming contradiction ("o15 has a fatal set,
      o18 doesn't") is fully resolved: earlier o18 searches never left single-word families. The o18-orbit
      evidence (symbolic continuation ×24 past the prior horizon: 122,015 clean generations to m≈10^85194, max
      separator 1, never adjacent 2-blocks) shows the orbit nowhere near the fatal region. **The exact remaining
      blocker is the PUSH-MARGIN LAW as a potential-function invariant** (an exact 2-block is born only by pushing
      v≡2 (mod 3), gaining 2(v−2)/3 margin; margins drain 1/POP; fatality needs two adjacent 2s at margin ≤1) —
      o4-ledger-shaped. **Honest tension:** if the push-margin invariant is provable, the o15/o18 orbits never
      halt — CONTRADICTING the community's "probviously halting" expectation; if the community's random-walk
      heuristic is right, the margins fail by a heavy-tail event. The two positions are now a precisely-stated
      standoff on one invariant.
      **STANDOFF RESOLVED AT THE MODEL LEVEL (2026-07-07, `O18_ANNEALED_STANDOFF_2026-07-07.md`):** the
      margin-aware annealed model AGREES with "probviously halting" — the decisive structural fact is that
      **clean generations re-seed the word at `((1,6))`: margins do NOT accumulate across generations** (i.i.d.
      excursions, constant per-generation fatal probability p*>0 ⇒ a.s. eventual halt in the model, at
      tower-astronomical time ~10⁴¹ generations, m_halt ≈ 10^(1.8×10⁴¹); the orbit at m≈10^85194 is ~5×10⁻³⁷ of
      the way — evidence for neither side). Two-2 words ARE itinerary-reachable (replay-verified witnesses,
      depth 51–56, always units-gapped); gap-0 fatal entrances have occupancy exactly 0 to depth 56; the
      suppression cascade is ≥10³⁷ but is a scale, not a barrier. **Consequence: the push-margin invariant cannot
      hold in all-itinerary form; it can only survive as an orbit-specific arithmetic theorem — (K)-shaped after
      all.** THE NEW STRUCTURAL AXIS this exposes: **LEDGER MEMORY** — o4/o3's ledgers are CUMULATIVE across
      generations (drift compounds ⇒ non-halt-leaning), o15/o18's margins are PER-EXCURSION (reset each clean
      generation ⇒ a.s.-halt-leaning in the annealed model). This is the sharp structural reason the two pairs
      lean opposite ways. One decisive external check remains: whether the community's Lean-verified halting
      class mod 3¹⁰⁸ is CLEAN-ROOTED (if yes, p* ≈ 3.9×10⁻⁴² exactly and the halting expectation is grounded).
      **RESOLVED (2026-07-07, main loop): CLEAN-ROOTED — CONFIRMED end-to-end.** The Lean file (int-y1/proofs,
      commit 1929f57) defines `A(n, ls) = blanks <{{F}} [1]^n ++ ls ++ blanks` and proves
      `halts_A_mod_3_pow_108 (k) : halts tm (A (3¹⁰⁸·k + 1975700577069254261393123959451562008800234946348086) [])`
      for ALL k, plus `reach_9_nil : c₀ ⊢* A 9 []` (blank-tape reachability of A(9)). Local verification: the
      blank o18 orbit passes exactly through this family — F-state, solid 1-block, head one cell left:
      A(9)@step 35, A(27), A(75), A(203), A(545), … (epoch law ≈8n/3 ✓). **Consequences:** (i) the per-epoch fatal
      probability p* is GROUNDED on a Lean-verified fatal class at the correct rooting; (ii) **o18's decision is
      now exactly: does the epoch sequence n_i ever hit a fatal congruence class** (the Lean class being one
      proven-fatal example) — a deterministic congruence-hitting question for an explicit ⌊8n/3⌋+c orbit =
      (K)-species, resetting-ledger variant, with the halting side carrying formal backing; (iii) the first
      122,015 epoch values (exact big ints from the orbit continuation) can be CHECKED against the Lean class —
      a free finite test (a hit would DECIDE o18 as halting). **EXECUTED (main loop): 121,801 clean epochs
      checked, hits = NONE** — the free decision chance is spent; the orbit remains clean to m≈10^85194.
      **SYNTHESIS TRACK CONFIRMS (2026-07-07, `O18_INVARIANT_SYNTHESIS_2026-07-07.md`):** a fully-verified
      **97-pass witness path from the exit cone to the fatal cell** exists under adversarial residues (explicit
      m₀, class mod 3⁹⁷, every edge concrete-confirmed at 5 magnitudes) ⇒ **NO residue-oblivious inductive
      invariant of ANY class exists** (meta-theorem: any T-closed set ⊇ exit cone contains the halting cell) —
      the finite-search door on the cryptid frontier is CLOSED, independently confirming the annealed prediction.
      Notable: the 97-digit fatal residue string contains **no 0s** (every ρ≡0 pass derails the conspiracy);
      random itineraries land in median 2 passes; p_halt/excursion ∈ [2×10⁻³⁸, 2.3×10⁻³]. Also: the earlier
      adversarial BFS's 10 abstract HALT paths were GLUE-JUMP artifacts (right verdict, wrong reason — root-caused);
      517 deep-R1 Unknown cells are adversarially reachable and must be pinned before any conditional-safety claim.
      FINAL SHAPE: o15/o18's decision (either direction) is an itinerary-coupled arithmetic question on
      (m mod 3^k, w) — (K)-species, per-excursion (resetting-ledger) variant.
- [x] "Opus 4.7 / DrDisentangle" provenance verified with the project owner: NOT ours — genuine third-party prior
      (see §2; o17 verdict hardened to PARTIAL-PRIOR with a named third party).
- [ ] Before public release: cite the wiki pages and the Lean artifacts for every PARTIAL-PRIOR item.

**Meta.** This is precisely what the pre-release novelty audit exists for: it caught (i) a machine-identity miss,
(ii) a stale negative claim, and (iii) a mandatory classical citation, before anything was published. ~37th–39th
self-caught corrections. No machine decided. No label upgraded.
