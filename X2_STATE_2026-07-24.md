# x2 — full state of the proof (2026-07-24 close)

Snapshot after the day's work. 15 T7 modules, **293 theorems**, `lake build` 55/55 GREEN,
no `sorry` / `native_decide` / Mathlib; everything `[propext, Quot.sound]`.
**No machine decided. No label upgraded. `x2` remains `[OPEN]`.**

---

## The target

`x2_nonhalt` needs `h_init` (✅), `h_low ∀g` (✅, both parities), and `h_doub ∀g` — the
doubling phase `M6(g) → M1(g+1)`. This document is `h_doub`'s state.

## The doubling phase, both parities

```
M1(g) --h_low--> M6(g) --topEntry--> descIn --headLaw--> regenIn 5
      --ladder--> cascadeReg(g+8) --topRung--> top --seam--> tail-IN --tail--> M1(g+1)
```

| segment | EVEN g | ODD g |
|---|---|---|
| `M6 → descIn` (topEntry) | ✅ `topEntryEven`, `topEntryEvenLT` | ✅ `topEntryOddFull` |
| `descIn → regenIn 5` (headLaw) | ✅ `headLaw` (∀n, parity-free) | ✅ same, measured +4 897 |
| ladder `→ cascadeReg(g+8)` | ✅ `ladderToCascade` | ✅ canonical (O1, full-shape) |
| top rung `→ top` | ✅ `topRung`/`RegenLawGen` (∀U) | measured, not yet built |
| seam `→ tail-IN` | ✅ `seam74` | different register (`0 0 · frameL` seam) |
| tail `→ M1(g+1)` | ✅ `tailLaw` (∀j) | ✅ measured bit-identical end |
| **whole phase** | ✅ **`doubPhaseEven`** | oddSpine + O4 remain |

## What is GREEN (the machines)

* **Head descent** `descLaw`/`descFold_all` (∀k), `headLaw` — the whole head, ∀, parity-free.
* **Tail** `frameDigit`/`frameFold`/`turn`/`fixedEnd`/`tailLaw` (∀j) — parity-free.
* **Ladder** `ladderToCascade`; **top rung** `RegenLawGen`/`topRung`/`topRungToMilestone` (∀U).
* **Even topEntry** the 4-phase `p1t`/`rUnitsFold`/`bridge`/`eChewFold` + assembly, and the
  `LL`/`TT`-freed variants that meet the spine exactly (E2/E3).
* **Even doubling phase** `doubPhaseEven` — `M6 → M1(g+1)`-frame in one theorem.
* **Odd topEntry** the carry machine `carry46`/`carryFold46`/`cross17`/`crossCarry`/`odTurn`
  and the full `topEntryOddFull` — `M6(g) → descIn(g+6)`, ∀ odd g.

## What remains (all MEASURED, no mechanism unknown)

1. **oddTopRung + oddSpine.** Below `descIn`, the odd dynamics ARE the even ones (headLaw
   verbatim; canonical ladder; bit-identical tail end). The one difference is the register the
   top rung / seam thread: even hard-codes a 9-cell seam (`0 0 1 0 1 0 1 0 0`), the odd marker
   is `0 0 · frameL (g−1) · turnWord · endWord` (measured exact at g=3). So `topRungToMilestone`
   must be re-derived for the odd seam — `topRung` (∀U) and `seam74` (∀X) and `tailLaw` (∀j)
   are all reusable; only the seam-prefix identity is new. Register bookkeeping, E2/E3-scale.
2. **O4 assembly** `topEntryOddFull ∘ oddSpine`, mirror of `doubPhaseEven` (one `steps_add`
   chain, M8). Anti-vacuity target: M6(3)→M1(4) = 8 476 791 steps (measured).
3. **H** — families → init, both parities. Padding law measured: `zeros k` left ⟹ `zeros (k−5)`,
   so `k = 16` delivers the `zeros 11` the phase consumes. `steps_lpad_zeros` gives `∃ j ≤ k`;
   pinning `j = k−5` needs a lower-bound variant or a free-left `h_low` restatement.
4. **F** — `exact x2_nonhalt …`, cold audit, red-team. Label discussion only after.

## Honest assessment

The hard, genuinely-unknown parts are done: both `topEntry` closed forms proven as machines
(the odd carry fold was the last real discovery), and the even phase assembled end to end.
What is left for `h_doub ∀g` is (a) one odd register/seam re-derivation, (b) mechanical
assembly, (c) the shared blank-padding obligation H, (d) the final fire — **no new dynamics**.
The methods that carried the even branch (M6 rename-to-generalise, M7 pre-audit, M8 named
config) apply directly to each.

BB(6) itself stays behind the (K) wall (14 machines = external normality mathematics); x2 is
one of the 17 named cryptids, and closing it decides one machine and validates the template
method for the carry-transparent island (~5–8 more). None of that is done. `x2` is `[OPEN]`.
