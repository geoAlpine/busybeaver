> **SUPERSEDED (2026-07-24 深夜).** 本書（第2版 + M8 追補）は `METHODS_2026-07-24_v3.md`
> （改訂第3版）に置き換えられた。改訂根拠: 制定後 1 日で E1–E3・O1–O2 が閉じ（GREEN +40 本、
> 予告先行 5/5、M8 初回緑 4/4、M6 が BlankNorm を 2 度不要化）、新失敗モード 5 種が同定された
> （Θ前置詞読み違い / 未測定構造の価格付け / 固定マーカー再利用 / 族指数規約 / BlankNorm ∃j）。
> 第3版で M0/M1/M5/M6/M7 改訂、M9（レジスタ索引表）新設、実行計画は R1–R7 に置換。本文は記録保存（M5）。

# 手法書 METHODS 改訂第2版 — BB(6)/x2 解明の明確な手続き (2026-07-24)

**本書は `METHODS_2026-07-23.md` を全面改訂し置き換える**（旧版は記録として保存し supersession
バナーを付す）。改訂根拠は 2026-07-23 の1日分の実戦データ: GREEN 定理 20+ 本、較正事件 3 件
（全て主張前に捕捉）、予告先行測定 3 件全的中、アーキテクチャ訂正 1 件（梯子頂上の非正準性）。
ラベル規律は不変: [PROVEN]（Lean green, `[propext, Quot.sound]`）/ [MEASURED]（計器検証済み
実測）/ [OPEN]。**本書は手法の記録であり、いかなる機械も決定せず、いかなるラベルも昇格しない。
x2 は [OPEN] のまま。**

---

## 第0部 — 改訂の根拠データ（2026-07-23 実績の解析）

### 0.1 閉じた定理（全て GREEN, `[propext, Quot.sound]`, cold build 41/41）

| 定理 | 内容 | ファイル |
|---|---|---|
| `descLaw` / `descFold_all` | 頭部降下 `descIn k → k−1 → … → descIn 3`、∀k≥4、段コスト `9(2^{k−1}−1)` | T7DLaw |
| `descTotal_closed` | `descTotal n = 9(2^{n+4}−n−9)`（減算なし形で表明） | T7DLaw |
| `E1` / `E2` / `regenEntry` | `descIn 3 →200→ cascadeReg 4 →215→ regenIn 5`、固定・∀p ∀M ∀R | T7E1/T7E2 |
| `headLaw` | `descIn(n+4) → regenIn 5` ∀k = DescFold ∘ E1 ∘ E2。頭部全体が1定理 | T7E2 |
| `frameDigit`/`frameFold`/`turn`/`fixedEnd`/`tailLaw` | 尾部 = `27(g−1)+110` の枠桁オドメータ、∀j | T7Tail |
| `regenLawGen_4..7` (+`_specialises`) | RegenLaw の櫛自由化、基底4レベル（chunked rfl） | T7RegenGen |
| `trailOut_allGen`/`leadOut_allGen`/`regenOutGen_anchor_forced` | 証明鎖の櫛自由移植（各 `_specialises` 統制付） | T7TopRung |
| **`regenLawGen_closed`** | **∀k≥4 ∀p ∀U ∀R** — 櫛が自由な REGEN 法則 | T7TopRung |
| **`topRung`** | `cascadeReg k → cascadeRegGen (k+1)`、梯子最終段、∀k≥4 | T7TopRung |

### 0.2 実測データ資産（計器: `x2r2_sim.py` — span可変・有界検査付き。アンカー全通過が使用条件）

- **マイルストーン・アンカー**（各クラスタ先頭）:
  `M1(1..8) = 188 099 / 732 733 / 2 852 091 / 11 329 301 / 44 986 995 / 179 590 445 /
  716 937 515 / 2 866 093 189`
- **topEntry 閉形式**（両パリティとも予告先行で out-of-sample 確認済、各4点）:
  - 偶: `384·2^g + 53g + 384`、入口 `descIn(K−1)`、K=g+8
  - 奇: `6080·2^g + 53g + 105`、入口 `descIn(K−2)`、マーカー先頭にちょうど `1^20`
  - 共通線形項 `53g`。8世代の生データ表は `R3_TAIL_MEASURED_2026-07-23.md`
- **tail 構造**: `tail(g) = 入口(パリティ依存) ∘ (g−1)×frameDigit(27) ∘ turn(35) ∘ fixedEnd(75)`
  — tailLaw の IN レジスタは g=1..4 でセル単位一致（`x2r3_tailin.py`）
- **正準性規則**: 梯子は **g+8 まで正準** `cascadeReg`（櫛 = 必要数+1、すなわち Lc=1）、
  **頂上 g+9 のみ非正準**（g=2 全フェーズ + g=4 全走査で確認）。**奇数 g には正準 cascadeReg が
  存在しない**（頂上 `0^13` 型）— 奇数枝は Lean 形状が未定義
- **櫛保存則**: REGEN 輸送中、頭部は左へ `|ones(2^k−3)++[0,1,0,0,1]|` までしか入らず、
  櫛 `pow01 a` は `a→a+1` で**運搬される**（実測 + 移植の無修正通過による二重確認）
- **74歩継ぎ目**: 頂上 → tail IN、g=2/g=4 でビット同一の固定エピソード（頭部走査 左11/右9、未構築）

### 0.3 較正事件（3件、全て主張前に捕捉 — M1 強化の直接根拠）

1. `descIn` 検出器が `descCascade` 未検査 → 段コスト誤報（768/384/…）。記録との矛盾で「計器を疑う」が機能
2. チャンク抽出で状態フィールド脱落（全境界を E と仮定）→ 9個中5個が C/A/D/F。`rfl` が即拒否
3. **`cascadeReg` 判定が右辺のみ** → 梯子頂上の非正準性を丸一日見落とし。設計文書の
   「ladderToCascade が tail に接続する」という on-orbit 主張の誤りが遅れて発覚

### 0.4 進捗指標の教訓（「なかなか閉じない」の解析）

- 歩数被覆率は誤解を招く: g=2 で 25.3%→99.9% と跳ねたが、**残義務の個数はほぼ不変**（下表 S1–S7）。
  被覆は「未証明義務の個数と各サイズ」で数える（M0 に追記）。
- 遅さの主因は**フルシェイプ照合の規律そのもの**であり、これを省いた箇所（較正事件3）が
  唯一の丸一日の損失を生んだ。規律を前倒しする（M7 新設）ことが最速化である。
- 偽の証明はゼロを維持（較正3件はいずれも主張前に捕捉）。

---

## 第I部 — 共通規律 M0–M7（M1/M4 強化、M6/M7 新設）

### M0. 測定第一の原則（不変 + 指標規律を追記）
構造についての主張は形式化・文書化の前に必ず実軌道測定で検証する。測定と物語を区別してラベルする。
**追記（指標規律）**: 進捗・被覆の主張は**計量単位を明示**する（歩数か・義務数か・レベル数か）。
歩数被覆は単一の巨大区間に支配されるため、義務数を併記しない歩数被覆は禁止。
**新実績:** descLaw の一様カット訂正・regenEntry の解体（615→ descLaw×2 + 200 + 215）・
tail のオドメータ構造・topEntry 閉形式・櫛不活性 — いずれも測定が形式化に先行して正解を与えた。

### M1. 計器検証（強化 — フルシェイプ照合を義務化）
**手続き:**
1. シミュレータは**記録済みアンカーへの厳密一致**で検証してから使う（x2: M1(1..8) 上表）。
   不一致なら結論を保留し計器を疑う。
2. **テープは有界検査付き**（`x2r2_sim.py`: span 可変、両端 assert）。`bytearray` の負インデックスは
   静かに wrap する — 旧 `x2t7_lib.py` は SPAN=2^14 固定で ~14M 歩以降 IndexError（実測）。
3. **配置族の同定は Lean 定義の全フィールド照合**（左右両面・状態・head・位置）。部分シグネチャ
   （右辺のみ・先頭 run のみ）は禁止。較正事件 1〜3 は全てこの違反から生じた。
4. 中間配置の抽出は `(st, pos, left, head, right)` の**5フィールド全て**（較正事件2）。
**実績:** アンカーガードが初回から誤計器を3連捕捉（2026-07-22 の C:1LE 事件に続き本記録でも）。

### M2. カット原則（不変・実績追加）
輸送法則は「総和が一様になるカット点」で宣言する。継ぎ目の煩雑さは機械の性質ではなく
**どこで切るかの性質**。カットが悪ければ動かす。
**新実績:** tail の不規則 211/184\*/265 は frameDigit カットで `27(g−1)+110` の一様則に解消。
descLaw は descIn カットで `9(2^{k−1}−1)` 厳密一様（k=4..9 の6段実測）。regenEntry「不透明な
615歩」はカットを変えると descLaw×2 + 固定200 + 固定215 に解体された。

### M3. タイル+fold+強帰納（不変）+ **M3′ 固定エピソード標準機械**（新設・定式化）
自己相似カスケードは従来どおり タイル → ∀長 fold → 強帰納。
**M3′（chunked-rfl パイプライン）— 固定エピソードの標準証明機械:**
1. 実軌道で端点をフルシェイプ抽出し、**2世代以上でビット同一**を確認（固定性の実証）
2. 頭部走査幅を測定 → 具体部の外は ∀ 変数にできる（∀M ∀R の証明可能性の根拠）
3. `#eval` でチャンク境界配置を**5フィールド**抽出（20–25歩刻み）
4. private `rfl` チャンク群 → `steps_add` 連鎖 → `steps_pos_shift` で ∀p 持ち上げ
5. スクリプトで機械生成する（手書きしない）
**チャンク校正表（実測）:** `rfl` は 25歩 0.28s / 50歩 4.3s / 100歩 39s超で破綻。**20–25歩を標準とする。**
**実績:** E1(200=8×25)、E2(215=8×25+15)、fixedEnd(75=3×25)、regenLawGen_4..7（20/22/25/46刻み、
計 115 チャンクまで実証）。

### M4. 反空虚性統制（強化 — 予告先行測定を第一手続きに）
**手続き:**
1. **予告先行**: 閉形式・構造則は、フィットに使わない世代への予測値を**プローブの docstring に
   書き込んでから**走らせる。的中して初めて「確認」と呼ぶ（フィット点数＝パラメータ数の式は
   予測力ゼロであり、必ず out-of-sample を要求）。
2. ∀定理は (a) 独立証明済み具体レベルへの特殊化一致、(b) 未接地レベルへの到達、(c) 総歩数の
   実測再現、を `decide` でファイル内に固定する。
3. **一般化定理には `_specialises` 統制**: 旧定理が新定理の instance であることを別定理で証明する
   （RegenLawGen: `U := pow01(2^{k−1}−2) ++ marker`）。
**実績（予告先行 3/3 的中）:** 奇 topEntry g=7 = 778 716・descIn 13・1-run 20 / 偶 topEntry g=8 =
99 112・descIn 15 / frameDigit g=4 = 3 段。いずれも予測値を docstring に記録してから測定し完全一致。

### M5. 撤回の作法（不変・実績追加）
誤りは削除せず supersession バナーで上書きし、何が・なぜ・どの測定で誤りと判明したかを残す。
**新実績:** fixedEnd 110→75 歩（Lean が110歩文を拒否、turn 35 の取り違えと判明、総和不変）、
ladderToCascade docstring の on-orbit 主張の訂正（定理自体は真のまま）、「99.2%」歩数被覆の
指標訂正（→0.4節）。

### M6. 【新設】改名による一般化（不活性パラメータ移植）
**手続き:** 定理のパラメータ P が実は使われていないと疑われるとき:
1. **頭部走査測定**で P の領域に頭が入らないことを確認（不活性の実証）
2. 保存則を測る（x2 の櫛: `a → a+1` で個数保存）
3. P を自由変数に置換した一般化定義を書き、`#eval` で複数レベル真偽検定（+ 旧定理が instance に
   なる統制も `#eval`）
4. **元の証明スクリプトを、該当部分項を自由変数に改名しただけで再コンパイル**する。
   無修正で通ればそれ自体が不活性の三重目の検証。`_specialises` 統制を付す。
**実績:** RegenLawGen ∀k — `regenLaw_closed` の証明鎖（trailing 4本・lead 1本・anchor 1本・組立）が
改名のみで全通し、倍加フェーズの未証明 75% が1セッションで閉じた。**測定→#eval→移植の三段で、
新しい数学を一切書かずに済む場合がある。**

### M7. 【新設】合成スコープ検査（on-orbit 接続の事前監査）
**手続き:** 2つの GREEN 定理を軌道上で合成する（A の OUT = B の IN と主張する）前に、必ず:
1. 両端点族が実軌道に**フルシェイプで出現**することを走査で確認する（M1-3 の適用）
2. A の OUT パラメータと B の IN パラメータの**同定に必要な恒等式を列挙**し、それぞれを
   [PROVEN]/[MEASURED]/[OPEN] でラベルする
3. docstring の on-orbit 主張は、この監査に通ったもののみ書く
**根拠:** ladderToCascade の docstring は「OUT が tail に接続」と主張していたが、頂上は非正準で
接続せず（較正事件3）、発覚が1日遅れた。**定理が真でも合成主張は別の命題**である。
**現在の未監査継ぎ目:** S1（headLaw OUT ↔ ladderToCascade IN のマーカー/パッド同定）— 下表。

---

## 第II部 — x2 完全証明の手法（改訂版実行計画）

**目標:** `h_doub ∀g` を閉じ、`x2_nonhalt` を発火する（`h_init`/`h_low` は済）。

### 確定アーキテクチャ（偶数 g、K = g+8）— 2026-07-23 実測+証明で確定

```
M6(g) ─topEntry(g)────▶ descIn(K−1)        [S3: 輸送OPEN — コストは閉形式で既知]
      ─headLaw────────▶ regenIn 5           [PROVEN ∀k]
      ─(S1: マーカー/パッド同定)──────────   [OPEN — 純List恒等式のはず、未証明]
      ─ladderToCascade─▶ cascadeReg(g+8)    [PROVEN]
      ─topRung────────▶ cascadeRegGen(g+9)  [PROVEN ∀k]  ← 本日新設
      ─(S2: 74歩継ぎ目)─▶ tail IN            [MEASURED固定 — 未構築]
      ─tailLaw────────▶ M1(g+1)             [PROVEN ∀j]
```

### 残義務表（旧 R1–R6 を置換。R1/R2/tail/topRung は完了）

| # | 義務 | 手法 | 規模 | 状態 |
|---|---|---|---|---|
| S1 | マーカー/パッド同定継ぎ目: `descMarkInner n M = ladderMarker 5 n ++ marker'` かつ `R = zeros 16 ++ R''` の提示 | 純 List 恒等式 + `regenIn_pad`。M の具体形は S3 の OUT に依存 | 小〜中 | [OPEN] |
| S2 | 74歩継ぎ目（topRung OUT → tailLaw IN） | M3′ 標準機械。端点実測済（g=2/4 同一） | 小 | [OPEN・unblocked] |
| S3 | topEntry 輸送（偶数枝から） | h_low 方式のパリティ別 ∀TAIL 連鎖。コスト `384·2^g+53g+384` 既知 | **h_low 弧と同規模** | [OPEN・最大] |
| S4 | doubPhase 組立 + 反空虚性 | `steps_add` 連鎖。総和 2 119 015 / 8.43M / 33.66M の厳密再現（M4） | 中 | [OPEN] |
| S5 | 義務 H（§5am 族 → init） | `realizeM1_port` + `BlankNorm` + `steps_pos_shift`、`probe_g01` から | 中 | [OPEN] |
| S6 | 最終組立 + cold audit + red-team | `exact x2_nonhalt …` → 全 `#print axioms` 監査 | 小 | [OPEN] |
| S7 | **奇数枝**: 奇 g 梯子頂上の Lean 形状定義・奇 topEntry・奇 tail 入口 | まず形状の M1-3 フルシェイプ抽出から | 中〜大 | [OPEN] |

**順序:** S2（即可能・小）→ S3 偶数枝（最大・律速）→ S1（S3 の OUT が M を与える）→ S4 偶数 →
S7 奇数枝 → S4 完成 → S5 → S6。

### リスク・監視項目
- `descFold_all` の精緻化異常（同一証明が2回 400s 超ハング後、以後 0.4s）— 原因未特定。
  再発時はチャンク分割で回避し記録する。
- 奇数枝は**全面的に Lean 未着手**（S7）。偶数枝の定理群がどこまで流用できるかは、奇装飾
  （マーカー先頭 `1^20`・頂上 `0^13`）の形状定義後でないと判定できない。
- `omega` は `2^k` 原子を含む目標で既定 maxRecDepth を破ることがある → 付録の定型で回避。

### 付録 — Lean 戦術工学ノート（本セッションで確立した定型）
- 長い `rfl` は分割必須: 20–25歩/チャンク（校正表は M3′）。境界は5フィールド抽出。
- `omega` + `2^k`: `generalize (2:Nat)^(…) = A` で原子化し、必要なら `set_option maxRecDepth 8000`。
- 定義等式で `rw` が構文照合に失敗する場面は `show` で目標を張り替えるか `exact ih …` で渡す。
- ∀p 持ち上げ: 位置0で証明 → `steps_pos_shift (d := p)` → `rw [show (0:Int)+p = p …]` →
  `cfgPos (by omega)` の4行定型。
- Mathlib 名は存在しない（`pow_add`/`ring`/`norm_num` 不可）。冪は `Nat.pow_succ` /
  `Nat.pow_le_pow_right` + `omega` で。

---

## 第III部 — carry-transparent 島の手法（post-x2、変更なし）

対象 ~5–8 機械（`CARRY_DICHOTOMY_2026-07-11`）。第II部の道具箱（M3′/M6/M7 を含む改訂版）を
転写。x2 が閉じれば各機械が同型の決定候補。(K) には触れない。

## 第IV部 — BB(6)/(K) の手法（外部段階、変更なし）

不可能性群（No-Structure、decider-preemption、coisometry no-go、AIU/ENT、√階層）は再挑戦しない。
生きたレバー: 定理E / M₂ᵒᵈᵈ / o4-first（1行スカラー、実測 1519 倍内側）。外部送付はオーナーの
per-send go-ahead 必須。o7/SpaceNeedle/o17 は新着想待ち。

---

## 一枚図（更新）

| 対象 | 手法 | 段階 |
|---|---|---|
| x2 h_doub 偶数枝 | S2→S3→S1→S4（M0–M7 の下で） | **実行** — 定理6本 GREEN、残り3継ぎ目+topEntry |
| x2 h_doub 奇数枝 | S7: フルシェイプ抽出から | 開（形状未定義） |
| x2 総組立 | S5→S6 | 待機 |
| carry島 ~5–8機械 | 改訂道具箱の転写 | post-x2 |
| (K) 14機械 | 定理E/M₂ᵒᵈᵈ/o4-first を外部へ | gated — go-ahead 待ち |
| o7/SN/o17 | 新着想待ち | 開 |

**機械は未決定。ラベルは未昇格。x2 は [OPEN]、BB(6) は (K) 壁の背後。**

---

## 追補（2026-07-24 夕）— M8. 名前付き配置の規律 (named-config discipline)

`topEntryEven` の配管失敗（数学完了・#eval 済みなのに rewrite 照合で停止）の事後分析から。
根拠と撤退基準は `STRATEGY_REVIEW_2026-07-24.md` §5。

1. **合成は名前付き `def` 族の上でのみ**。無名 `⟨.E, p, ⟨…⟩⟩` リテラルを跨ぐ `rw` は禁止。
   各 phase の IN/OUT を `def` として先に定義し、原始定理をその形で restate してから合成
   （成功例: `descIn`/`regenIn`/`cascadeReg`。失敗例: T7TopEntry の 4 相直結）。
2. **算術の綴りはファイル内単一形**（`2*h+2+k` を保持、簡約は最後の `cfgPos`/`omega` のみ）。
3. **∃ witness は明示項**。`⟨_, _, ?_⟩` は `some` の下の metavar を合成できない。
4. **配管予算則**: 1 合成 ~45 分超で停止 → 緑部品を銀行 → sorry 明記スタブ（defaultTargets 外）
   → M8-1 で再アプローチ。
