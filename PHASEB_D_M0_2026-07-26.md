# `D` — M0 groundwork for the Lean development (2026-07-26)

`D = 1RB0RA_1LC0LE_0LD0LB_1RA0LF_1LB0RD_1LD---`, confirmed `(2,4)` at `4·10⁸`, a distinct
transition graph from `x2`'s, in the 1104 list.  Work in the REVERSED form (same 1104 entry, and it
grows rightward like `x2`):

    D^R = 1LB0LA_1RC0RE_0RD0RB_1LA0RF_1RB0LD_1RD---

**No machine decided beyond `x2`.  No label upgraded.**

## Milestones (left-frontier record clusters)

| t | width | head pos | state | span |
|---:|---:|---:|---|---:|
| 160 | 20 | −4 | A | — |
| 894 | 50 | −8 | A | 734 |
| 1 420 | 61 | −10 | A | 526 |
| 14 130 | 185 | −16 | A | 12 710 |
| 66 906 | 422 | −24 | A | 52 776 |
| 291 168 | 893 | −32 | A | 224 262 |
| 1 196 412 | 1832 | −40 | A | 905 244 |
| 4 846 662 | 3707 | −48 | A | 3 650 250 |
| 19 488 198 | 7454 | −56 | A | 14 641 536 |

* milestone state is **`A`** at every one (`x2`'s is `E`);
* head position steps by a uniform **`−8`** from the 4th milestone on (`x2`'s is `−6`);
* span ratios `4.15, 4.25, 4.04, 4.03, 4.01` → **4**, and width ratios → **2** — the same `(2,4)`
  fingerprint as `x2`.

## Marker words

    even-type : 0^33 ++ pow10 k                       (dense (1 0) comb, 43+ runs and continuing)
    odd-type  : 0^2 (1 0)^4 0^6 ++ pow10 k

versus `x2`'s `0^21 ++ uUnits 1 ++ (1 :: 0^10 ++ ones 1021 ++ m1casc …)`.

> `x2` is a **CASCADE** doubler (sparse `uUnits` register + descending `2^k − 3` `ones` blocks);
> `D` is a **COMB** doubler (pure dense `pow10` register).  Different species, same alphabet.

## Right-frontier plateaus (the ladder)

    fine   : reach 197, 200, 203, 206, 209   — +3 per plateau, span ≈ 510 each
    coarse : reach 398, 861, 1792, 3659, 7398 — doubling, spans 45 171 / 218 645 / 887 995 /
             3 579 863 / 14 371 555 (ratio → 4)

## What this gives the Lean development

1. the milestone family shape (`state A`, `pos −8k`, `0^33 ++ pow10 k` / `0^2 (10)^4 0^6 ++ pow10 k`);
2. the epoch decomposition to bracket (`low phase → entry → ladder → rung → tail`, as `x2`);
3. exact anti-vacuity anchors: the milestone times and spans above.

`TapeCalc` and the `pow10`/`pow01`/`ones`/`zeros` algebra are already library; the machine-specific
work is `D`'s own words and tiles.

**No machine decided beyond `x2`.  No label upgraded.  Push HELD.**
