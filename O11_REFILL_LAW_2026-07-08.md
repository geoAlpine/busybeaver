# o11's refill law, EXACTLY — the mod-4 statement refines to a complete arithmetic model; o11 reduces (grid-exact) to ONE ×3/2 orbit sampled at self-determined times; the 5th draw computed; a prior [OBSERVED] claim refuted (2026-07-08)

*Deep dive on the sharpest handle of the Mahler sea (`MAHLER_SEA_CLASSIFICATION_2026-07-07.md` §3):
o11 = `1RB1RE_1LC1LD_---1LA_1LB1LE_0RF0RA_1LD1RF`, halt = C reads 0 with left neighbor 0 (00-gap,
`REDUCE_O11_O16.md` §1 [PROVEN]). SOUNDNESS: all rules are exact concrete simulation on stated grids —
labels [PROVEN on grid] / [OBSERVED] / [MODEL] / [OPEN]; the arithmetic continuation beyond simulation is
always labeled as such. **No machine decided.** Scripts: `o11_refill_anatomy.py`, `o11_refill_grid.py`,
`o11_refill_rules.py`, `o11_refill_fixedpoint.py`, `o11_refill_arith.py`.
Interpreter `/Users/aokiyousuke/quantum-ecc/.venv/bin/python`. Not committed.*

## 0. Headline

> **o11's dynamics on B/right-extreme milestones `(k = leading block, m = sea)` is a SIX-RULE
> transition system, each rule verified exact on large grids with zero mismatches — and iterating the
> rules is a complete arithmetic model of the machine.** The whole machine collapses to: one affine
> ×3/2 map `T(m) = ⌊3m/2⌋+4` iterated from the FIXED seed `m=2`, sampled at self-determined indices
> `e = ⌊k/4⌋`; the sampled value's parity / mod-4 residue decides everything. Consequences:
> (i) the "fatal ⟺ collapse ≡ 0 (mod 4)" criterion is the immediate channel of a THREE-channel exact
> criterion (it refines value-dependently — no fixed modulus is exact); (ii) the collapse-value orbit
> has a CLOSED FORM `c' = 3·T^{⌊(c−2)/4⌋}(2) + δ`, making o11's protection a pure arithmetic statement —
> the sharpest decision target in the sea; (iii) the 5th refill draw (t ≈ 6.4×10²⁸, far beyond any
> simulation) is COMPUTED: `c₅ = 459,271,258,863,237 ≡ 1 (mod 4)` — safe, and epoch 5 is
> unconditionally safe, so **o11 provably-on-the-grid-laws survives into epoch 6**; the first genuinely
> open residue draw is `T^{114,817,814,715,808}(2) mod 4` (a ~2×10¹³-digit integer — Mahler-hard);
> (iv) the naive annealed halt-lean is **p\* = 1/2 per epoch** (not the classification's 1/4) — the
> largest in the frontier; expected ~2.6 more epochs [MODEL]. Also: the classification's "k ≡ 3 rows
> safe" is **REFUTED** ((3,2) halts at step 740 — a budget artifact; the model predicted it).

## 1. The refill/collapse event, exactly (task 1) — anatomy `[OBSERVED, exact]` (`o11_refill_anatomy.py`)

Tracked every right-extreme configuration change around the four real events (t=9, 46, 271, 28100):

- **Collapse**: the epoch's terminal reorganization ends in a **pure block `[1^c]`, state F, head ON the
  last 1** (t=9 `F [3]`, t=46 `F [9]`, t=271 `F [26]`, t=28100 `F [303]`).
- **Refill**: exactly **7 steps** later the machine is at the B/right-extreme milestone
  `[1^{c−2} 0 1 0 1]` = epoch seed `(k, m) = (c−2, 2)`. Local mechanics (read off the trace): F extends
  the block by one, then two E-steps punch single 0s two cells apart, A/B rebuild — the block donates
  its last ~3 cells to seed the new sea `(10)^2`.
- **The sea seed is EXACTLY m=2 at every refill** — RESETTING memory confirmed at the event level:
  4 real refills + 15 grid refills (`o11_refill_grid.py`) + 1004 R6 unit tests (below): `(c−2, 2)`
  every single time, never any other seed.

**R6 (refill map) `[PROVEN on grid]`:** `[1^c]`, state F, head on last 1 → milestone `(c−2, 2)` in
exactly 7 steps: **1004/1004** for c = 3..1000 plus spot magnitudes 4155, 4156, 65536, 65537, 100003,
100004 (all four residues, three decades). The refill itself NEVER halts — all fatality lives in the
collapse that precedes it (below).

## 2. The six rules `[PROVEN on the stated grids]` (`o11_refill_grid.py`, `o11_refill_rules.py`)

With `T(m) = ⌊3m/2⌋ + 4` (the sea law, re-verified everywhere):

| rule | statement | grid | exact |
|---|---|---|---|
| R1 in-epoch | `(k,m) → (k−4, T(m))` for k ≥ 5 | k=4..60 × m=1..120 (+ spots (245,4156),(301,1226),(85,1843),(61,2768)) | 6840/6840 + 4/4 |
| R2 boundary | `(4,m) → (1, T(m)−1)` | m=1..120 (in R1 grid) | ✓ |
| R3 terminal | `(3,m) →` collapse `c = 3m+5` (m odd) / `3m+6` (m even) | m=1..400 + 1226, 1843 | 400/400 |
| R4 terminal | `(2,m) → HALT` | m=1..400 + 1226, 1843 | 400/400, all halts = `C reads 0, left nbr 0` |
| R5 terminal | `(1,m) → HALT` iff m odd; else collapse `c = 3m+3` | m=1..400 + 1226, 1843, 4156 | 400/400 |
| R6 refill | `[1^c]@F → (c−2, 2)` in 7 steps | c=3..1000 + spots to 10⁵ | 1004/1004 |

- The countdown `k → k−4` is DETERMINISTIC (independent of the sea's residue itinerary); the epoch from
  seed `(k, 2)` reaches its terminal node `(k mod 4 or 4-boundary form, T-iterate)` in exactly
  `e = ⌊k/4⌋` sea steps.
- Bookkeeping note: the `(1, m)` milestone tape is bit-identical to the pure sea `(10)^{m+1}` (verified
  cell-level), so R5 can be read as: *a pure sea of s alternations collapses safely iff s is odd*
  (`c = 3s`), and R2 as the `k=4` epoch dropping into the pure-sea form.
- **All fatality is concentrated in the terminal collapse**: R1/R2/R3/R6 have no halt branch anywhere on
  the grids; the 00-gap forms only in the collapse reorganization from `(2, m)` (always) or `(1, m odd)`.
  Halt anatomy is the unique gate (`C,0` with left neighbor 0) in every one of the 600+ observed halts.

## 3. The arithmetic model = o11 (grid-exact reduction) (`o11_refill_arith.py`)

Iterating R1–R6 from epoch seed k (sea seed always 2), with `r = k mod 4`, `e = (k−r)/4`,
**sample `m* = T^e(2)`**:

> - `r = 2`: **HALT** (at this epoch's terminal collapse)
> - `r = 0`: **HALT iff m\* even**; else `c = 3m*`
> - `r = 1`: **HALT iff m\* odd**; else `c = 3m* + 3`
> - `r = 3`: never halts; `c = 3m* + 5` (m\* odd) / `3m* + 6` (m\* even)
> - refill: `k' = c − 2`, sea re-seeded to 2.   Blank orbit enters at k = 1 (after the t=9 transient `[1³]`).

**Predict-and-confirm** (all in `o11_refill_arith.py`):
- **PC0**: seeded `(301,2)` standalone reproduces the real orbit's epoch-4 milestone chain
  step-delta-exact (7 generations) — the standalone protocol is faithful.
- **PC1**: first-epoch outcome (halt vs exact collapse value c) on the full (k,m) grid k=2..17 ×
  m=1..16: **256/256 exact** — including general sea seeds m ≠ 2 (`m* = T^e(m)`).
- **PC2a**: multi-epoch halt classification on the same grid, budget 20M, censoring handled honestly
  (model-halt with chain-cost estimate < budget/2 must halt; sim-halt where the model escapes = refutation):
  **148/148 must-halts confirmed, 108 censored cells all consistent, 0 refutations**.
- **PC2b**: the pure-collapse family `[1^c]` (state F), c = 3..58, budget 40M — the per-draw criterion
  (§4) + cascades confirmed with **0 refutations** across all four residue rows (c≡0: 14/14 halt;
  the H-cells in the c≡1,2,3 rows are exactly the model's multi-epoch cascade halts, e.g. c=5 → k=3 →
  c'=12 → k'=10 ≡ 2 → halt).
- **PC3**: single-rule spot checks at real-orbit magnitudes (m = 1226..4156, e.g. R1 (245,4156) →
  (241,6238); R5 (1,4156) → collapse 12471, (1,1843) → HALT): all exact.

## 4. The fatal criterion, final form (task 1 answer) — it REFINES, value-dependently

In collapse-value coordinates (`k = c − 2`), the exact per-draw criterion (verified identical to the
model on c = 3..2000, and by PC2b runs):

> **Draw c is fatal (its epoch halts at its own terminal collapse) ⟺**
> **c ≡ 0 (mod 4)**  [immediate: k ≡ 2 — the observed-data criterion]
> **OR c ≡ 2 (mod 4) AND T^{(c−2)/4}(2) even**  [parity channel at r=0]
> **OR c ≡ 3 (mod 4) AND T^{(c−3)/4}(2) odd**  [parity channel at r=1]
> (c ≡ 1 (mod 4) is never fatal at its own epoch.)

So `MAHLER_SEA_CLASSIFICATION`'s "fatal refill ⟺ collapse ≡ 0 (mod 4)" is exactly the FIRST channel;
the criterion is **not mod 8, not any fixed modulus — it is value-dependent** through the parity of a
T-iterate whose index is determined by c (the parity sequence of `T^i(2)` has no period ≤ 1500 on a
3000-window `[OBSERVED]`, so no congruence in c can capture it).

**REFUTED `[was OBSERVED, now disproved by run]`:** the classification's "standalone rows k ≡ 3 (mod 4)
safe at every m tested". Re-running its own protocol: row k=3 is `HH..HH..HH......` (m=1..16); (3,2)
**halts at step 740**. The old '.'-cells were budget censoring; the model predicts every H and every
'.' of the whole historical grid (PC1/PC2a), including the "quasi-periodic mixing" in the k≡0,1 rows —
that mixing is exactly `parity(T^e(m))` plus multi-epoch cascades. The headline claims that survive
unchanged: k≡2 rows all-fatal (R4), the real orbit's residue itinerary, and "collapse ≡ 0 (mod 4) ⇒
fatal" (as one channel).

## 5. Fixed-point closed forms (task 2) `[PROVEN]` (`o11_refill_fixedpoint.py`)

The sea map's branches and their integer fixed points (the o4/O15 trick, at (2,3)):

| branch | map | fixed point | run closed form |
|---|---|---|---|
| m even | `m' = 3m/2 + 4` | **x = −8** | maximal even-run = `v₂(m+8)` |
| m odd | `m' = (3m+7)/2` | **x = −7** | maximal odd-run = `v₂(m+7)` |

*Proof:* distance to the branch fixed point multiplies by exactly 3/2 on a same-branch step
(`m'+8 = (3/2)(m+8)` even; `m'+7 = (3/2)(m+7)` odd), and branch membership is 2-divisibility of that
distance. ∎ Verified exhaustively m = 1..2×10⁵ (200000/200000) and at every run start on the orbit
`T^i(2)`, i ≤ 4000 (1973/1973).

- **The task-brief guess x = −8 is right for the even branch; the odd branch is x = −7.**
- **mod-4 dictionary** (what the fatal criterion actually reads): `m ≡ 0/2 (mod 4) ⟺ v₂(m+8) ≥ 2 / = 1`;
  `m ≡ 1/3 (mod 4) ⟺ v₂(m+7) ≥ 2 / = 1` — o11's fatality is literally the next depth level of the
  2-adic countdown.
- **Mirror coordinates:** `W = m+8` makes even steps EXACTLY ×3/2 (odd: `W' = (3W−1)/2`); `U = m+7`
  makes odd steps exactly ×3/2. o11's residue itinerary is the **v₂-depth process of a ×3/2 orbit — the
  same object as Antihydra** (v₂ of ×3/2), o4 (v₃ of ×4/3), o15 (v₃ of ×8/3). The mirror ladder gains
  its fifth member, at Antihydra's own (p,q) = (2,3).
- **Run cap:** `run(m) ≤ log₂(m+8)`; on the orbit (`m_i ~ C·(3/2)^i`) run at sea-index i ≤
  `i·log₂(3/2) + O(1) ≈ 0.585·i` (max observed run on i ≤ 4000: 12; cap never violated).
- **Budget/criticality (o11's row):** the epoch budget is the leading block k, drained at the CONSTANT
  rate −4 per sea step (R1) — the drain does **not couple to the residue itinerary at all** (contrast
  o4/Antihydra, where runs drain the ledger). The run-cap/budget-slope criterion is **inapplicable**:
  in-epoch fatality is structurally absent (no halt branch in R1/R2 on the grids), and the whole
  fatality channel is ONE sampled residue per epoch at the self-determined terminal index `e = ⌊k/4⌋`.
  o11's criticality row: run-cap slope `log₂(3/2) = 0.585` [same depth process as Antihydra], budget
  RESETTING with deterministic drain, single-run kill N/A, per-epoch exposure = 1 draw, naive p(fatal) = 1/2.

## 6. The protection statement, final form (task 3)

> **[OPEN — o11 does not halt] ⟺ the arithmetic recursion of §3 never halts ⟺ the ×3/2 orbit
> `{T^i(2)}`, sampled at the self-determined index ladder `e₁ = 0, e₂ = 1, e₃ = 6, e₄ = 75,
> e₅ = 114,817,814,715,808, …` (`e_{n+1} ≈ (3/4)·T^{e_n}(2)`), never lands in the fatal residue class
> of the current row** (r=0: even; r=1: odd; r-transitions per §3/§4).

This IS an o2/Antihydra-style seeded-(K) instance — with three sharpenings that make it the cleanest in
the sea: (1) the seed is the absolute constant **2** (re-imposed at every refill; RESETTING confirmed
event-exact), not an orbit-dependent quantity; (2) the collapse-value orbit is CLOSED-FORM,
`c' = Φ(c) = 3·T^{⌊(c−2)/4⌋}(2) + δ(c mod 4, parity)` — pure arithmetic, no tape left anywhere; (3) the
fatality read-out is the shallowest possible depth data (parity / v₂-level ≤ 2 of `m*+8`, `m*+7`). The
"re-coordinatization, not a reduction in difficulty" verdict stands — each sample index is
doubly-exponential in the previous (`T^{e₅}(2)` has ~2.0×10¹³ digits), so deciding the n-th draw needs
exactly the quenched ×3/2-residue theorem of `NEW_MATH_PROGRAM` — but o11 is now a ONE-LINE arithmetic
question, the sharpest decision target in the sea.

## 7. Real-orbit census + margins (task 4) `[OBSERVED + arithmetic continuation]`

Only **4 draws are simulation-reachable** (refills doubly-exp sparse); the 5th is computed from the
grid-exact laws; the 6th is genuinely open:

| draw | c | c mod 4 | epoch seeded (k, r) | decisive sample | outcome |
|---|---|---|---|---|---|
| c₁ | 3 (t=9, blank transient) | 3 | (1, r=1) | `T⁰(2) = 2` even (needed even) | SAFE — coin 1 |
| c₂ | 9 (t=46) | 1 | (7, r=3) | `T¹(2) = 7 ≡ 3 (mod 4)` (needed {0,3} → r'=0) | SAFE — coin 2 |
| c₃ | 26 (t=271) | 2 | (24, r=0) | `T⁶(2) = 101` odd (needed odd); `101 ≡ 1 (4)` → k₄ ≡ 1 | SAFE — coin 3 |
| c₄ | 303 (t=28100) | 3 | (301, r=1) | `T⁷⁵(2) = 153,090,419,621,078` **even** (needed even); `≡ 2 (4)` → k₅ ≡ 3 | SAFE — coin 4 |
| c₅ | **459,271,258,863,237** `[arith, grid-exact laws]` | **1** | (459,271,258,863,235, r=3) | epoch 5 unconditionally safe | SAFE (t ≈ 6.4×10²⁸) |
| c₆ | = `3·T^{e₅}(2) + 5/6`, `e₅ = 114,817,814,715,808` | **OPEN** | k₆ even: r₆ ∈ {0,2} | `T^{e₅}(2) mod 4` — a ~2×10¹³-digit computation | **OPEN** |

- **Epoch 4 (running now, at t ≥ 20M with 60 of its 75 countdown steps to go) hinged on the parity of
  `T⁷⁵(2)` — it is even, so the current epoch is safe** and o11 reaches epoch 6 alive (on the grid-exact
  laws; the real orbit is machine-verified only to t = 4×10⁷). Epoch 6's row is decided by
  `T^{e₅}(2) mod 4`: classes {1,2} → k₆ ≡ 2 → **halt at epoch 6**; {0,3} → k₆ ≡ 0 → a further parity coin.
- **Margins**: the survived events are pure binary residue coins — 4/4 safe (naive 2⁻⁴ = 6%); there is no
  near-miss metric beyond the residues themselves (fatality is a codimension-0 residue class, not a
  distance-to-zero ledger).
- **Annealed `[MODEL]`**: per-epoch survival = **1/2 in every live row** (r=0,1: parity coin; r=3: the
  next-row coin) — o11's naive halt-lean is p\* = 1/2 per epoch, sharper than the classification's 1/4
  and the largest in the analyzed frontier; expected remaining epochs from the current r=3 state: **2.6**;
  P(survive n more) ~ 2⁻ⁿ. What protects o11 observationally is the tower timescale (epoch n+1 is
  ~(3/2)^{(3/4)·length of epoch n} long) — each coin costs a doubly-exponential wait.

## 8. Soundness ledger

- R1–R6 are `[PROVEN on the stated grids]` (exact runs, zero mismatches, halts independently anatomized
  as the unique gate); the fixed-point run laws are `[PROVEN]` (2-line proof + exhaustive check). The
  arithmetic model is their iteration: **exact wherever simulation can see** (PC0–PC3, 0 refutations),
  but its extension to unbounded (k, m) is NOT proven — c₅ and the epoch-4 survival are labeled
  `[arithmetic consequence of grid-exact rules]`, never machine facts.
- REFUTED and corrected this session: "k ≡ 3 rows safe" (budget artifact); the classification's naive
  p\* ~ 1/4 (correct value 1/2); the collapse-offset "milestone start = collapse − 2" is confirmed and
  is now R6 with the 7-step mechanics.
- The protection (§6) is `[OPEN]`. **No machine decided. No label upgraded.**

## Reproduce
`o11_refill_anatomy.py [N]` (collapse-window traces) · `o11_refill_grid.py [kmax]` (standalone epoch
grid) · `o11_refill_rules.py [kmax mmax tmax]` (R1–R6 unit grids) · `o11_refill_fixedpoint.py [MMAX]`
(run laws, cap, dictionary) · `o11_refill_arith.py` (model, PC0–PC3, census). Basis:
`MAHLER_SEA_CLASSIFICATION_2026-07-07.md`, `REDUCE_O11_O16.md`, `O4_RUN_STRUCTURE_2026-07-07.md`,
`O15_FIXEDPOINT_2026-07-07.md`, `ANTIHYDRA_LEDGER_UNIFICATION_2026-07-07.md`, `msea_*.py`.

**No machine decided. No label upgraded.**
