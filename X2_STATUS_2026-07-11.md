# The integer-×2 machine — authoritative status (2026-07-11)

*Consolidated, CORRECTED status of the whole ×2 arc. This supersedes `X2_FRONTIER_MAP_2026-07-11.md`, whose central
framing ("o4 wall in base-2 / counter-dependent") was REFUTED mid-campaign (§2). Machine
`1RB0RE_1RC---_0LD1LE_0RE1LD_1RF0LC_0RA1RE`, the cleanest integer-×2 base-2 doubling odometer from the 1104-holdout
census. NOT decided. The honest record includes the correction cascade — every optimism that the careful work refuted.*

## 1. Top line

**NOT decided. The machine is carry-TRANSPARENT (decidable-in-principle, NOT (K)/o4-wall-hard), the low phase is
PROVEN safe ∀g, and the only open piece is the symbolic composition of the doubling-phase episodes ∀g — which needs
Lean-level tooling (List induction + exponential 2^K arithmetic), not a new idea.** Adversarial red-team (5 attacks,
to g≈15 / 2.3·10¹²) found no break.

## 2. The correction cascade (the honest arc)

| stage | claim | corrected by |
|---|---|---|
| launch (mine) | "×2 is outside (K) ⟹ decidable / the template proves non-halt outright" | closure + route A |
| route A (`X2_TEMPLATE_PROOF`) | "even-gap length counter-dependent ⟹ **o4 wall in base-2**" | **carry-calculus + main-loop adjudication** |
| carry-calculus | "finite chain, all ingredients present, **engineering-volume only**" | **composition (§4): 3 real gaps** |
| composition | "closes with Lean tooling" | *(not yet verified — treated as OPEN)* |

The **main-loop adjudication** (`x2_verify_gaps.py`) was decisive: E-met gaps ≥3 are `{4,6,8,10,14,18,22}` EARLY
(step<200k) but STABILIZE to `{6,10,18}` LATE (step≥200k); `{4,8,14,22}` are immature transient only. Route A looked
only at the transient (its data ended at step 188125). **The mature behavior is CONSTANT — carry-transparent, not
counter-dependent.** Independently re-verified; red-team confirmed to g=15/2.3·10¹² (period-2 `{10,18}`/`{6,10,18}`).

## 3. PROVEN / OPEN ledger

| item | status |
|---|---|
| halt gate `HALT ⟺ E meets a gap of length exactly 3` | **PROVEN** (from table, ±control) |
| parity-only finite-state routing | **PROVEN** |
| doubling closed forms `b=2^k−3`, `v=2^k−2` | **PROVEN** |
| R/L/D cycle closed forms | **PROVEN** (certified ≤40; red-team ≤200) |
| **low phase M1(g)→M6(g) ∀g** (event ledger {18,10,2(g+1),6-iff-even}, never 3) | **PROVEN** (symbolic induction, `x2cc_prove` 4/4) |
| C1 chew-body ∀k,r / C2 separator ∀k,s | **PROVEN** |
| chew-fold ∀k,r / per-block cascade step ∀k,r,s | **PROVEN** (0 gap≥3) |
| eraser-even channel — the genuine one, comb-repack `(01)^m→1^{2m}` | **PROVEN, Lean** (`lean/X2.lean` sweepEF_even) |
| doubling full transport g=2..6 | machine-checked (not symbolic ∀g) |
| **cascade fold over g+6 non-uniform 2^j−3 blocks** | **OPEN** (G1: needs List induction) |
| **register-rebuild + doubling arithmetic** | **OPEN** (G3: couples to 2^K) |
| halting overall | **OPEN**; adversarial support to g≈15 / 2.3·10¹², no latent gap-3 anywhere (90 milestones to 3.1·10¹¹) |

## 4. The three exact gaps to a decision

- **G1 — cascade fold** `[OPEN, representational]`: composing the per-block step over `g+6` blocks of DISTINCT lengths
  `2^j−3` needs variable-length non-uniform List induction. The affine Python executor (`x2cc_symb`) cannot represent
  a symbolic block count or `2^j`; no uniform `config(p)→config(p−1)` shift exists (entry sizes 61,29,13,5,1). **Needs
  a Lean-style List induction** (o4 `prefix_bodies` / o3 `body_iter`).
- **G2 — big-block sweep/entry/repack** `[OPEN, engineering]`: bounded/uniform but verified per-g (g=2..6) only, not
  lifted to parametric lemmas.
- **G3 — register-rebuild (deepest)** `[OPEN, representational]`: reconstructing `M1(g+1)` needs
  `2·(comb-total) + corrections = 2^(K+1)−3`, coupling the exponential high part `2^K` to the transport —
  unrepresentable in the affine executor. **Explicit exponential arithmetic, NOT a (K)-wall.**

**None of the three is a (K)/Mahler wall.** They are tooling/representation limits + explicit exponential arithmetic.

## 5. The honest next step

Formalize the doubling transport in **Lean** (List induction over the cascade + `2^K` arithmetic), exactly the
architecture o4/o3 used (`Suffix.lean` `generation_odometer`, `O3.lean` `body_iter`). If G1+G2+G3 close in Lean, the
machine is DECIDED (the first BB(6) frontier decision), pending red-team + axiom audit. Honest caveat: o3's body glue
was also "finite composition" and took real work; G3's 2^K coupling is the deepest. No optimism until Lean closes it.

## 6. Significance

Even un-decided, this arc produced: the carry-transparent/opaque dichotomy (`CARRY_DICHOTOMY`, drawing the exact (K)
boundary — transparent = the small integer-×2 island, opaque = the rest of the frontier); a decoded rigid register
where the record expected a (K)-orbit; the low-phase non-halt proven ∀g; and a fully-mapped path to a decision through
explicit (non-(K)) tooling. And a clean record of ~6 optimisms refuted by careful work — the discipline holding across
every stage.

## Sources
Phase 1: `X2_DECIDABILITY`, `X2_CLOSURE`, `X2_TEMPLATE_PROOF`, `X2_ARITHMETIC_PROOF`, `X2_PARITY_ORDERING`. Phase 2:
`X2_CARRY_CALCULUS` (+ `x2_verify_gaps.py` adjudication). Phase 3: `X2_COMPOSITION`, `X2_REDTEAM`. Weapon frame:
`WEAPON_ANALYSIS`, `CARRY_DICHOTOMY`. Lean: `lean/X2.lean`. No machine decided. No label upgraded.
