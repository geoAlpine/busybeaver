# o13 & o14 fixed-point closed forms — the Mahler-sea ×3/2 engines pinned to o11 depth (2026-07-08)

*Extends the fixed-point / closed-form program (`O11_REFILL_LAW_2026-07-08.md` — the model) to BB(6)
cryptids o13 and o14. Both carry a ×3/2 inner engine; here their branch fixed points, exact v₂ run
closed forms, W-mirror coordinates, criticality rows, and exact halt/protection statements are derived
and machine-verified. SOUNDNESS: labels `[PROVEN]` (elementary + exhaustive machine check) /
`[PROVEN on grid]` (exact simulation, stated grid) / `[OBSERVED]` (numeric, stated horizon) /
`[OPEN]`. Interpreter `/Users/aokiyousuke/quantum-ecc/.venv/bin/python`. Not committed.
Scripts: `o1314_instr.py`, `o1314_epoch.py`, `o1314_rules.py`, `o1314_fixedpoint.py`, `o1314_halt.py`.*

Specs: o13 `1RB0LC_0LC0RF_1RD1LC_0RA1LE_---0LD_1LF1LA` (halt = E reads 0, entered only via `D,1→1LE`);
o14 `1RB0LC_1LC0RD_1LF1LA_1LB1RE_1RB1LE_---0LE` (halt = F reads 0, entered only via `C,0→1LF`).

## 0. Headline

> Both machines reduce, on their designated milestones, to an in-epoch linear countdown plus a ×3/2
> **a-start** recursion whose branch fixed points give **exact v₂ run closed forms** — the same 2-adic
> depth process as Antihydra/o11 at (p,q)=(2,3). **o14 is the o11 twin**: a SINGLE floor correction
> `T₁₄(a)=⌊3a/2⌋+6`, matched fixed points **x_e=−12, x_o=−11** (differ by 1), runs `v₂(a+12)`/`v₂(a+11)`.
> **o13 is the o12-flavor variant**: a parity-split correction `c∈{7,4}=3δ+1` (even→+7, odd→+4),
> **UNMATCHED** fixed points **x_e=−14, x_o=−7**, runs `v₂(a+14)`/`v₂(a+7)`. Both closed forms verified
> **exhaustive a≤2·10⁵ (199 999/199 999 each)** and on-orbit. o13's halt is a clean **per-sweep parity
> gate** (o10 twin: a D→E eat-sweep of run L halts ⟺ L odd, entry-cell-included) — protection = every
> sweep even, **7261/7261** safe. o14's halt is a **00-gap** collision — protection = the C-read-0
> left neighbor is 1, **6382/6382** safe. Both criticality rows: budget = leading counter drained at a
> **constant −2/step decoupled from residues** ⇒ the run-cap/budget ratio criterion is INAPPLICABLE
> (o11's verdict), fatality being a residue draw, not a ledger-drain race. Ledger **RESETTING** (both).
> One refinement to `MAHLER_SEA_CLASSIFICATION`: its `[OBSERVED]` two-valued o13 correction is
> **deterministic by parity** (even+7/odd+4, 0 exceptions), and o14's `+6` is exact — both are the floor
> structure of two-branch ×3/2 maps, not stochastic corrections.

## 1. The rule systems `[PROVEN on grid]` (`o1314_rules.py`, grids a→200)

o13 milestone `[1^a 0 1^b 0 (10)^m]`, state C, head on leftmost 1:

| rule | statement | grid | exact |
|---|---|---|---|
| R1 in-epoch | `[a,b,1^m] → [a−2, b+3, 1^m]` (a≥4) | a=4..120 even × b=1..40 × m=0..3 | **9440/9440** |
| R2 collapse | `[2,b,1^m] → [b+3, 1^m]` (pure `[b+3]` if m=0) | b=1..200 × m=0..3 | **800/800** |
| R3 a-start | `a' = ⌊3a/2⌋ + 7 (a even) / +4 (a odd)` | on-orbit, 14 forward epoch-steps | **14/14** `[OBS]` |

o14 milestone `[1^a 0 1 0 1^b 0 (1)…0 1^4 0 1^4 0 1^2]` (marker tail `4,4,2`), state E, head on leftmost 1:

| rule | statement | grid | exact |
|---|---|---|---|
| R1 in-epoch | `[a,1,b,tail] → [a−2, 1, b+3, tail]` (a≥3) | a=3..80 × b=4..30 × tails `{[4,4,2],[1,4,4,2],[1,1,4,4,2]}` | **6318/6318** |
| R3 a-start | `a' = ⌊3a/2⌋ + 6` | a=5..200 (standalone full epochs) | **196/196** |
| terminal | a hits 1: `[1, b+…, tail]` merge + refill (never a pure single block — marker accretes) | on-orbit | `[OBS]` |

- The a-countdown (a→a−2) is deterministic, independent of the residue itinerary — exactly o11's
  constant-rate leading-block drain (o11 was k→k−4). The ×3/2 growth lives in the a-start (epoch) map,
  where b accumulates 3/2× the a-drain (`c = b+3 = (3·a_start+8)/2` at collapse).
- o13 collapses `9,19,63` (`t=93,372,3328`, doubly-exp) reproduced; refill re-seeds the sea.
  o14 never forms a pure single block (marker tail `(4,4,2)` stable — 1382+ gens `[OBS]`), matching
  `REDUCE_O11_O16`.

## 2. Fixed-point closed forms `[PROVEN]` (`o1314_fixedpoint.py`)

The a-start map's two affine branches (the o4/O11 trick at (2,3)):

| machine | branch | map | fixed point | run closed form |
|---|---|---|---|---|
| **o13** | even a | `a' = 3a/2 + 7` | **x_e = −14** | even-run `= v₂(a+14)` |
| **o13** | odd a | `a' = (3a+7)/2` | **x_o = −7** | odd-run `= v₂(a+7)` |
| **o14** | even a | `a' = 3a/2 + 6` | **x_e = −12** | even-run `= v₂(a+12)` |
| **o14** | odd a | `a' = (3a+11)/2` | **x_o = −11** | odd-run `= v₂(a+11)` |

*Proof:* on a same-parity run the distance to the branch fixed point multiplies by exactly 3/2
(`a'−x = (3/2)(a−x)`, 3 a 2-adic unit) and branch membership is the 2-divisibility of that distance;
hence the maximal run = v₂ of the distance. ∎ Verified **exhaustive a=2..2·10⁵: 199 999/199 999 exact**
for each machine, and on the real a-start orbit.

- **o13's correction is two-valued (7,4) so the fixed points are UNMATCHED (−14, −7), differing by 7**
  (= 3δ+1 structure, the o12 family). **o14's is a single floor constant (+6) so the pair is MATCHED
  (−12, −11), differing by 1** — literally o11's shape (o11: +4 → −8,−7). The general law: `⌊3a/2⌋+C`
  gives x_e=−2C, x_o=−(2C−1); o14 obeys it (C=6), o13 breaks it (C_even=7≠C_odd=4).
- **W-mirror coordinates `[PROVEN]` (exact on a<5000):** o13 `W=a+14` makes even steps pure ×3/2
  (odd: `W'=(3W−7)/2`), `U=a+7` makes odd steps pure ×3/2; o14 `W=a+12` even ×3/2 (odd `W'=(3W−1)/2`),
  `U=a+11` odd ×3/2. Each is **Antihydra's own (p,q)=(2,3)** — the v₂-depth process of a ×3/2 orbit,
  joining the mirror ladder (o11 also (2,3); o4 (3,4); o15 (3,8)).
- **Run cap:** `run(a) ≤ log₂(a − x)` (`x` the larger-magnitude fixed point); on the orbit `a_i∼C(3/2)^i`
  so run `≤ i·log₂(3/2)+O(1) ≈ 0.585 i`. Max branch-run on reachable a-starts: 3 (o13), 5 (o14); cap
  never violated `[OBS]`.

## 3. Protection + criticality (task 3)

**Exact halt statements `[PROVEN mechanic]`:**
- **o13** halts ⟺ some `D→E` leftward eat-sweep scans a run of 1s of length **L odd** (entry-cell
  included; states alternate D,E,D,E…, so the terminating 0 is read in E ⟺ L odd — unit-verified: L=1,3,5,7
  HALT immediately, L=2 safe). Equivalently (`REDUCE`'s entry-excluded convention) the eaten run is
  **even** ⟺ halt. This is o10's structural twin (opposite halt parity).
- **o14** halts ⟺ a `C→F` leftward step lands on a 0, i.e. the cell `C` reads as 0 has **left neighbor 0**
  (a 00-gap) — F fires only on that first cell.

**Criticality rows.** Within an epoch the leading counter drains at a **constant −2/step, completely
decoupled from the ×3/2 residue itinerary** (o11's situation, drain −4). Hence:

| machine | depth process | run-cap slope ρ | budget slope β (memory) | ρ/β | verdict |
|---|---|---|---|---|---|
| o13 | v₂ under ×3/2 | log₂(3/2)=0.585 `[PROVEN]` | −2/step, **RESETTING** (refill re-seeds) | n/a | criterion **inapplicable**: no cumulative ledger a run can drain; fatality = **per-sweep parity draw** |
| o14 | v₂ under ×3/2 | log₂(3/2)=0.585 `[PROVEN]` | −2/step, **RESETTING** | n/a | criterion **inapplicable**: fatality = per-epoch **00-gap** existence draw |

- **Not Antihydra's 1.1699.** The ledger structure differs from Antihydra's cumulative drain-vs-budget
  race: neither o13 nor o14 accumulates a scalar balance that a single deep run must exhaust. Exposure is
  a residue coincidence — **o13 exposed once per sweep** (≈once per milestone; 7261 sweeps / 50M steps, an
  o17-in-value per-sweep gate), **o14 once per epoch** (the 00-gap alignment at a refill). Both share
  o11's "criterion inapplicable / single-run kill N/A" row but with per-sweep (o13) vs per-epoch (o14)
  exposure.
- **Protection as a seeded-(K) instance `[OPEN]`.** Non-halt ⟺ the ×3/2 a-start orbit
  `{a_i} = {T^i(seed)}` (re-seeded at each doubly-exp refill; **RESETTING** confirmed) never presents the
  fatal residue at a sampling point: **o13** — never an odd (entry-included) eat-run at any sweep;
  **o14** — never a 00-gap at any refill C→F alignment. Each sampling reads the shallowest depth datum
  (a parity / v₂-level of `a−x`) of a Mahler-hard orbit ⇒ a re-coordinatization, not a reduction.

## 4. Fatal set + margins (task 4) `[PROVEN nonempty by run]` (`o1314_halt.py`)

Standalone mutants in the reachable-form families (a HALT = concrete fatal member; blank reachability
NOT claimed):

| machine | family | halted/tried | smallest examples |
|---|---|---|---|
| o13 | `[a,b,(10)^m]`, C@leftmost | **337/528** | `(a,b,m)=(2,1,0)`@39, `(2,1,1)`@171, `(2,3,0)`@47 |
| o14 | `[a,1,b,1^f,4,4,2]`, E@leftmost | **169/324** | `(2,1,2)`@222, `(2,3,1)`@289, `(2,5,0)`@383 |

(`(2,1,1)`@171 for o13 and `(2,1,f=2)`@222 for o14 reproduce the classification's exhibited fatals.)
**Margins:** fatality is codimension-0 (a residue/parity class), so there is no distance-to-zero ledger —
the only margin is the parity/neighbor bit itself. Real-orbit census over 50M steps: **o13 every eat-run
even = safe (7261/7261)**, sample L `[2,2,2,2,12,10,8,6,4,2,…]` (always even, tracking the −2 countdown);
**o14 every C-read-0 left-neighbor 1 = safe (6382/6382)**. a-start mod-4 itineraries visit all classes
and both parities (o13 `[10,22,40,67,104,163,…]` parity `0,0,0,1,0,1,…`; o14 `[6,15,28,48,78,10,21,…]`),
so no fixed congruence captures the fatal draw — the parity of `Tⁱ(seed)` is the open object.

## 5. Correction to `MAHLER_SEA_CLASSIFICATION_2026-07-07`

- **o13 sharpened (not refuted).** Its `[OBSERVED]` a-start correction "`c∈{7,4}=3δ+1`, 17/19" is now
  **deterministic by parity: even a→+7, odd a→+4, 0 exceptions on every forward epoch-step of the orbit**
  — the floor structure of the two-branch ×3/2 map (x_e=−14, x_o=−7), not a stochastic pair. The prior
  "17/19" misses were epoch boundaries (refill re-seeds), exactly as flagged.
- **o14 confirmed + sharpened.** "`⌊3a/2⌋+6`, single correction, 15/18" is **exact** (single floor +6,
  both parities), the matched-pair o11 shape (x_e=−12, x_o=−11).
- No analogue of o11's refuted "k≡3 safe" claim exists for o13/o14 (the classification made no
  per-residue safe-row assertion for them); the fatal-set rows and marker-tail stability are reproduced.

## 6. Soundness ledger

- R1/R2 (o13), R1 + a-start (o14) `[PROVEN on the stated grids]` (exact runs, 0 mismatches). The
  fixed-point run closed forms + mirror coordinates `[PROVEN]` (2-line proof + exhaustive a≤2·10⁵). Halt
  mechanics `[PROVEN, unit-tested]` (o13 parity gate, o14 00-gap). The a-start recursion and all census
  counts `[OBSERVED]` on the stated horizons; their extension to unbounded a is NOT proven. Fatal configs
  `[PROVEN by run]`; **blank-tape reachability of any fatal config is `[OPEN]` = the protection**.
- Criticality verdicts are theorems GIVEN the labeled slopes; for o13/o14 they record the ABSENCE of a
  usable protection handle, strengthening no non-halting claim.

**No machine decided. No label upgraded.**

## Reproduce
`o1314_instr.py [N]` (milestone chains) · `o1314_epoch.py [N]` (epoch/counter/a-start census) ·
`o1314_rules.py [amax]` (R1/R2/a-start grids) · `o1314_fixedpoint.py [MMAX N]` (run closed forms,
mirrors, on-orbit) · `o1314_halt.py [N]` (halt mechanic + fatal probes). Basis:
`O11_REFILL_LAW_2026-07-08.md`, `MAHLER_SEA_CLASSIFICATION_2026-07-07.md`,
`ANTIHYDRA_LEDGER_UNIFICATION_2026-07-07.md`, `REDUCE_O11_O16.md`, `CATALOGUE_O13_SN.md`, `msea_*.py`.

**No machine decided. No label upgraded.**
