# New-math build synthesis: three coordinates, one wall — the sharpened design spec for the missing tool

*Synthesis of the 2026-07-09 construction attempt. Re-armed with the campaign's weapons (uniform fixed-point theorem,
run-cap, criticality, measured whiteness, exact reload map), we attacked the (K) wall as a genuine BUILD — not
reconnaissance — from three independent coordinates. All three land on the same wall, but they pincer it from three
sides, localizing the missing tool to one crisp object and yielding two genuine partial advances. This is the honest
outcome of "if new mathematics is needed, build it": the tool was not completed, but its design spec is now exact, and
the next attempt starts from a blueprint, not a fog. No machine decided.*

## The three coordinates and what each built

**(1) Excursion / reload (`NEWMATH_SOLENOID_BUILD`).** Built the **exact reload map** `[CONSTRUCTED-partial, verified]`:
the uniform run law induces `K_odd = v₂(3^{K_even}·u − 1)`, `K_even = v₂(3^{K_odd}·u' + 1)` — an exact arithmetic
handle on the excursion process that `EXCURSION_SYNTHESIS` treated abstractly. It **pins the conditional law** (K is
exactly geometric given the reload units `u_i` equidistributed on ℤ_q^×) and thereby **identifies the fatal adversary
precisely**: a ℤ_q^×-misalignment clustering `u_i` near `3^{∓K}`. Bonus: proved the {2,3}-smooth cryptids are ALL
lattice elements of ONE rank-2 host on ONE (2,3)-solenoid, so the **AIU neutral-obstruction partial extends from one
element to the entire {2,3}-host** — a genuine one-step publishable advance.

**(2) Potential / adversary (`O4_NEWMATH_BUILD`).** Re-identified the fatal functional exactly (o4 halts iff level-1
cylinder freq `#1/n ≥ 4/5`, a FIRST-moment quantity) and **proved both a-priori weapons orthogonal to it**: the
run-cap bounds only depth support (vacuous — fatal runs are shallow, depth 5), the magnitude bound is quadratic
(Cauchy–Schwarz gives only `n^{3/2}`, weaker than the fatal `0.8n` by `√n`). Constructed **two explicit cap-legal
fatal adversaries** driving `#1/n → 1` while respecting the run-cap at equality — answering "does the run-cap kill the
heavy-tail adversary": **NO** (o4's fatal mode is a shallow high-frequency conspiracy, not a deep run). The new
distinction: Antihydra's boundary is AT the annealed mean (zero margin → forced to E[K²]); **o4's is 2.4× the mean, so
its adversary is a genuine wrong-frequency large-deviation orbit — a strictly LOWER rung**.

**(3) Digit / autonomy (`NEWMATH_DIGIT_BRIDGE`).** Built the **autonomy-split bridge lemma** `[CONSTRUCTED-partial]`:
per-orbit p-adic valuation frequencies exist ONLY for constant-coefficient linear recurrences (residues eventually
periodic — Lucas `4·3^{k−1}`), and the o4 odometer's residues mod `3^k` are provably **NON-periodic** (the AEV
non-descent: dividing by 3 forces dependence on `G mod 3^{k+1}`). So the cryptid sits **provably on the OPEN side** of
the split: autonomous-recurrence quenched machinery cannot grip it, the fresh annealed frontier (Non-Archimedean
Koksma 2025, Digit Mixing 2026) reaches only a.e./a.s. **SML is empty** (`3W′=4W+f(ρ)` is non-autonomous — no fixed
characteristic polynomial — `W_n` is not a linear recurrence at all). Plus two soundness corrections: arXiv:2411.03468
"Mahler 3/2" is **WITHDRAWN** (not a proof); Drmota–Spiegelhofer confirmed as digit-SUM (Baker), not the run lemma.

## The one wall (where all three converge)

The three residuals are the SAME object in three descriptions:
$$\text{ℤ}_q^\times\text{-equidistribution of the reload units } u_i \;\Longleftrightarrow\; \text{a-priori } E[K^2]/\|d^{(k)}\|^2 \text{ bound} \;\Longleftrightarrow\; \text{effective base-}p/q\text{ normality mod } q^k \;=\; \text{AEV Conj 1.6} \;=\; (K).$$
Coordinate (1) leaves it free in the conditional law (u_i is the orbit's own higher bits); (2) leaves it free as the
first-moment margin (both weapons cap at support/second-moment, orthogonal); (3) leaves it free as the non-descent
(no analytic interpolant). None crosses.

## The sharpened design spec for the missing tool

Any tool that decides o4 (the easiest rung) must produce an **a-priori one-sided large-deviation bound
`freq{3∣W_n} ≤ 4/5 − ε` for the seed-57 orbit** (lossy tolerated, NOT equidistribution; single-orbit; base-4/3;
margin 2.4). By the pincer it must be:
- **non-spectral** (coisometry op-norm Lyapunov ≡ 0, `EUE_COISOMETRY_NOGO`),
- **non-structural** (No-Structure theorem: constant on M_feas),
- **non-autonomous-recurrence** (SML empty — `W_n` has no fixed characteristic polynomial),
- **neutral-blind-defeating** (AIU: the completion generator is zero-Lyapunov on one place, deformation-uniform),
- **not depth-only, not second-moment** (the two proven weapons are orthogonal to the first-moment fatal direction),
- and it must read the **specific seed's ℤ_q^× reload-unit trajectory** (the exact reload map is the handle).

The single un-blocked mechanism remains an **a-priori excursion/return-time estimate built ON the exact reload map**,
uniform over the orbit, forcing ℤ_q^× reload equidistribution — the `NEW_MATH_PROGRAM` object, now with an exact
arithmetic model of the excursion process (the reload map) it previously lacked, and pinned to the easiest margin
(o4's 2.4, not Antihydra's 0). Whether the reload map's arithmetic can force what the abstract excursion could not is
the precise open question the next attempt inherits.

## Verdict `[honest]`
**(b) — no crossing; three genuine partial advances + an exact design spec.** The tool is not built. But: the AIU
partial now covers the whole {2,3}-host; the excursion process has an exact arithmetic model (the reload map); o4 is
pinned as a strictly lower rung with an explicit lossy target; the fatal adversary is identified as a ℤ_q^×
misalignment; and the whole autonomy/analytic landscape is mapped (cryptid provably on the open side, two literature
corrections). The wall did not move — but it is now a blueprint, not a fog, and the next builder starts here.
**No machine decided. No label upgraded.**

## Reproduce / sources
`O4_NEWMATH_BUILD_2026-07-09.md` (+`o4_newmath_build.py`), `NEWMATH_SOLENOID_BUILD_2026-07-09.md`
(+`scratchpad/reload_verify.py`), `NEWMATH_DIGIT_BRIDGE_2026-07-09.md` (+`newmath_digit_bridge.py`); prior
`NEW_MATH_PROGRAM.md`, `EXCURSION_SYNTHESIS.md`, `AIU_NEUTRAL_OBSTRUCTION_THEOREM.md`, `O4_COBOUNDARY_LP_2026-07-08.md`,
`PAPER_MIRROR_LADDER.md`, `FREQUENCY_AXIS_PROBE_2026-07-08.md`. Kernel anchor: AEV arXiv:2510.11723.
