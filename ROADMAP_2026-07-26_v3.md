# BB(6) — roadmap v3 (2026-07-26)

Supersedes `ROADMAP_2026-07-25_v2.md`.  **No machine decided beyond `x2`; `C`'s label is HELD
pending its cold build.  Push to the PUBLIC origin remains HELD at the owner's instruction.**

---

## 0. The map, redrawn from measurement

```
                    BB(6) complete proof = 1104 holdouts = 909 distinct GRAPHS
                                            │
                ┌───────────────────────────┴────────────────────────────┐
                │                                                        │
    the named 19 cryptids                              the 1087 residual (~890 graphs)
    ALL (K) — MEASURED, not inferred                   composition first measured 2026-07-26
    every resolved ratio is 3/2, 4/3, 8/3, 5/2                │
    (odd-prime Mahler ⟹ carry-opaque)          ┌──────────────┴───────────────┐
                                    template island                   the rest
                                    ≥ 7 graphs (2,4)                  mostly odd-prime ratios
                                    x2 [PROVEN], C (audit),           (7/6, 9/8, 5/4, 8/7, 3/2,
                                    D, E, F, H confirmed; G, I        6/5, 4/3) ⟹ (K)-like;
                                    probable                          61 % still unmeasured
```

## 1. Done

| | |
|---|---|
| **`x2` PROVEN non-halting** | audited: cold build, instrument, anti-vacuity, red-team, cross-instrument; verified a genuine 1104 holdout |
| **`C` proven, label held** | `C_machine_nonhalt` `[propext, Quot.sound]`; the state-relabelling isomorphism is **axiom-free in Lean**; instrument audit clean; cold build running |
| **`TapeCalc`** | the machine-independent layer, 41 theorems, both sides, `sorry` 0, no `Classical.choice` |
| **the named 19 are `(K)`** | by direct measurement |
| **the residual is 909 graphs** | not 1104 independent machines |
| **the template island exists and is multi-graph** | ≥ 7 graphs |
| **the `(K)` side has zero unfired generators** | `#18` fired; o17 frontier probe fired and refuted |

## 2. Next, in order

| # | move | why |
|---|---|---|
| 1 | finish `C`'s cold build → upgrade or hold | the 2nd of 1104 |
| 2 | **`D`** — measure its phase skeleton and tiles, then build | a COMB doubler (pure dense `pow10` register) where `x2` is a CASCADE doubler; new word structure but on a much larger reusable base, and a comb register is *simpler* than a cascade |
| 3 | `E`, `F`, `H` likewise; settle `G`, `I` with longer runs | the rest of the island |
| 4 | finish the DEEP per-graph residual census (`3·10⁷`, running) | 61 % of the block is still unmeasured; this is what makes the island's size a fact rather than a lower bound |
| 5 | Tier 0 siege: Lean-ify the `(K)`-reduction of the confirmed five | converts external progress into BB(6) instantly |

## 3. What has NOT changed

`(K)` is untouched.  Every one of the 11 no-go layers stands; the two kernels remain final and
mutually independent; `#18` produced no new attack.  The reachable programme decides individual
residual machines — it does not approach the named 19, and it does not approach BB(6) itself.

## 4. Discipline record for the session

Six self-corrections, all committed with their corrections: two failed discriminators, two
epoch-alignment errors, "the tiles are different" (they were shared), and the framing of `C` as a
new machine (it is `x2`'s own graph).  Plus one wrong-directory write, caught and cleaned.
**The measurements were sound each time; the explanations were what needed fixing.**
