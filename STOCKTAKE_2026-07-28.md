# BB(6) 完全証明 — 全情報の棚卸しと攻め方の再検討(2026-07-28)

これまでの全攻撃で得た情報を一度棚卸しし、完全証明に向けた攻め方を入念に検討する。
**新しい測定を2つ行い、うち1つは残余の組成についての既存の読みを覆した。**

**No machine decided by this document. No label upgraded.**

---

## §0 規模

| | |
|---|---|
| 文書 | 552 md |
| 計器 | 806 py |
| Lean | 29,951 行 / **2,026 定理宣言** / `sorryAx` 0 / `Classical.choice` 0 |
| コミット | 949(うち直近2日で 34) |
| 決着済み機械 | **x2, C**(いずれも `[PROVEN]`、無条件) |

---

## §1 完全証明の構造 — 4義務と、その「種類の非対称性」

`lean/Completion.lean`:`BB6_eq_championSteps` の組立ては既に `[PROVEN]`。残るのは4義務。

| # | 義務 | 種類 | (K) 依存 | 規模 |
|---|---|---|---|---|
| 1 | `champion_lower` | 証明書1本 | **否** | 小 |
| 2 | `enumeration_upper` | 検証済み TNF 列挙器 | **否** | Coq-BB5 規模、多人年 |
| 3 | 17 named `*_nonhalt` | 未解決数学 | **是** | (K) |
| 4 | `holdouts1087_nonhalt` | 混合 | 大半が是 | 1087 台 |

### ここが決定的に重要

**完全証明は 1104 台すべてを要する。1台でも (K) が残れば完成しない。**

したがって:

> **島ルート(テンプレート機械の機械的決着)は、完全証明を完成させ得ない。**
> それができるのは「残余公理 `holdouts1087_nonhalt` の中身を減らす」ことだけである。

これは今セッションの成果(D をただ1つの義務に還元、18台にタイル付与)を評価する上での
前提であり、成果を否定するものではないが、**それらは完全証明への距離を1ミリも縮めていない**
ことを明記しておく。縮めるのは義務3・4の**全滅**だけである。

---

## §2 (K) 壁 — 「まだ見つかっていない」ではなく「存在し得ないと証明されている」

### 独立な核は2つだけ

| 核 | 内容 | 属する機 |
|---|---|---|
| **Mahler 核** | 単一軌道の `⌊μⁿ⌋ mod p` 等分布(`μ = 2^a/3^b`) | Antihydra, o10, o15, o18, o4, … |
| **o17 核** | 等長 base-3 carrying odometer の overflow halt | o17 |

### 証明済みの不可能性(ここが本質)

* **No-Structure 定理** — ergodic optimization `β = +1/2 > 0`、停止不動点 `o=1` で達成。
  **structure-only / all-orbits の証明は存在し得ない。**
* **specification による非普遍性** — Birkhoff 値を全実現する full-dimension の違反者が存在。
* **証明書複雑性階層** — `star-free ⊊ REG ⊊ SLIN ⊊ 2-automatic ⊊ CF ⊊ CS` の5段厳密分離が
  conjecture-free に確立。cryptid はその**外側**の軸に乗る。**有限証明書も存在し得ない。**
* **annealed 作用素が全奇指標を消す**(`L_ann χ_odd ≡ 0`)。新定理は annealed 縮小からは来ない。

### 攻撃の網羅性

~20 の独立攻撃路、~19 の分野横断(transfer operator, Gowers U²/U³, Christol automaticity,
Ostrowski/Baker, p-adic dynamics, measure rigidity, Siegel Wiener-Tauberian, 非Pisot diffraction …)
が**すべて同じ annealed/quenched 継ぎ目で停止**する。

### 継ぎ目の正確な所在

Antihydra のパリティは `c_n mod 2 = bit_n(8·3^n) ⊕ bit_n(T_n)`。
第1項は S-unit 桁(Subspace が触れる)、第2項は**自己誘導 carry-sum 桁**。
入力ビット列が*独立*なら Mauduit–Rivat の carry lemma で平衡する。
軌道は**自分自身のパリティ履歴を入力に戻す(閉ループ)**。その自己参照ビットの平衡証明が (K) である。

> **評決:残る経路は軌道特異的 — 単一軌道の正規性。これは有名な未解決問題クラスであり、
> 「まだ試していない道具」を提案するのは不誠実である。**

### 【今日の追加測定】named 19 への厳密インタフェース走査 — **0 ヒット**

今セッションで作った厳密機構分類器(`RungCalc.Atoms`、シミュレーション不要)を named 19 に
掛けた。**1台も comb-doubler 機構を持たない**(最高は o15/o18 の 3/6)。
「named 19 は全て (K)」という主張は、この**独立かつ厳密な検査を生き延びた**。

(範囲の明示:これは comb-doubler 機構についてのみの検査であり、他機構については何も言わない。)

---

## §3 【新規発見】残余の組成についての既存の読みは**確立していなかった**

`RESIDUAL_CENSUS_2026-07-26.md` は 5×10⁶ 歩のシミュレーションで epoch 幅比を測り、
「残余は `7/6, 9/8, 5/4, 8/7, 3/2, 6/5, 4/3` など**奇素数を含む比が支配的 ⟹ carry-opaque ⟹
(K)-like**」と結論した。ただし同文書は**673/1104(61%)が UNRESOLVED** であることも正直に記していた。

### 今日の測定

厳密インタフェース走査が見つけた **18台**を、その census がどう分類していたか照合した:

| census の判定 | 台数 |
|---|---:|
| **UNRESOLVED** | **14** |
| 2.064 / 2.069 / 1.944 | 3 |
| `2/1` | 1 |

> **厳密分類器が見つける機械の 78% を、シミュレーション census は取り逃していた。**

### 何が言えて、何が言えないか

**言える:** census の「残余は (K)-like が支配的」という結論は、**未解決 61% を根拠に含められない**。
そして未解決バケツの中に、機械的に決着可能な機械が現に入っていることが実証された。
**残余の真の組成は未知である。**

**言えない:** 残余が実は透明だらけである、とは言えない。18/1104 = 1.6% しか実証されていない。
これは「census が過小評価していた」であって「残余が透明」ではない。

**方法論上の含意:** 残余を分類する正しい方法は**シミュレーションではなく厳密な構造照合**である。
前者は 5×10⁶ 歩/台を費やして 61% を落とし、後者は瞬時で厳密。

---

## §4 今セッションで実際に変わったもの(島側)

| | |
|---|---|
| `RungCalc` | rung タイル・turn 相・`rung0`・`descend`・`tileIter` が**機械独立**に。1台あたり **6 rfl** |
| 到達 | **18/1104** がタイル取得(`lean/IslandTiles.lean`)。全18台が実軌道で 100% 発火 |
| `D` | **ただ1つの義務 `EpochLaw`** に還元(`DReduce`)。entry・組立て・語族はすべて `[PROVEN]` |
| epoch | 4法則が epoch の **99.98%** を覆う。`mainDouble` 1本で **95.1%** |
| 新ライブラリ補題 | `TapeCalc.steps_rext`(右文脈の任意拡張輸送) |

**ただし §1 のとおり、これらは完全証明への距離を縮めていない。**

---

## §5 新しい攻め方 — 候補と評決

### A. 機構インタフェース・ライブラリによる厳密 census 【推奨・新規】

今セッションの `Atoms` が実証した方法を一般化する。証明済みの各機構
(comb doubler = `RungCalc.Atoms`、cascade doubler = x2 の機構)から**インタフェースを抽出**し、
残余全体を**シミュレーションなしで厳密に分類**する。

* **根拠:** §3。この方法は既存 census より実測で 4〜6 倍広く見える。
* **コスト:** 低〜中(x2 のインタフェース抽出が主作業。x2 の Lean は既にある)。
* **期待値:** **未知 — そしてそれが要点**。残余の真の組成を知る唯一の手段である。
* **評決:採用。最優先。**

### B. 機構の自動発見 【新規・高価値】

インタフェースを人手で抽出する代わりに、軌道から**原子候補を自動抽出**する:
短い状態ループで、テープ効果が文脈に一様なもの(`∀ b L R`)を探し、fold を自動生成する。
今セッションで手作業でやったこと(`d_rung_atoms.py` → `Atoms` → 6 rfl)の機械化。

* **根拠:** 残余は 1000 台超。人手で機構を1つずつ見つけるのは不可能。
* **コスト:** 中。
* **評決:採用。A の次。** これが効けば島ルートは初めてスケールする。

### C. 近傍バンド(4/6・3/6)【小】

行動的走査で **5/6 は 0 台**と確定済み(1原子差の機械は存在しない)。
4/6(4台)・3/6(36台)は最低2つの新原子仕様を要する。
* **評決:低優先。** A・B が先。

### D. (K) 側の新しい武器 【無い、と言う根拠がある】

§2 のとおり、structure-only / all-orbits / 有限証明書は**いずれも存在し得ないことが証明済み**で、
~19 分野が同じ継ぎ目で停止している。**新武器を提案するのは不誠実。**
唯一の正直な選択肢は外部連携(AEV 著者 / Einsiedler–Lindenstrauss–Host 圏)であり、
これは既に `bb6-external-policy` の下で owner 判断待ちである。

### E. named 19 への厳密走査 【今日実施 — 0 ヒット】

コストがゼロなので実施した。反例なし。(K) 分類は強化された。
* **評決:完了。** 他機構のインタフェースができたら再走査する価値がある。

### F. 目標の再設定 【限界価値が低い】

`enumeration_upper` は (K) 独立で、「BB(6) = championSteps unless one of these 1104 runs forever」
という**無条件・完全機械検証済みの還元**は達成可能。
* **ただし** bbchallenge / Coq-BB5 コミュニティが同じことを実施中であり、限界価値は低い。
* **評決:自前実装は非推奨。** 証明書輸入方式なら検討余地あり。

---

## §6 推奨計画

| 順 | 手 | 目的 | コスト |
|---|---|---|---|
| **1** | **x2(cascade)機構のインタフェース抽出 + 残余への厳密走査** | 残余の真の組成を知る | 低〜中 |
| **2** | **原子の自動発見器** | 島ルートを初めてスケールさせる | 中 |
| 3 | 1・2 の結果で島の到達可能範囲が判明 → その時点で再判断 | — | — |
| 4 | `D` の `EpochLaw`(ブロック遷移パラメータの閉形式が唯一の未知) | 3台目の決着 | 中〜大 |
| 5 | (K) — 外部連携のみ | — | owner 判断 |

**1 と 2 を先に置く理由:** 現在の最大の不確実性は「(K) をどう破るか」ではなく
**「残余の何割が機械的に決着可能なのか」**である。それが分からないまま `D` の残りに数セッション
投じるのは、投資判断として順序が逆である。§3 が示したとおり、その数字は**まだ誰も知らない**。

---

## §7 正直な総括

1. **BB(6) の完全証明は現行数学では到達不能である。** これは推測ではなく、
   structure-only / all-orbits / 有限証明書の各クラスについて**証明された不可能性**に基づく。
   残る経路は単一軌道の正規性であり、有名な未解決問題クラスである。

2. **島ルートは完全証明を完成させ得ない**(§1)。それは残余公理の中身を減らすだけである。
   今セッションの成果(18台、`D` の1義務化)もこの制約の下にある。

3. **しかし、島ルートの到達可能範囲は誰も知らない。** §3 の測定は、既存 census が
   その範囲を系統的に過小評価していたことを示した。**これが本棚卸しの最大の発見であり、
   次に取るべき手を決めている。**

4. **プログラムの防御可能な成果は、依然として構造限界定理(result B)である** —
   「BB(6) の Collatz 核が何と等価で、なぜ構造的に耐性を持つのか」の特徴づけ。
   これは cryptid を1台も解かずに成立する original contribution であり、
   今セッションの機械独立層(`TapeCalc` + `RungCalc`)はその工学側の対応物である。

---

*本文書のための新規測定:名前付き19機への厳密インタフェース走査(0ヒット)、
厳密ヒット18台 vs シミュレーション census の照合(14/18 が UNRESOLVED)。
計器はいずれも `atoms_flex_scan.py` / `residual_census.py` の再利用。*

**No machine decided by this document. No label upgraded. `D` and the residual remain `[OPEN]`.**
