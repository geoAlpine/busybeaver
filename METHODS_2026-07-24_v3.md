# 手法書 METHODS 改訂第3版 — BB(6)/x2 解明の明確な手続き (2026-07-24 深夜)

**本書は `METHODS_2026-07-24.md`（第2版 + M8 追補）を置き換える**（旧版は記録保存・バナー付与）。
改訂根拠は第2版制定後わずか1日の実行データ: **27 コミット、GREEN 定理 +40 本超（T7 計 307、
build 57/57）、E1–E3・O1–O2 閉鎖、予告先行測定 5/5 的中、同日訂正 3 件（全て主張前捕捉）、
指数バグ 4 件（全て compile 捕捉）**。ラベル規律不変: [PROVEN]（Lean green,
`[propext, Quot.sound]`）/ [MEASURED] / [UNMEASURED]（新設・下記 M0）/ [OPEN]。
**本書は手法の記録であり、機械も決定せず、ラベルも昇格しない。x2 は [OPEN] のまま。**

---

## 第0部 — 改訂の根拠データ（第2版制定後 1 日分の解析）

### 0.1 第2版の手法が実戦で挙げた成績（定量）

| 手法 | 成績 |
|---|---|
| **M8**（名前付き配置） | 採用後の組立 **初回コンパイル緑 4/4**（topEntryEven の M6_even/teAB/teBC/teCD、carry46+carryFold46、cross17+crossCarry、odAB/odBC/odCD）。採用前日に丸1セッション失敗した同一対象が採用当日に即閉鎖 |
| **M6**（改名一般化） | **BlankNorm を 2 度不要化**：`p1tLL`（左基底自由化）が zeros 1/zeros 11 ギャップを、`m1casc_descCascade_tail`（尾自由化）が TAIL ギャップを、存在量化子ゼロで閉鎖 |
| **M4**（予告先行） | **5/5 的中**：奇 g=7 (778 716)・偶 g=8 (99 112)・frameDigit g=4・奇 span 48 485・g=6 相境界。加えて奇分解の算術が独立確認済み閉形式と 4 世代厳密一致 |
| **M7**（合成スコープ検査） | 過大主張 2 件を **Lean 記述前に**捕捉（O4「evenSpine そのまま」/ oddSeam≠seam74） |
| **M5**（撤回の作法） | 同日訂正 3 件を全て記録付きで処理。うち 1 件は「撤回自体の訂正」（下記 0.3-i） |

### 0.2 新しい資産（第2版以降の GREEN、主要なもの）

- **偶数倍加フェーズ端到端**: `doubPhaseEven`（M6→M1枠）。部品: `topEntryEven/LT`、
  `p1t`/`p1tLL`/`rUnitsFold`/`bridge`/`eChewFold`、`evenSpine`、S1 恒等式
  `descMarkInner_eq_ladderMarker`、`seam74`/`topRungToMilestone`、E2 恒等式 `tlM_spineMk`
- **奇数 topEntry 端到端**: `topEntryOddFull`（M6→descIn(g+6)）。新規機械は
  `carry46`/`carryFold46`（46 歩キャリータイル、奇数枝唯一の Θ 規模新規）と
  `cross17`/`odTurn` のみ。`p1t`/`rUnitsFold`/`eChewFold` は偶数から**そのまま**
- **H**: `hlow_padded`（∃j≤m）、`hlow_to_phase`、`hlow_j_ge`（j≥10 下界、`steps_left_mono`）
- **奇数マーカー代数（rfl）**: 奇 cReg11 marker = `0 0 · frameL 2 X = 0 0 1 · U`、
  `U = 0 1 0 1 0 0 · frameL 1 X` → `topRung`(∀U) は奇数で**発火**、`seam74` は**不発火**

### 0.3 新しい失敗モード（各 1 回発生・全て回収済み — 本版の規律改訂の根拠）

- **(i) Θ 前置詞の読み違い**: `regenIn 5` の marker 先頭 60 セルを読んで「1^9 が消えた」と誤診
  → 実は `ladderMarker`（1979 セル）の櫛を見ていた。**既知長の生成前置詞を剥がしてから
  payload を読む**（M1-5 新設）。この誤診が evenSpine 撤回→撤回の訂正という往復を生んだ
- **(ii) 未測定構造の価格付け**: O2 を「低リスク」→「第二の大未知」→「タイル1個」と 1 日で
  3 回改訂。原因は**コスト（閉形式）だけ測って構造を測っていない**対象に確信度を付けたこと
  → [UNMEASURED] ラベル新設（M0-3）
- **(iii) 固定マーカー定理の再利用主張**: `topRungToMilestone` はマーカーをリテラルに固定して
  おり、∀ に見えて ∀ でない。**セル単位アラインメント監査**（M7-4 新設）が 2 件捕捉
- **(iv) 族の指数規約ミス**: `descCascade d` の先頭ブロックは `2^{d+3}−3` — これを 2 回
  取り違え（compile が捕捉）。**索引表の参照を義務化**（M9 新設）
- **(v) BlankNorm ∃j は固定 IN と噛み合わない**: `steps_lpad_zeros` の j は非決定で、下界は
  `steps_left_mono` で出るが上界は一般に出ない。**源流パディング（M6-5）を第一選択に**

---

## 第I部 — 共通規律 M0–M9（M0/M1/M6/M7 改訂、M9 新設）

### M0. 測定第一の原則（改訂: [UNMEASURED] ラベル）
不変部は第2版どおり。**追記:**
3. **構造未測定の対象に確信度を与えない。** コスト閉形式が確認済みでも、内部構造を一度も
   走査していない区間は [UNMEASURED] と明記し、リスク表・義務表には「構造測定済み? Y/N」列を
   付す。価格付けは構造プローブ後のみ（根拠: O2/O4 の同日 3 回転）。

### M1. 計器検証（改訂: 前置詞剥がし）
1–4 は第2版どおり。**追記:**
5. **marker/payload を読む前に、既知長の生成前置詞（`pow01`/`ladderMarker`/`rUnitsDep` 等、
   長さ Θ(2^k)）をその既知長で剥がす。** 先頭 N セルの窓読みは前置詞を読む（0.3-i）。
6. 「族が出現しない」を主張するときは**フェーズ全域走査**を要する（奇 cReg12 の不在は
   全域走査で確定した — 部分走査の不在は証拠でない）。

### M2. カット原則（不変）
実績追加: 奇数 topEntry も 6 相の一様カット（`88+11+15g+17+46(2^{g+7}−7)+6+6·2^{g+5}`）で
確認済み閉形式と 4 世代厳密一致。

### M3 / M3′（不変）
チャンク校正表・5 フィールド抽出・機械生成、全て第2版どおり。実績: E1/E2/fixedEnd に続き
p1t/カット群で再実証。

### M4. 反空虚性統制（不変・実績更新）
予告先行 5/5。**独立確認済み閉形式との照合は、フィット由来でない相互検証として最強**
（奇分解: 閉形式は構造測定の前に g=7 out-of-sample 確認済みだった）。

### M5. 撤回の作法（改訂: 撤回自体の再測定）
不変部は第2版どおり。**追記:** 撤回も主張である。**撤回の根拠測定に M1 を適用**してから
公開する（evenSpine「能動レジスタ」撤回は M1-5 違反の測定に基づいており、撤回自体を同日
訂正することになった）。往復は全て記録した — 偽の証明はゼロを維持。

### M6. 改名による一般化（**昇格: 継ぎ目閉鎖の第一選択**）
手順 1–4 は第2版どおり。**追記:**
5. **源流でパディングせよ (pad at the source)。** 2 定理が末尾ブランクの有限リスト表現で
   食い違うとき、境界で BlankNorm の ∃j により整合を試みる前に、**上流の生産者の不活性
   レジスタを自由変数化**（LL/TT スレッディング）して、必要なブランクを最初から運ばせる。
   BlankNorm の ∃j は固定 IN の消費者と噛み合わない（上界が一般に出ない — 0.3-v）。
   実績: E2/E3 は BlankNorm ゼロで閉じた。BlankNorm は義務 H（init 接続）専用に温存。

### M7. 合成スコープ検査（改訂: セル単位アラインメント監査）
1–3 は第2版どおり。**追記:**
4. **リテラル・マーカー監査。** 定理がマーカー/レジスタを具体語で固定している場合
   （`topRungToMilestone` の 9 セル seam、`seam74` の 11 セル窓）、その定理の「再利用」を
   主張する前に、**実測レジスタと定理のリテラルをセル単位でアラインする**（Python 文字列
   照合または Lean `rfl` 代数）。∀ 変数の位置と具体語の位置を混同しない。
   実績: O4 過大主張と oddSeam≠seam74 を Lean 記述前に捕捉（コスト: 数分）。

### M8. 名前付き配置の規律（不変 — 成績 4/4 で実証済み）
第2版追補どおり。実績が规律を裏付けた: 採用前の失敗セッションと採用後の初回緑を対照されたい。

### M9. 【新設】レジスタ索引表の参照
**指数・添字を書く前に必ずこの表を引く**（違反 2 件が同日発生、いずれも compile 捕捉）:

| 族 | 先頭ブロック | 内部カスケード | 備考 |
|---|---|---|---|
| `descCascade d` | `2^{d+3}−3` | `descCascade (d−1)` | `descCascade (d+1) = 1^{2^{d+3}−3} · 00 · descCascade d` |
| `descIn k` | `2^k−3` | `descCascade (k−2)` | 左櫛 `pow01 (2^{k−1})` |
| `regenIn k` | `ones (2^k−3)`(左) | `descCascade (k−4)` | 分離子 `01001`、櫛 `pow01(2^{k−1}−2)` |
| `cascadeReg k` | `1^{2^k−3}` | `descCascade (k−3)` | 左櫛 `pow01(Lc+2^{k−1}−2)` |
| `m1casc (h+1) (h+2) T` | — | `= 00 · descCascade h · T` | 証明済み恒等式 |
| `frameL j X` | — | `(1010100)^j · X` | 偶 seam `= 001·001010100·frameL j`、奇 `= 001·010100·frameL 1` 位相 |

---

## 第II部 — x2 完全証明（第3版実行計画）

**確定アーキテクチャ（両パリティ、全て実測整合済み）:**

```
M1(g) ─h_low ✅─▶ M6(g) ─topEntry ✅✅(両パリティ)─▶ descIn ─headLaw ✅─▶ regenIn 5
      ─ladder ✅─▶ cascadeReg(g+8)[両パリティ正準] ─topRung ✅(∀U,奇でも発火)─▶ top
      ─seam74 ✅(偶) / oddSeam [UNMEASURED→要測定](奇)─▶ tail-IN ─tailLaw ✅─▶ M1(g+1)枠
偶数フェーズ全体 = doubPhaseEven ✅
```

### 残義務表（構造測定列付き — M0-3）

| # | 義務 | 構造測定済み? | 手法 | 規模 | 状態 |
|---|---|---|---|---|---|
| R1 | **`oddSeam`** — 奇の頂上→tail-IN 固定エピソード（`seam74` の奇対応物） | **N**（端点未 bracket） | 端点実測 → M3′ | seam74 級 | [OPEN] 律速 |
| R2 | `oddTopRungToMilestone` = `topRung ∘ oddSeam ∘ tailLaw` | 部品は Y | 3 行合成 (M8) | 小 | R1 待ち |
| R3 | `oddSpine` = `headToLadder`(∀marker) ∘ R2 | Y | M8 | 小 | R2 待ち |
| R4 | `doubPhaseOdd` = `topEntryOddFull ∘ oddSpine` | Y | M8; 反空虚 8 476 791 | 中 | R3 待ち |
| R5 | **H 上界** j=10 pin | Y（pos 不変・j=m−6 実測） | **推奨: 低フェーズ 5 補題の p1tLL 式 `++LL` 移植**（M6、実績あり）。BlankNorm 下界+上界路線は上界が出ず不採 | 中 | [OPEN] |
| R6 | H 組立: `hlow16 ∘ doubPhase{Even,Odd}` = M1(g)→M1(g+1) realized 両パリティ | Y | M8 + M6-5 | 中 | R4/R5 待ち |
| R7 | F: `x2_nonhalt` 発火 + cold audit + red-team | — | — | 小 | 最後 |

**順序: R1（構造測定から）→ R2→R3→R4 ‖ R5 → R6 → R7。** R1 と R5 は独立で並行可。

### R1 の測定指針（次セッションの初手）
奇 g=3 の cReg11 @5 018 196 → tail-top @11 329 137 の 6 310 941 歩のうち、topRung 相当
（topgrind+exit ≈ 6 310 921?）の後の**端点 2 点を bracket** する。偶の類推では
`topGrindSteps 11 + exitSteps 12 = 4 188 167 + 2 122 754 = 6 310 921`、残り **20 歩**が
oddSeam 候補 — まず `5018196 + 6310921` の配置をフルシェイプで取れ（M1-5 適用、
`rUnitsDep`/`pow10` 前置詞を既知長で剥がすこと）。

### 付録 — Lean 戦術工学（追記分のみ）
- **∃ witness は必ず明示項**（再確認 ×3）。`⟨_, _, ?_⟩` は `some` の下では合成不能。
  存在文をやめて明示 pos/marker で述べるのが最速（`topEntryOddFull` 方式）。
- 冪分割の定型: `rw [show a+k = (a+j)+d, Nat.pow_add, show (2:Nat)^d = c from rfl, Nat.mul_comm]`。
- fold の `rw` が照合しないときは、fold の綴り（`2*m+r` 等）に **`show` で先に張り替える**か、
  全引数明示で `exact`。
- `lakefile` 編集は **defaultTargets と `[[lean_lib]]` の両方**を確認（T7H 事件: 片方だけ
  編集され未ビルドのまま「緑」と誤認しかけた — ビルドジョブ数の増加を必ず確認）。

## 第III部 / 第IV部（変更なし）
carry 島転写は x2 後。(K) は外部ペイロード 3 本（送信 gated）。墓標に再挑戦しない。

---

## 一枚図（第3版）

| 対象 | 状態 | 次の一手 |
|---|---|---|
| x2 偶数フェーズ | **端到端 GREEN** (`doubPhaseEven`) | — |
| x2 奇数 topEntry | **端到端 GREEN** (`topEntryOddFull`) | — |
| x2 奇数 spine | R1 oddSeam のみ新規 | **端点 bracket 測定**（指針上記） |
| x2 H | 下界緑・[10,16] 挟撃 | R5: 低フェーズ 5 補題の `++LL` 移植 |
| x2 発火 | R6→R7 | — |
| carry 島 / (K) / o7/SN/o17 | 変更なし | — |

**機械は未決定。ラベルは未昇格。x2 は [OPEN]、BB(6) は (K) 壁の背後。**
