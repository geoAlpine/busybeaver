# 全体戦略レビュー (2026-07-24)

2 日間の集中実行（33 コミット、GREEN 定理 ~40 本追加、撤回 2 件・較正 5 件すべて主張前捕捉）
の後の立ち止まり。**何を最適化しているのか・x2 続行が正しいのか・やり方をどう変えるか**を
データで判定する。ラベル規律不変。**本書は戦略文書であり、機械も決定せず、ラベルも昇格しない。**

---

## 1. 目的関数の再確認 — BB(6) 完全証明への正直な地図

```
BB(6) = [A] 17 named cryptids + [B] ~1087 holdouts + [C] champion 検証 + [D] 全列挙
```

- **[A] のうち 14 機 = (K) 壁**：Mahler 3/2 型正規性に還元済み [PROVEN]。内部手法の不可能性
  メタ定理群により、**このプログラム内では閉じない**。生きたレバーは外部ペイロード 3 本
  （定理E / M₂ᵒᵈᵈ / o4-first スカラー）のみで、送信はオーナーの per-send go-ahead 待ち。
- **[A] の非-(K) 残り**：x2（integer doubler）、o7/SpaceNeedle、o17。o7/SN/o17 は既知手法が
  反駁済みで**新着想待ち**。
- **x2 だけが「数学が全部測定・確認済みで、残りが機械作業のみ」の対象**。これが 2 日前に
  x2 に全力を向けた根拠であり、今日も変わっていない。

**結論（目的関数）**: BB(6) 全体はこのプログラム単独では閉じない（(K) 壁 + community 規模の
[B][D]）。現実的な最適化対象は：
1. **x2 を無条件定理にする**（17 named の 1 機を決定、テンプレート法の最大実証）
2. その道具箱で **carry 島 ~5–8 機**を続けて決定（[B] を削る新可決定領域）
3. (K) への外部ペイロードを**送信可能な状態に維持**（送信判断はオーナー）
4. 不可能性定理群・還元連鎖という**既に銀行済みの成果**を汚さない（偽証明ゼロ維持）

---

## 2. 資産の棚卸し（2026-07-24 時点）

### GREEN（cold build 45/45、sorry/native_decide/Mathlib なし）
- **X2.lean 基盤**: `regenLaw_closed ∀k`、`h_init`(realized)、`h_low ∀g` 両パリティ、BlankNorm 一式
- **T7 偶数枝**: `descLaw`/`descFold_all`(頭部降下 ∀k)、`headLaw`(頭部全体)、
  `ladderToCascade`(梯子)、`RegenLawGen ∀k`+`topRung`(頂上段)、`seam74`、`tailLaw`(尾部 ∀j)、
  `topRungToMilestone`(出口全体)、**S1 恒等式**(`descMarkInner_eq_ladderMarker`)、
  `headToLadder`、`evenSpine`(BlankNorm ブリッジ待ちの合成体)、S3 の 4 相原始定理 +
  3 グルー補題(`m1casc_descCascade`/`bridge`/`M6_even`)
- 歩数反空虚性はすべて実測 span を `decide` で再現（2,117,332 / 33,650,954 等）

### MEASURED（未証明・確認済み）
- topEntry 閉形式・両パリティ（予告先行で out-of-sample 的中、8 世代）
- tail 構造 `27(g−1)+110`、正準性規則（g+8 まで／頂上非正準）、櫛保存則
- アンカー M1(1..8)

### 未着手 / OPEN
- 奇数枝の Lean 形状（梯子頂上 `0^13` 型、`1^20` 装飾、M6 奇数形の輸送）
- 義務 H（族 → init）、最終監査

---

## 3. 残義務の正確な勘定（METHODS 0.4: 義務数で数える）

| # | 義務 | 種別 | 見積 | リスク |
|---|---|---|---|---|
| E1 | `topEntryEven` の Lean 配管（数学済・#eval 済） | 配管 | 1 セッション | 低（内容ゼロ） |
| E2 | BlankNorm ブリッジ（`zeros 1`↔`zeros 11`、TAIL `[]`↔pads）— `steps_lpad_zeros`/`steps_rpad_zeros` は既存 | 配管 | 小 | 低 |
| E3 | S4-偶数: `h_low_even ∘ topEntryEven ∘ evenSpine` を M1(g)→M1(g+1) に連結 + 枠 OUT ↔ `M1(g+1)` 定義の照合 | 合成+1照合 | 中 | 中（照合に驚き 1 個の余地） |
| O1 | 奇数枝の canonicity 走査（梯子中腹は奇 g でも正準か — 未測定！） | 測定 | 小 | — |
| O2 | 奇数 M6 形（`pow10 10`、ブロック −13）の topEntry 4 相測定+構築 | 測定+構築 | 1–2 セッション | 中 |
| O3 | 奇数の梯子頂上→tail 入口（正準 cascadeReg なし、`0^13` 型） | 測定+構築 | 中 | **高**（新しい形） |
| O4 | S4-奇数組立 | 合成 | 中 | 中 |
| H | 義務 H: 3 仮説を realized 族で統一（`realizeM1_port`+BlankNorm、`probe_g01` から） | 構築 | 1–2 セッション | 中 |
| F | `x2_nonhalt` 発火 + cold audit + red-team | 監査 | 小 | 低 |

**計 ~9 義務。** 直近ペース（2 日で ~8 義務相当）から、驚きゼロなら ~1 週間規模、
各継ぎ目に驚き 1 個の実績則を織り込むと **1.5–2 週間規模のセッション作業**。
数学的未知は **O3 のみ**（奇数頂上の形 — ただし h_low_odd の前例があり、種別は既知）。

---

## 4. 選択肢の評価

| 選択肢 | 価値 | コスト | リスク | 判定 |
|---|---|---|---|---|
| **(A) x2 続行**（現路線） | 17 named の 1 機決定 + 島への道具箱 + 手法の最大実証 | 1.5–2 週 | O3 の形状のみ | **採用** |
| (B) 外部ペイロード送信準備 | (K) 14 機への唯一の道 | 小（準備のみ） | 送信はオーナー判断 | **並行可**（準備まで） |
| (C) o17 ≥298 下界の形式化 | 小さな銀行（E-tier） | 小 | 低 | x2 後の息抜き候補 |
| (D) o7/SN 再攻撃 | — | 大 | 既知手法反駁済み | 却下（新着想待ち） |
| (E) (K) 内部再挑戦 | — | — | 不可能性定理群に抵触 | 却下（墓標） |
| (F) x2 中断・全面見直し | — | 2 日分の勢い喪失 | 残りは機械作業のみなのに | 却下 |

**判定: (A) 続行 + (B) の準備のみ並行。** x2 は「残りに数学的未知がほぼ無い」唯一の対象で
あり、ここで止める合理性がない。ただし§6 の撤退基準を明文化する。

---

## 5. プロセス修正 — 「配管税」の削減（METHODS 追補 M8 として銀行）

2 日間の失敗はすべて 2 種に還元される：
**(i) 部分シグネチャ測定**（M1 強化済み）と **(ii) 無名リテラル上の rewrite 照合失敗**。

(ii) が topEntryEven を止めた。修正は機械的：

### M8. 名前付き配置の規律 (named-config discipline)
1. **合成は名前付き `def` 族の上でのみ行う**。`⟨.E, p, ⟨…⟩⟩` の無名リテラルを跨ぐ `rw` は禁止。
   各 phase の IN/OUT を `def phaseXIn (params) : Cfg` として先に定義し、原始定理をその形で
   **restate** してから合成する（`descIn`/`regenIn`/`cascadeReg` が成功例、`topEntryEven` の
   4 相が失敗例）。
2. **算術の綴りはファイル内で単一形に固定**（例: `2*h+2+k` のまま、途中で `2h+10` 等に
   簡約しない）。簡約は最後の `cfgPos`/`omega` でのみ。
3. **∃ の witness は必ず明示項**。`⟨_, _, ?_⟩` は `some` の下の metavar を合成できない。
4. **配管予算則**: 1 合成に ~45 分超の反復を要したら停止 → 緑部品を銀行 → sorry 明記の
   スタブ（defaultTargets 外）→ 名前付き配置で再アプローチ。（今日実施した通り。規則化。）

---

## 6. 撤退・再評価基準（事前に固定）

- **O3（奇数頂上）が h_low_odd 型の装飾で説明できない新機構を見せたら**：その時点で停止し、
  偶数枝のみの部分結果を文書化してから再評価（偶数枝だけでは非停止証明にならないことを
  明記した上で）。
- **義務 H が §5am 族の再設計を要求したら**：H の設計文書を先に書き、実装前にレビュー。
- 上記以外の「感触が悪い」だけでの路線変更はしない（本レビューで判定済みのため）。

---

## 7. マイルストーン（順序固定）

1. **M-even**: E1→E2→E3 ⟹ `h_doub` 偶数 g 端到端（`M1(g) → M1(g+1)`、偶数）
   — これが次の対外的に意味のある区切り
2. **M-odd**: O1→O2→O3→O4 ⟹ `h_doub ∀g`
3. **M-fire**: H→F ⟹ `x2_nonhalt` 発火、cold audit、red-team、**そこで初めてラベル議論**
4. **M-island**: 道具箱の carry 島転写（別レビューで対象機選定）

---

## 8. 一枚図（更新）

| 対象 | 状態 | 次の一手 |
|---|---|---|
| x2 偶数枝 | 配管 3 件のみ | E1: named-config で topEntryEven |
| x2 奇数枝 | 測定 1 + 構築 3 | O1 canonicity 走査から |
| x2 H/監査 | 未着手 | M-odd 後 |
| carry 島 | 待機 | M-fire 後 |
| (K) 14 機 | 外部のみ | ペイロード整備（送信は gated） |
| o7/SN/o17 | 新着想待ち | （o17 下界形式化は息抜き候補） |

**機械は未決定。ラベルは未昇格。x2 は [OPEN]、BB(6) は (K) 壁の背後 — この 2 行は
x2 が発火するまで全文書で維持する。**
