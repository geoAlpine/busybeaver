# T7 boundary #3 — the real orbit satisfies ladderFold's premise (2026-07-22)

`ladderFold` (`lean/T7Ladder.lean`) proves the ABSTRACT ladder: from a `regenIn b` config carrying
the nested marker `ladderMarker b n` and pad `ladderPad b n`, `n` rungs reach `regenIn (b+n)`. The
honest caveat was: **does the real `M6(g)` orbit actually PRESENT that nested data?** This measures
it directly on the g=2 orbit (`x2t7_boundary.py`, on the instrument-validated `x2t7_lib.py`).

## Result — YES, exactly, for both marker and pad `[MEASURED]`

At each `regenIn k` rung I parsed the config `left = ones(2^k−3) · 01001 · (01)^{2^{k-1}-2} · MARKER_k`
and the right-tape zeros-run `z_k`, then tested the two `ladderFold` recursions:

**Marker** — `marker_k = layer_k ++ marker_{k+1}` where `layer_k = 0 0 1 (01)^{2^k−2}`:

| k | `marker_k` starts with `layer_k`? | rest = `marker_{k+1}`? |
|---|---|---|
| 5 | yes (`00 1 (01)^30`) | **EXACT** (len 18307 = 18307) |
| 6 | yes (`00 1 (01)^62`) | **EXACT** (18180) |
| 7 | yes (`00 1 (01)^126`) | **EXACT** (17925) |
| 8 | yes (`00 1 (01)^254`) | **EXACT** (17414) |
| 9 | yes (`00 1 (01)^510`) | **EXACT** (16391) |

The marker peels **exactly one `ascMarker` layer per rung** — it IS `ladderMarker 5 n ++ base`, on
the nose.

**Pad** — `z_k − z_{k+1} = 2^{k-1}` (the net of `ladderStep`'s built-in `2^{k-1}+9` and the extra
`zeros(2^k)`):

| k | `z_k` | `z_k − z_{k+1}` | `2^{k-1}` |
|---|---|---|---|
| 5 | 14326 | 16 | 16 ✓ |
| 6 | 14310 | 32 | 32 ✓ |
| 7 | 14278 | 64 | 64 ✓ |
| 8 | 14214 | 128 | 128 ✓ |
| 9 | 14086 | 256 | 256 ✓ |
| 10 | 13830 | 512 | 512 ✓ |

Every rung matches. (The net `2^{k-1}` is exactly what `ladderStep` predicts:
`(2^{k-1}+9 + 2^k) − (2^k+9) = 2^{k-1}` — regenIn's built-in pad plus the `zeros(2^k)` extra, minus
the next rung's built-in `2^k`. So the pad accounting of `ladderFold` is confirmed, not contradicted.)

## What this closes

**Boundary #3's nesting premise is CONFIRMED on the real orbit.** `ladderFold` is not vacuous: the
marker and pad the real `M6(g)` orbit carries at `regenIn 5` genuinely have the form
`ladderMarker 5 n ++ base_marker` / `ladderPad 5 n ++ base_pad`. So the `∀n` interior fold applies to
the actual machine trajectory, not merely to an abstract config.

## What remains for T7 (now sharper)

The residual is three concrete boundary objects, none needing a new transport:
1. **The head** `M6(g) → regenIn 5`. Lengths 6580 / 53382 / 25024 at g=2/3/4 — parity-split, no
   closed form yet. This must produce the `regenIn 5` config with the full nested marker/pad.
2. **The base** `(base_marker, base_pad)` at the top rung, and the **top-rung exit → M1(g+1)** (the
   g=3 `+80` anomaly lives here).
3. **The `∀g` count**: that generation g uses `n = g+4` rungs (levels 5…g+9), i.e. the nested-stack
   depth equals the generation index. Measured shape (marker length shrinking to a base) is
   consistent; the exact `n(g)` and base remain to be pinned.

**No machine is decided. No label is upgraded.** All `[MEASURED]` on the g=2 orbit; the recursion is
confirmed at every measured rung, consistent with the Lean `ladderFold`.
