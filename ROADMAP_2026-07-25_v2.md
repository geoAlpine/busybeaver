# BB(6) — roadmap after the x2 closure day (2026-07-25, v2)

Supersedes `ROADMAP_2026-07-25.md`, which predates everything below.
**No machine decided beyond `x2`. No label upgraded. Push to the PUBLIC origin is HELD.**

---

## 0. What is now settled

| | |
|---|---|
| **`x2` is PROVEN non-halting** | `x2_nonhalt_blank : ∀ N, steps N init ≠ none`, `[propext, Quot.sound]`, Mathlib-free, cold build 63/63, `sorryAx` 0.  Full audit in `X2_CLOSURE_AUDIT_2026-07-25.md`. |
| **`x2` was genuinely open** | novelty oracle: `is_holdout(x2) = True` against the 1104 list (April-2026 snapshot); all 18 named cryptids also True, so the oracle is not vacuously accepting. |
| **The named 19 are the `(K)` hard core** | MEASURED, not extrapolated: every epoch ratio that resolved is `3/2`, `4/3`, `8/3` or `5/2` — a Mahler multiplier with an odd prime ⟹ carry-opaque.  **Not one shows ratio 2.** |
| **The "slow-width" bucket was an artifact** | `x2` — a PROVEN clean doubler — reproduces the `≈1.1` width-ratio signature under a crude extractor. |
| **The residual has 909 graphs, not 1104 entries** | graph-canonical dedup over all 720 state permutations: 170 graphs occur more than once, covering 365 entries. |
| **`TapeCalc` exists** | the whole machine-independent layer — boundary congruences forward/reverse/**exact**, both sides, translation, monotonicity, `nonhalt_of_invariant` — 41 theorems, `sorry` 0, no `Classical.choice`. |

## 1. Where the template island is — current honest state

The pre-flight screened the 1104 residual for the `x2` signature.  Three candidates survived the
loose filter; on inspection:

| candidate | verdict |
|---|---|
| **B** | **REFUTED** at longer runs (phase change: `w = 4.68`, `t = 26.1`). |
| **C** | **is `x2`'s own transition graph**, started in `x2`'s state `B` (verified: `σ(x2) = C` on all six rows).  A distinct 1104 entry with a genuinely different orbit, but **not a new machine family**. |
| **A** | the **only genuinely independent** candidate (graph-canonical form differs from `x2`'s).  Its `(2, 4)` signature was `(≈2, ≈4)` but noisy.  **Decisive long run in progress.** |

> **So there is, as of now, NO confirmed second template machine family.**  Whether the method
> generalises beyond `x2`'s graph is exactly the open question, and candidate `A` is the test.

A **deep screen** (2·10⁷ steps, the strict `(2,4)` signature) is running over all 1104; at 200/1104
it has returned one hit — `x2` itself, i.e. the calibration.

## 2. Deciding `C` — what it actually costs

`C`'s non-halting ⟺ `x2`'s `step` never halting from `⟨B, 0, blank⟩`.  Measured milestone words:

    x2-from-A even : 0^21 uUnits 1 (1 :: 0^10 ++ ones (2^10−3) ++ m1casc …)   = MEven 0 []
    x2-from-A odd  : 0^21 uUnits 2 (1 :: 0^4  ++ pow10 6 ++ …)                = MOdd  0 R
    x2-from-B      : 0^21 uUnits 1 (1 :: 0^10 ++ ones (2^11−3) ++ m1casc …)
    x2-from-B      : 0^21 uUnits 0 (1 :: 0^4  ++ pow10 6 ++ …)

Same shape; the `uUnits` count is **one lower relative to the block scale**.  The low-phase cores
(`h_low_even_core k`, `h_low_odd_core k`) are `∀k`, so they re-instantiate — but the `uUnits`
parity shifts, so **the roles of "even" and "odd" swap**.  That is a genuine re-derivation of the
phase assembly, not a free call.  (An earlier note that `x2`'s theorems could simply be re-invoked
was wrong and is corrected here.)

Cost estimate: a fraction of the `x2` effort — the tile spans, word algebra and skeleton are all
shared and measured — but not zero.

## 3. Recommended order from here

| # | move | why |
|---|---|---|
| 1 | finish the deep 1104 screen; **re-aggregate by graph-canonical form** | otherwise start-state variants get counted as discoveries — the exact trap `C` set today |
| 2 | settle candidate **A** | the only test of whether the method leaves `x2`'s graph |
| 3 | decide **`C`** (`x2` from state `B`) | takes the `x2` graph from 1 to 2 of the 1104; cheap relative to `x2` |
| 4 | Tier 0 siege: Lean-ify the `(K)`-reduction of the confirmed five | converts external progress into BB(6) instantly |
| 5 | `#18` premise-complement mining | last unfired generator |

If **A** fails, the honest conclusion is that the template island is one graph, and the programme's
reachable set is `x2` + its start-variants — worth stating plainly rather than papering over.

## 4. Discipline note

Five self-corrections were made and committed today: two failed discriminators, two epoch-alignment
errors, one "the tiles are different" inference, and the `C`-is-a-new-machine framing.  All are in
the git history with their corrections.  **The measurements were sound each time; the explanations
were what needed fixing.**
